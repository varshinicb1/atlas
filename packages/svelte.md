---
kind: Package
id: package:svelte
name: "svelte Knowledge Package"
version: "0.1.0"
purpose: |
  Auto-generated knowledge package crawled from https://svelte.dev/docs.
  Covers 5 pages of documentation.
problem_solved: |
  Provides structured knowledge extracted from the official svelte.dev documentation
  for use in AI agent decision-making.
install: |
  ```bash
  atlas install svelte.md
  ```
concepts:
  - name: "Documentation"
    id: concept:page_0_svelte
    description: |
      Extracted from documentation: Documentation
  - name: "I’m brand new here"
    id: concept:page_1_svelte
    description: |
      Extracted from documentation: I’m brand new here
  - name: "I’m migrating an app from Svelte 4"
    id: concept:page_2_svelte
    description: |
      Extracted from documentation: I’m migrating an app from Svelte 4
  - name: "I just want to try it out"
    id: concept:page_3_svelte
    description: |
      Extracted from documentation: I just want to try it out
  - name: "I’m a Large Language Model (LLM)"
    id: concept:page_4_svelte
    description: |
      Extracted from documentation: I’m a Large Language Model (LLM)
  - name: "I’m looking for the old docs"
    id: concept:page_5_svelte
    description: |
      Extracted from documentation: I’m looking for the old docs
  - name: "Help! I’m stuck"
    id: concept:page_6_svelte
    description: |
      Extracted from documentation: Help! I’m stuck
  - name: "Svelte Documentation for LLMs"
    id: concept:page_7_svelte
    description: |
      Extracted from documentation: Svelte Documentation for LLMs
  - name: "Documentation Sets"
    id: concept:page_8_svelte
    description: |
      Extracted from documentation: Documentation Sets
  - name: "Individual Package Documentation"
    id: concept:page_9_svelte
    description: |
      Extracted from documentation: Individual Package Documentation
  - name: "Notes"
    id: concept:page_10_svelte
    description: |
      Extracted from documentation: Notes
  - name: "Start of Svelte documentation"
    id: concept:page_11_svelte
    description: |
      Extracted from documentation: Start of Svelte documentation
  - name: "Overview"
    id: concept:page_12_svelte
    description: |
      Extracted from documentation: Overview
  - name: "Getting started"
    id: concept:page_13_svelte
    description: |
      Extracted from documentation: Getting started
  - name: "Alternatives to SvelteKit"
    id: concept:page_14_svelte
    description: |
      Extracted from documentation: Alternatives to SvelteKit
  - name: "Editor tooling"
    id: concept:page_15_svelte
    description: |
      Extracted from documentation: Editor tooling
  - name: "Getting help"
    id: concept:page_16_svelte
    description: |
      Extracted from documentation: Getting help
  - name: ".svelte files"
    id: concept:page_17_svelte
    description: |
      Extracted from documentation: .svelte files
  - name: "`<script>`"
    id: concept:page_18_svelte
    description: |
      Extracted from documentation: `<script>`
  - name: "`<script module>`"
    id: concept:page_19_svelte
    description: |
      Extracted from documentation: `<script module>`
  - name: "`<style>`"
    id: concept:page_20_svelte
    description: |
      Extracted from documentation: `<style>`
  - name: ".svelte.js and .svelte.ts files"
    id: concept:page_21_svelte
    description: |
      Extracted from documentation: .svelte.js and .svelte.ts files
  - name: "What are runes?"
    id: concept:page_22_svelte
    description: |
      Extracted from documentation: What are runes?
  - name: "$state"
    id: concept:page_23_svelte
    description: |
      Extracted from documentation: $state
  - name: "Deep state"
    id: concept:page_24_svelte
    description: |
      Extracted from documentation: Deep state
  - name: "Classes"
    id: concept:page_25_svelte
    description: |
      Extracted from documentation: Classes
  - name: "Built-in classes"
    id: concept:page_26_svelte
    description: |
      Extracted from documentation: Built-in classes
  - name: "`$state.raw`"
    id: concept:page_27_svelte
    description: |
      Extracted from documentation: `$state.raw`
  - name: "`$state.snapshot`"
    id: concept:page_28_svelte
    description: |
      Extracted from documentation: `$state.snapshot`
  - name: "`$state.eager`"
    id: concept:page_29_svelte
    description: |
      Extracted from documentation: `$state.eager`
  - name: "Passing state into functions"
    id: concept:page_30_svelte
    description: |
      Extracted from documentation: Passing state into functions
  - name: "Passing state across modules"
    id: concept:page_31_svelte
    description: |
      Extracted from documentation: Passing state across modules
  - name: "$derived"
    id: concept:page_32_svelte
    description: |
      Extracted from documentation: $derived
  - name: "`$derived.by`"
    id: concept:page_33_svelte
    description: |
      Extracted from documentation: `$derived.by`
  - name: "Understanding dependencies"
    id: concept:page_34_svelte
    description: |
      Extracted from documentation: Understanding dependencies
  - name: "Overriding derived values"
    id: concept:page_35_svelte
    description: |
      Extracted from documentation: Overriding derived values
  - name: "Deriveds and reactivity"
    id: concept:page_36_svelte
    description: |
      Extracted from documentation: Deriveds and reactivity
  - name: "Destructuring"
    id: concept:page_37_svelte
    description: |
      Extracted from documentation: Destructuring
  - name: "Update propagation"
    id: concept:page_38_svelte
    description: |
      Extracted from documentation: Update propagation
  - name: "$effect"
    id: concept:page_39_svelte
    description: |
      Extracted from documentation: $effect
  - name: "Understanding lifecycle"
    id: concept:page_40_svelte
    description: |
      Extracted from documentation: Understanding lifecycle
  - name: "`$effect.pre`"
    id: concept:page_41_svelte
    description: |
      Extracted from documentation: `$effect.pre`
  - name: "`$effect.tracking`"
    id: concept:page_42_svelte
    description: |
      Extracted from documentation: `$effect.tracking`
  - name: "`$effect.pending`"
    id: concept:page_43_svelte
    description: |
      Extracted from documentation: `$effect.pending`
  - name: "`$effect.root`"
    id: concept:page_44_svelte
    description: |
      Extracted from documentation: `$effect.root`
  - name: "When not to use `$effect`"
    id: concept:page_45_svelte
    description: |
      Extracted from documentation: When not to use `$effect`
  - name: "$props"
    id: concept:page_46_svelte
    description: |
      Extracted from documentation: $props
  - name: "Fallback values"
    id: concept:page_47_svelte
    description: |
      Extracted from documentation: Fallback values
  - name: "Renaming props"
    id: concept:page_48_svelte
    description: |
      Extracted from documentation: Renaming props
  - name: "Rest props"
    id: concept:page_49_svelte
    description: |
      Extracted from documentation: Rest props
  - name: "Updating props"
    id: concept:page_50_svelte
    description: |
      Extracted from documentation: Updating props
  - name: "Type safety"
    id: concept:page_51_svelte
    description: |
      Extracted from documentation: Type safety
  - name: "`$props.id()`"
    id: concept:page_52_svelte
    description: |
      Extracted from documentation: `$props.id()`
  - name: "$bindable"
    id: concept:page_53_svelte
    description: |
      Extracted from documentation: $bindable
  - name: "$inspect"
    id: concept:page_54_svelte
    description: |
      Extracted from documentation: $inspect
  - name: "$inspect(...).with"
    id: concept:page_55_svelte
    description: |
      Extracted from documentation: $inspect(...).with
  - name: "$inspect.trace(...)"
    id: concept:page_56_svelte
    description: |
      Extracted from documentation: $inspect.trace(...)
  - name: "$host"
    id: concept:page_57_svelte
    description: |
      Extracted from documentation: $host
  - name: "Basic markup"
    id: concept:page_58_svelte
    description: |
      Extracted from documentation: Basic markup
  - name: "Tags"
    id: concept:page_59_svelte
    description: |
      Extracted from documentation: Tags
  - name: "Element attributes"
    id: concept:page_60_svelte
    description: |
      Extracted from documentation: Element attributes
  - name: "Component props"
    id: concept:page_61_svelte
    description: |
      Extracted from documentation: Component props
  - name: "Spread attributes"
    id: concept:page_62_svelte
    description: |
      Extracted from documentation: Spread attributes
  - name: "Events"
    id: concept:page_63_svelte
    description: |
      Extracted from documentation: Events
  - name: "Event delegation"
    id: concept:page_64_svelte
    description: |
      Extracted from documentation: Event delegation
  - name: "Text expressions"
    id: concept:page_65_svelte
    description: |
      Extracted from documentation: Text expressions
  - name: "Comments"
    id: concept:page_66_svelte
    description: |
      Extracted from documentation: Comments
  - name: "{#if ...}"
    id: concept:page_67_svelte
    description: |
      Extracted from documentation: {#if ...}
  - name: "{#each ...}"
    id: concept:page_68_svelte
    description: |
      Extracted from documentation: {#each ...}
  - name: "Keyed each blocks"
    id: concept:page_69_svelte
    description: |
      Extracted from documentation: Keyed each blocks
  - name: "Each blocks without an item"
    id: concept:page_70_svelte
    description: |
      Extracted from documentation: Each blocks without an item
  - name: "Else blocks"
    id: concept:page_71_svelte
    description: |
      Extracted from documentation: Else blocks
  - name: "{#key ...}"
    id: concept:page_72_svelte
    description: |
      Extracted from documentation: {#key ...}
  - name: "{#await ...}"
    id: concept:page_73_svelte
    description: |
      Extracted from documentation: {#await ...}
  - name: "{#snippet ...}"
    id: concept:page_74_svelte
    description: |
      Extracted from documentation: {#snippet ...}
  - name: "Snippet scope"
    id: concept:page_75_svelte
    description: |
      Extracted from documentation: Snippet scope
  - name: "Passing snippets to components"
    id: concept:page_76_svelte
    description: |
      Extracted from documentation: Passing snippets to components
  - name: "Explicit props"
    id: concept:page_77_svelte
    description: |
      Extracted from documentation: Explicit props
  - name: "Implicit props"
    id: concept:page_78_svelte
    description: |
      Extracted from documentation: Implicit props
  - name: "Implicit `children` snippet"
    id: concept:page_79_svelte
    description: |
      Extracted from documentation: Implicit `children` snippet
  - name: "Optional snippet props"
    id: concept:page_80_svelte
    description: |
      Extracted from documentation: Optional snippet props
  - name: "Typing snippets"
    id: concept:page_81_svelte
    description: |
      Extracted from documentation: Typing snippets
  - name: "Exporting snippets"
    id: concept:page_82_svelte
    description: |
      Extracted from documentation: Exporting snippets
  - name: "Programmatic snippets"
    id: concept:page_83_svelte
    description: |
      Extracted from documentation: Programmatic snippets
  - name: "Snippets and slots"
    id: concept:page_84_svelte
    description: |
      Extracted from documentation: Snippets and slots
  - name: "{@render ...}"
    id: concept:page_85_svelte
    description: |
      Extracted from documentation: {@render ...}
  - name: "Optional snippets"
    id: concept:page_86_svelte
    description: |
      Extracted from documentation: Optional snippets
  - name: "{@html ...}"
    id: concept:page_87_svelte
    description: |
      Extracted from documentation: {@html ...}
  - name: "Styling"
    id: concept:page_88_svelte
    description: |
      Extracted from documentation: Styling
  - name: "{@attach ...}"
    id: concept:page_89_svelte
    description: |
      Extracted from documentation: {@attach ...}
  - name: "Attachment factories"
    id: concept:page_90_svelte
    description: |
      Extracted from documentation: Attachment factories
  - name: "Inline attachments"
    id: concept:page_91_svelte
    description: |
      Extracted from documentation: Inline attachments
  - name: "Conditional attachments"
    id: concept:page_92_svelte
    description: |
      Extracted from documentation: Conditional attachments
  - name: "Passing attachments to components"
    id: concept:page_93_svelte
    description: |
      Extracted from documentation: Passing attachments to components
  - name: "Controlling when attachments re-run"
    id: concept:page_94_svelte
    description: |
      Extracted from documentation: Controlling when attachments re-run
  - name: "Creating attachments programmatically"
    id: concept:page_95_svelte
    description: |
      Extracted from documentation: Creating attachments programmatically
  - name: "Converting actions to attachments"
    id: concept:page_96_svelte
    description: |
      Extracted from documentation: Converting actions to attachments
  - name: "{@const ...}"
    id: concept:page_97_svelte
    description: |
      Extracted from documentation: {@const ...}
  - name: "{@debug ...}"
    id: concept:page_98_svelte
    description: |
      Extracted from documentation: {@debug ...}
  - name: "{let/const ...}"
    id: concept:page_99_svelte
    description: |
      Extracted from documentation: {let/const ...}
  - name: "bind:"
    id: concept:page_100_svelte
    description: |
      Extracted from documentation: bind:
  - name: "Function bindings"
    id: concept:page_101_svelte
    description: |
      Extracted from documentation: Function bindings
  - name: "`<input bind:value>`"
    id: concept:page_102_svelte
    description: |
      Extracted from documentation: `<input bind:value>`
  - name: "`<input bind:checked>`"
    id: concept:page_103_svelte
    description: |
      Extracted from documentation: `<input bind:checked>`
  - name: "`<input bind:indeterminate>`"
    id: concept:page_104_svelte
    description: |
      Extracted from documentation: `<input bind:indeterminate>`
  - name: "`<input bind:group>`"
    id: concept:page_105_svelte
    description: |
      Extracted from documentation: `<input bind:group>`
  - name: "`<input bind:files>`"
    id: concept:page_106_svelte
    description: |
      Extracted from documentation: `<input bind:files>`
  - name: "`<select bind:value>`"
    id: concept:page_107_svelte
    description: |
      Extracted from documentation: `<select bind:value>`
  - name: "`<audio>`"
    id: concept:page_108_svelte
    description: |
      Extracted from documentation: `<audio>`
  - name: "`<video>`"
    id: concept:page_109_svelte
    description: |
      Extracted from documentation: `<video>`
  - name: "`<img>`"
    id: concept:page_110_svelte
    description: |
      Extracted from documentation: `<img>`
  - name: "`<details bind:open>`"
    id: concept:page_111_svelte
    description: |
      Extracted from documentation: `<details bind:open>`
  - name: "`window` and `document`"
    id: concept:page_112_svelte
    description: |
      Extracted from documentation: `window` and `document`
  - name: "Contenteditable bindings"
    id: concept:page_113_svelte
    description: |
      Extracted from documentation: Contenteditable bindings
  - name: "Dimensions"
    id: concept:page_114_svelte
    description: |
      Extracted from documentation: Dimensions
  - name: "bind:this"
    id: concept:page_115_svelte
    description: |
      Extracted from documentation: bind:this
  - name: "bind:_property_ for components"
    id: concept:page_116_svelte
    description: |
      Extracted from documentation: bind:_property_ for components
  - name: "use:"
    id: concept:page_117_svelte
    description: |
      Extracted from documentation: use:
  - name: "Typing"
    id: concept:page_118_svelte
    description: |
      Extracted from documentation: Typing
  - name: "transition:"
    id: concept:page_119_svelte
    description: |
      Extracted from documentation: transition:
  - name: "Local vs global"
    id: concept:page_120_svelte
    description: |
      Extracted from documentation: Local vs global
  - name: "Built-in transitions"
    id: concept:page_121_svelte
    description: |
      Extracted from documentation: Built-in transitions
  - name: "Transition parameters"
    id: concept:page_122_svelte
    description: |
      Extracted from documentation: Transition parameters
  - name: "Custom transition functions"
    id: concept:page_123_svelte
    description: |
      Extracted from documentation: Custom transition functions
  - name: "Transition events"
    id: concept:page_124_svelte
    description: |
      Extracted from documentation: Transition events
  - name: "in: and out:"
    id: concept:page_125_svelte
    description: |
      Extracted from documentation: in: and out:
  - name: "animate:"
    id: concept:page_126_svelte
    description: |
      Extracted from documentation: animate:
  - name: "Animation Parameters"
    id: concept:page_127_svelte
    description: |
      Extracted from documentation: Animation Parameters
  - name: "Custom animation functions"
    id: concept:page_128_svelte
    description: |
      Extracted from documentation: Custom animation functions
  - name: "style:"
    id: concept:page_129_svelte
    description: |
      Extracted from documentation: style:
  - name: "class"
    id: concept:page_130_svelte
    description: |
      Extracted from documentation: class
  - name: "Attributes"
    id: concept:page_131_svelte
    description: |
      Extracted from documentation: Attributes
  - name: "Objects and arrays"
    id: concept:page_132_svelte
    description: |
      Extracted from documentation: Objects and arrays
  - name: "The `class:` directive"
    id: concept:page_133_svelte
    description: |
      Extracted from documentation: The `class:` directive
  - name: "await"
    id: concept:page_134_svelte
    description: |
      Extracted from documentation: await
  - name: "Synchronized updates"
    id: concept:page_135_svelte
    description: |
      Extracted from documentation: Synchronized updates
  - name: "Concurrency"
    id: concept:page_136_svelte
    description: |
      Extracted from documentation: Concurrency
  - name: "Indicating loading states"
    id: concept:page_137_svelte
    description: |
      Extracted from documentation: Indicating loading states
  - name: "Error handling"
    id: concept:page_138_svelte
    description: |
      Extracted from documentation: Error handling
  - name: "Server-side rendering"
    id: concept:page_139_svelte
    description: |
      Extracted from documentation: Server-side rendering
  - name: "Forking"
    id: concept:page_140_svelte
    description: |
      Extracted from documentation: Forking
  - name: "Caveats"
    id: concept:page_141_svelte
    description: |
      Extracted from documentation: Caveats
  - name: "Breaking changes"
    id: concept:page_142_svelte
    description: |
      Extracted from documentation: Breaking changes
  - name: "Scoped styles"
    id: concept:page_143_svelte
    description: |
      Extracted from documentation: Scoped styles
  - name: "Specificity"
    id: concept:page_144_svelte
    description: |
      Extracted from documentation: Specificity
  - name: "Scoped keyframes"
    id: concept:page_145_svelte
    description: |
      Extracted from documentation: Scoped keyframes
  - name: "Global styles"
    id: concept:page_146_svelte
    description: |
      Extracted from documentation: Global styles
  - name: ":global(...)"
    id: concept:page_147_svelte
    description: |
      Extracted from documentation: :global(...)
  - name: ":global"
    id: concept:page_148_svelte
    description: |
      Extracted from documentation: :global
  - name: "Custom properties"
    id: concept:page_149_svelte
    description: |
      Extracted from documentation: Custom properties
  - name: "Nested `<style>` elements"
    id: concept:page_150_svelte
    description: |
      Extracted from documentation: Nested `<style>` elements
  - name: "`<svelte:boundary>`"
    id: concept:page_151_svelte
    description: |
      Extracted from documentation: `<svelte:boundary>`
  - name: "Properties"
    id: concept:page_152_svelte
    description: |
      Extracted from documentation: Properties
  - name: "`pending`"
    id: concept:page_153_svelte
    description: |
      Extracted from documentation: `pending`
  - name: "`failed`"
    id: concept:page_154_svelte
    description: |
      Extracted from documentation: `failed`
  - name: "`onerror`"
    id: concept:page_155_svelte
    description: |
      Extracted from documentation: `onerror`
  - name: "Using `transformError`"
    id: concept:page_156_svelte
    description: |
      Extracted from documentation: Using `transformError`
  - name: "`<svelte:window>`"
    id: concept:page_157_svelte
    description: |
      Extracted from documentation: `<svelte:window>`
  - name: "`<svelte:document>`"
    id: concept:page_158_svelte
    description: |
      Extracted from documentation: `<svelte:document>`
  - name: "`<svelte:body>`"
    id: concept:page_159_svelte
    description: |
      Extracted from documentation: `<svelte:body>`
  - name: "`<svelte:head>`"
    id: concept:page_160_svelte
    description: |
      Extracted from documentation: `<svelte:head>`
  - name: "`<svelte:element>`"
    id: concept:page_161_svelte
    description: |
      Extracted from documentation: `<svelte:element>`
  - name: "`<svelte:options>`"
    id: concept:page_162_svelte
    description: |
      Extracted from documentation: `<svelte:options>`
  - name: "Stores"
    id: concept:page_163_svelte
    description: |
      Extracted from documentation: Stores
  - name: "When to use stores"
    id: concept:page_164_svelte
    description: |
      Extracted from documentation: When to use stores
  - name: "svelte/store"
    id: concept:page_165_svelte
    description: |
      Extracted from documentation: svelte/store
  - name: "`writable`"
    id: concept:page_166_svelte
    description: |
      Extracted from documentation: `writable`
  - name: "`readable`"
    id: concept:page_167_svelte
    description: |
      Extracted from documentation: `readable`
  - name: "`derived`"
    id: concept:page_168_svelte
    description: |
      Extracted from documentation: `derived`
  - name: "`readonly`"
    id: concept:page_169_svelte
    description: |
      Extracted from documentation: `readonly`
  - name: "`get`"
    id: concept:page_170_svelte
    description: |
      Extracted from documentation: `get`
  - name: "Store contract"
    id: concept:page_171_svelte
    description: |
      Extracted from documentation: Store contract
  - name: "Context"
    id: concept:page_172_svelte
    description: |
      Extracted from documentation: Context
  - name: "`setContext` and `getContext`"
    id: concept:page_173_svelte
    description: |
      Extracted from documentation: `setContext` and `getContext`
  - name: "Using context with state"
    id: concept:page_174_svelte
    description: |
      Extracted from documentation: Using context with state
  - name: "Component testing"
    id: concept:page_175_svelte
    description: |
      Extracted from documentation: Component testing
  - name: "Replacing global state"
    id: concept:page_176_svelte
    description: |
      Extracted from documentation: Replacing global state
  - name: "Lifecycle hooks"
    id: concept:page_177_svelte
    description: |
      Extracted from documentation: Lifecycle hooks
  - name: "`onMount`"
    id: concept:page_178_svelte
    description: |
      Extracted from documentation: `onMount`
  - name: "`onDestroy`"
    id: concept:page_179_svelte
    description: |
      Extracted from documentation: `onDestroy`
  - name: "`tick`"
    id: concept:page_180_svelte
    description: |
      Extracted from documentation: `tick`
  - name: "Deprecated: `beforeUpdate` / `afterUpdate`"
    id: concept:page_181_svelte
    description: |
      Extracted from documentation: Deprecated: `beforeUpdate` / `afterUpdate`
  - name: "Chat window example"
    id: concept:page_182_svelte
    description: |
      Extracted from documentation: Chat window example
  - name: "Imperative component API"
    id: concept:page_183_svelte
    description: |
      Extracted from documentation: Imperative component API
  - name: "`mount`"
    id: concept:page_184_svelte
    description: |
      Extracted from documentation: `mount`
  - name: "`unmount`"
    id: concept:page_185_svelte
    description: |
      Extracted from documentation: `unmount`
  - name: "`render`"
    id: concept:page_186_svelte
    description: |
      Extracted from documentation: `render`
  - name: "`hydrate`"
    id: concept:page_187_svelte
    description: |
      Extracted from documentation: `hydrate`
  - name: "Hydratable data"
    id: concept:page_188_svelte
    description: |
      Extracted from documentation: Hydratable data
  - name: "Serialization"
    id: concept:page_189_svelte
    description: |
      Extracted from documentation: Serialization
  - name: "CSP"
    id: concept:page_190_svelte
    description: |
      Extracted from documentation: CSP
  - name: "Best practices"
    id: concept:page_191_svelte
    description: |
      Extracted from documentation: Best practices
  - name: "`$state`"
    id: concept:page_192_svelte
    description: |
      Extracted from documentation: `$state`
  - name: "`$derived`"
    id: concept:page_193_svelte
    description: |
      Extracted from documentation: `$derived`
  - name: "`$effect`"
    id: concept:page_194_svelte
    description: |
      Extracted from documentation: `$effect`
  - name: "`$props`"
    id: concept:page_195_svelte
    description: |
      Extracted from documentation: `$props`
  - name: "`$inspect.trace`"
    id: concept:page_196_svelte
    description: |
      Extracted from documentation: `$inspect.trace`
  - name: "Snippets"
    id: concept:page_197_svelte
    description: |
      Extracted from documentation: Snippets
  - name: "Each blocks"
    id: concept:page_198_svelte
    description: |
      Extracted from documentation: Each blocks
  - name: "Using JavaScript variables in CSS"
    id: concept:page_199_svelte
    description: |
      Extracted from documentation: Using JavaScript variables in CSS
  - name: "Styling child components"
    id: concept:page_200_svelte
    description: |
      Extracted from documentation: Styling child components
  - name: "Async Svelte"
    id: concept:page_201_svelte
    description: |
      Extracted from documentation: Async Svelte
  - name: "Avoid legacy features"
    id: concept:page_202_svelte
    description: |
      Extracted from documentation: Avoid legacy features
  - name: "Testing"
    id: concept:page_203_svelte
    description: |
      Extracted from documentation: Testing
  - name: "Unit and component tests with Vitest"
    id: concept:page_204_svelte
    description: |
      Extracted from documentation: Unit and component tests with Vitest
  - name: "Using runes inside your test files"
    id: concept:page_205_svelte
    description: |
      Extracted from documentation: Using runes inside your test files
  - name: "Component tests with Storybook"
    id: concept:page_206_svelte
    description: |
      Extracted from documentation: Component tests with Storybook
  - name: "End-to-end tests with Playwright"
    id: concept:page_207_svelte
    description: |
      Extracted from documentation: End-to-end tests with Playwright
  - name: "TypeScript"
    id: concept:page_208_svelte
    description: |
      Extracted from documentation: TypeScript
  - name: "`<script lang=\"ts\">`"
    id: concept:page_209_svelte
    description: |
      Extracted from documentation: `<script lang="ts">`
  - name: "Preprocessor setup"
    id: concept:page_210_svelte
    description: |
      Extracted from documentation: Preprocessor setup
  - name: "Using Vite"
    id: concept:page_211_svelte
    description: |
      Extracted from documentation: Using Vite
  - name: "Using other build tools"
    id: concept:page_212_svelte
    description: |
      Extracted from documentation: Using other build tools
  - name: "tsconfig.json settings"
    id: concept:page_213_svelte
    description: |
      Extracted from documentation: tsconfig.json settings
  - name: "Typing `$props`"
    id: concept:page_214_svelte
    description: |
      Extracted from documentation: Typing `$props`
  - name: "Generic `$props`"
    id: concept:page_215_svelte
    description: |
      Extracted from documentation: Generic `$props`
  - name: "Typing wrapper components"
    id: concept:page_216_svelte
    description: |
      Extracted from documentation: Typing wrapper components
  - name: "Typing `$state`"
    id: concept:page_217_svelte
    description: |
      Extracted from documentation: Typing `$state`
  - name: "The `Component` type"
    id: concept:page_218_svelte
    description: |
      Extracted from documentation: The `Component` type
  - name: "Enhancing built-in DOM types"
    id: concept:page_219_svelte
    description: |
      Extracted from documentation: Enhancing built-in DOM types
  - name: "Custom elements"
    id: concept:page_220_svelte
    description: |
      Extracted from documentation: Custom elements
  - name: "Component lifecycle"
    id: concept:page_221_svelte
    description: |
      Extracted from documentation: Component lifecycle
  - name: "Component options"
    id: concept:page_222_svelte
    description: |
      Extracted from documentation: Component options
  - name: "Caveats and limitations"
    id: concept:page_223_svelte
    description: |
      Extracted from documentation: Caveats and limitations
  - name: "Browser support"
    id: concept:page_224_svelte
    description: |
      Extracted from documentation: Browser support
  - name: "Exceptions"
    id: concept:page_225_svelte
    description: |
      Extracted from documentation: Exceptions
  - name: "Svelte 4 migration guide"
    id: concept:page_226_svelte
    description: |
      Extracted from documentation: Svelte 4 migration guide
  - name: "Minimum version requirements"
    id: concept:page_227_svelte
    description: |
      Extracted from documentation: Minimum version requirements
  - name: "Browser conditions for bundlers"
    id: concept:page_228_svelte
    description: |
      Extracted from documentation: Browser conditions for bundlers
  - name: "Removal of CJS related output"
    id: concept:page_229_svelte
    description: |
      Extracted from documentation: Removal of CJS related output
  - name: "Stricter types for Svelte functions"
    id: concept:page_230_svelte
    description: |
      Extracted from documentation: Stricter types for Svelte functions
  - name: "Custom Elements with Svelte"
    id: concept:page_231_svelte
    description: |
      Extracted from documentation: Custom Elements with Svelte
  - name: "SvelteComponentTyped is deprecated"
    id: concept:page_232_svelte
    description: |
      Extracted from documentation: SvelteComponentTyped is deprecated
  - name: "Transitions are local by default"
    id: concept:page_233_svelte
    description: |
      Extracted from documentation: Transitions are local by default
  - name: "Default slot bindings"
    id: concept:page_234_svelte
    description: |
      Extracted from documentation: Default slot bindings
  - name: "Preprocessors"
    id: concept:page_235_svelte
    description: |
      Extracted from documentation: Preprocessors
  - name: "New eslint package"
    id: concept:page_236_svelte
    description: |
      Extracted from documentation: New eslint package
  - name: "Other breaking changes"
    id: concept:page_237_svelte
    description: |
      Extracted from documentation: Other breaking changes
  - name: "Svelte 5 migration guide"
    id: concept:page_238_svelte
    description: |
      Extracted from documentation: Svelte 5 migration guide
  - name: "Reactivity syntax changes"
    id: concept:page_239_svelte
    description: |
      Extracted from documentation: Reactivity syntax changes
  - name: "let → $state"
    id: concept:page_240_svelte
    description: |
      Extracted from documentation: let → $state
  - name: "$: → $derived/$effect"
    id: concept:page_241_svelte
    description: |
      Extracted from documentation: $: → $derived/$effect
  - name: "export let → $props"
    id: concept:page_242_svelte
    description: |
      Extracted from documentation: export let → $props
  - name: "Event changes"
    id: concept:page_243_svelte
    description: |
      Extracted from documentation: Event changes
  - name: "Component events"
    id: concept:page_244_svelte
    description: |
      Extracted from documentation: Component events
  - name: "Bubbling events"
    id: concept:page_245_svelte
    description: |
      Extracted from documentation: Bubbling events
  - name: "Event modifiers"
    id: concept:page_246_svelte
    description: |
      Extracted from documentation: Event modifiers
  - name: "Multiple event handlers"
    id: concept:page_247_svelte
    description: |
      Extracted from documentation: Multiple event handlers
  - name: "Snippets instead of slots"
    id: concept:page_248_svelte
    description: |
      Extracted from documentation: Snippets instead of slots
  - name: "Default content"
    id: concept:page_249_svelte
    description: |
      Extracted from documentation: Default content
  - name: "Multiple content placeholders"
    id: concept:page_250_svelte
    description: |
      Extracted from documentation: Multiple content placeholders
  - name: "Passing data back up"
    id: concept:page_251_svelte
    description: |
      Extracted from documentation: Passing data back up
  - name: "Migration script"
    id: concept:page_252_svelte
    description: |
      Extracted from documentation: Migration script
  - name: "run"
    id: concept:page_253_svelte
    description: |
      Extracted from documentation: run
  - name: "Things that are not automigrated"
    id: concept:page_254_svelte
    description: |
      Extracted from documentation: Things that are not automigrated
  - name: "Components are no longer classes"
    id: concept:page_255_svelte
    description: |
      Extracted from documentation: Components are no longer classes
  - name: "Server API changes"
    id: concept:page_256_svelte
    description: |
      Extracted from documentation: Server API changes
  - name: "Component typing changes"
    id: concept:page_257_svelte
    description: |
      Extracted from documentation: Component typing changes
  - name: "bind:this changes"
    id: concept:page_258_svelte
    description: |
      Extracted from documentation: bind:this changes
  - name: "`<svelte:component>` is no longer necessary"
    id: concept:page_259_svelte
    description: |
      Extracted from documentation: `<svelte:component>` is no longer necessary
  - name: "Dot notation indicates a component"
    id: concept:page_260_svelte
    description: |
      Extracted from documentation: Dot notation indicates a component
  - name: "Whitespace handling changed"
    id: concept:page_261_svelte
    description: |
      Extracted from documentation: Whitespace handling changed
  - name: "Modern browser required"
    id: concept:page_262_svelte
    description: |
      Extracted from documentation: Modern browser required
  - name: "Changes to compiler options"
    id: concept:page_263_svelte
    description: |
      Extracted from documentation: Changes to compiler options
  - name: "The `children` prop is reserved"
    id: concept:page_264_svelte
    description: |
      Extracted from documentation: The `children` prop is reserved
  - name: "Breaking changes in runes mode"
    id: concept:page_265_svelte
    description: |
      Extracted from documentation: Breaking changes in runes mode
  - name: "Bindings to component exports are not allowed"
    id: concept:page_266_svelte
    description: |
      Extracted from documentation: Bindings to component exports are not allowed
  - name: "Bindings need to be explicitly defined using `$bindable()`"
    id: concept:page_267_svelte
    description: |
      Extracted from documentation: Bindings need to be explicitly defined using `$bindable()`
  - name: "`accessors` option is ignored"
    id: concept:page_268_svelte
    description: |
      Extracted from documentation: `accessors` option is ignored
  - name: "`immutable` option is ignored"
    id: concept:page_269_svelte
    description: |
      Extracted from documentation: `immutable` option is ignored
  - name: "Classes are no longer \"auto-reactive\""
    id: concept:page_270_svelte
    description: |
      Extracted from documentation: Classes are no longer "auto-reactive"
  - name: "Touch events are passive"
    id: concept:page_271_svelte
    description: |
      Extracted from documentation: Touch events are passive
  - name: "Attribute/prop syntax is stricter"
    id: concept:page_272_svelte
    description: |
      Extracted from documentation: Attribute/prop syntax is stricter
  - name: "HTML structure is stricter"
    id: concept:page_273_svelte
    description: |
      Extracted from documentation: HTML structure is stricter
  - name: "Stricter `@const` assignment validation"
    id: concept:page_274_svelte
    description: |
      Extracted from documentation: Stricter `@const` assignment validation
  - name: ":is(...), :has(...), and :where(...) are scoped"
    id: concept:page_275_svelte
    description: |
      Extracted from documentation: :is(...), :has(...), and :where(...) are scoped
  - name: "CSS hash position no longer deterministic"
    id: concept:page_276_svelte
    description: |
      Extracted from documentation: CSS hash position no longer deterministic
  - name: "Scoped CSS uses :where(...)"
    id: concept:page_277_svelte
    description: |
      Extracted from documentation: Scoped CSS uses :where(...)
  - name: "Error/warning codes have been renamed"
    id: concept:page_278_svelte
    description: |
      Extracted from documentation: Error/warning codes have been renamed
  - name: "Reduced number of namespaces"
    id: concept:page_279_svelte
    description: |
      Extracted from documentation: Reduced number of namespaces
  - name: "beforeUpdate/afterUpdate changes"
    id: concept:page_280_svelte
    description: |
      Extracted from documentation: beforeUpdate/afterUpdate changes
  - name: "`contenteditable` behavior change"
    id: concept:page_281_svelte
    description: |
      Extracted from documentation: `contenteditable` behavior change
  - name: "`oneventname` attributes no longer accept string values"
    id: concept:page_282_svelte
    description: |
      Extracted from documentation: `oneventname` attributes no longer accept string values
  - name: "`null` and `undefined` become the empty string"
    id: concept:page_283_svelte
    description: |
      Extracted from documentation: `null` and `undefined` become the empty string
  - name: "`bind:files` values can only be `null`, `undefined` or `FileList`"
    id: concept:page_284_svelte
    description: |
      Extracted from documentation: `bind:files` values can only be `null`, `undefined` or `FileList`
  - name: "Bindings now react to form resets"
    id: concept:page_285_svelte
    description: |
      Extracted from documentation: Bindings now react to form resets
  - name: "`walk` no longer exported"
    id: concept:page_286_svelte
    description: |
      Extracted from documentation: `walk` no longer exported
  - name: "Content inside `svelte:options` is forbidden"
    id: concept:page_287_svelte
    description: |
      Extracted from documentation: Content inside `svelte:options` is forbidden
  - name: "`<slot>` elements in declarative shadow roots are preserved"
    id: concept:page_288_svelte
    description: |
      Extracted from documentation: `<slot>` elements in declarative shadow roots are preserved
  - name: "`<svelte:element>` tag must be an expression"
    id: concept:page_289_svelte
    description: |
      Extracted from documentation: `<svelte:element>` tag must be an expression
  - name: "`mount` plays transitions by default"
    id: concept:page_290_svelte
    description: |
      Extracted from documentation: `mount` plays transitions by default
  - name: "`<img src={...}>` and `{@html ...}` hydration mismatches are not repaired"
    id: concept:page_291_svelte
    description: |
      Extracted from documentation: `<img src={...}>` and `{@html ...}` hydration mismatches are not repaired
  - name: "Hydration works differently"
    id: concept:page_292_svelte
    description: |
      Extracted from documentation: Hydration works differently
  - name: "`onevent` attributes are delegated"
    id: concept:page_293_svelte
    description: |
      Extracted from documentation: `onevent` attributes are delegated
  - name: "`--style-props` uses a different element"
    id: concept:page_294_svelte
    description: |
      Extracted from documentation: `--style-props` uses a different element
  - name: "Frequently asked questions"
    id: concept:page_295_svelte
    description: |
      Extracted from documentation: Frequently asked questions
  - name: "I'm new to Svelte. Where should I start?"
    id: concept:page_296_svelte
    description: |
      Extracted from documentation: I'm new to Svelte. Where should I start?
  - name: "Where can I get support?"
    id: concept:page_297_svelte
    description: |
      Extracted from documentation: Where can I get support?
  - name: "Are there any third-party resources?"
    id: concept:page_298_svelte
    description: |
      Extracted from documentation: Are there any third-party resources?
  - name: "How can I get VS Code to syntax-highlight my .svelte files?"
    id: concept:page_299_svelte
    description: |
      Extracted from documentation: How can I get VS Code to syntax-highlight my .svelte files?
  - name: "Is there a tool to automatically format my .svelte files?"
    id: concept:page_300_svelte
    description: |
      Extracted from documentation: Is there a tool to automatically format my .svelte files?
  - name: "How do I document my components?"
    id: concept:page_301_svelte
    description: |
      Extracted from documentation: How do I document my components?
  - name: "Does Svelte scale?"
    id: concept:page_302_svelte
    description: |
      Extracted from documentation: Does Svelte scale?
  - name: "Is there a UI component library?"
    id: concept:page_303_svelte
    description: |
      Extracted from documentation: Is there a UI component library?
  - name: "How do I test Svelte apps?"
    id: concept:page_304_svelte
    description: |
      Extracted from documentation: How do I test Svelte apps?
  - name: "Is there a router?"
    id: concept:page_305_svelte
    description: |
      Extracted from documentation: Is there a router?
  - name: "How do I write a mobile app with Svelte?"
    id: concept:page_306_svelte
    description: |
      Extracted from documentation: How do I write a mobile app with Svelte?
  - name: "Can I tell Svelte not to remove my unused styles?"
    id: concept:page_307_svelte
    description: |
      Extracted from documentation: Can I tell Svelte not to remove my unused styles?
  - name: "Is Svelte v2 still available?"
    id: concept:page_308_svelte
    description: |
      Extracted from documentation: Is Svelte v2 still available?
  - name: "How do I do hot module reloading?"
    id: concept:page_309_svelte
    description: |
      Extracted from documentation: How do I do hot module reloading?
  - name: "svelte"
    id: concept:page_310_svelte
    description: |
      Extracted from documentation: svelte
  - name: "SvelteComponent"
    id: concept:page_311_svelte
    description: |
      Extracted from documentation: SvelteComponent
  - name: "SvelteComponentTyped"
    id: concept:page_312_svelte
    description: |
      Extracted from documentation: SvelteComponentTyped
  - name: "afterUpdate"
    id: concept:page_313_svelte
    description: |
      Extracted from documentation: afterUpdate
  - name: "beforeUpdate"
    id: concept:page_314_svelte
    description: |
      Extracted from documentation: beforeUpdate
  - name: "createContext"
    id: concept:page_315_svelte
    description: |
      Extracted from documentation: createContext
  - name: "createEventDispatcher"
    id: concept:page_316_svelte
    description: |
      Extracted from documentation: createEventDispatcher
  - name: "createRawSnippet"
    id: concept:page_317_svelte
    description: |
      Extracted from documentation: createRawSnippet
  - name: "flushSync"
    id: concept:page_318_svelte
    description: |
      Extracted from documentation: flushSync
  - name: "fork"
    id: concept:page_319_svelte
    description: |
      Extracted from documentation: fork
  - name: "getAbortSignal"
    id: concept:page_320_svelte
    description: |
      Extracted from documentation: getAbortSignal
  - name: "getAllContexts"
    id: concept:page_321_svelte
    description: |
      Extracted from documentation: getAllContexts
  - name: "getContext"
    id: concept:page_322_svelte
    description: |
      Extracted from documentation: getContext
  - name: "hasContext"
    id: concept:page_323_svelte
    description: |
      Extracted from documentation: hasContext
  - name: "hydratable"
    id: concept:page_324_svelte
    description: |
      Extracted from documentation: hydratable
  - name: "hydrate"
    id: concept:page_325_svelte
    description: |
      Extracted from documentation: hydrate
  - name: "mount"
    id: concept:page_326_svelte
    description: |
      Extracted from documentation: mount
  - name: "onDestroy"
    id: concept:page_327_svelte
    description: |
      Extracted from documentation: onDestroy
  - name: "onMount"
    id: concept:page_328_svelte
    description: |
      Extracted from documentation: onMount
  - name: "setContext"
    id: concept:page_329_svelte
    description: |
      Extracted from documentation: setContext
  - name: "settled"
    id: concept:page_330_svelte
    description: |
      Extracted from documentation: settled
  - name: "tick"
    id: concept:page_331_svelte
    description: |
      Extracted from documentation: tick
  - name: "unmount"
    id: concept:page_332_svelte
    description: |
      Extracted from documentation: unmount
  - name: "untrack"
    id: concept:page_333_svelte
    description: |
      Extracted from documentation: untrack
  - name: "Component"
    id: concept:page_334_svelte
    description: |
      Extracted from documentation: Component
  - name: "Example:"
    id: concept:page_335_svelte
    description: |
      Extracted from documentation: Example:
  - name: "ComponentConstructorOptions"
    id: concept:page_336_svelte
    description: |
      Extracted from documentation: ComponentConstructorOptions
  - name: "ComponentEvents"
    id: concept:page_337_svelte
    description: |
      Extracted from documentation: ComponentEvents
  - name: "ComponentInternals"
    id: concept:page_338_svelte
    description: |
      Extracted from documentation: ComponentInternals
  - name: "ComponentProps"
    id: concept:page_339_svelte
    description: |
      Extracted from documentation: ComponentProps
  - name: "ComponentType"
    id: concept:page_340_svelte
    description: |
      Extracted from documentation: ComponentType
  - name: "EventDispatcher"
    id: concept:page_341_svelte
    description: |
      Extracted from documentation: EventDispatcher
  - name: "Fork"
    id: concept:page_342_svelte
    description: |
      Extracted from documentation: Fork
  - name: "MountOptions"
    id: concept:page_343_svelte
    description: |
      Extracted from documentation: MountOptions
  - name: "Snippet"
    id: concept:page_344_svelte
    description: |
      Extracted from documentation: Snippet
  - name: "svelte/action"
    id: concept:page_345_svelte
    description: |
      Extracted from documentation: svelte/action
  - name: "Action"
    id: concept:page_346_svelte
    description: |
      Extracted from documentation: Action
  - name: "ActionReturn"
    id: concept:page_347_svelte
    description: |
      Extracted from documentation: ActionReturn
  - name: "svelte/animate"
    id: concept:page_348_svelte
    description: |
      Extracted from documentation: svelte/animate
  - name: "flip"
    id: concept:page_349_svelte
    description: |
      Extracted from documentation: flip
  - name: "AnimationConfig"
    id: concept:page_350_svelte
    description: |
      Extracted from documentation: AnimationConfig
  - name: "FlipParams"
    id: concept:page_351_svelte
    description: |
      Extracted from documentation: FlipParams
  - name: "svelte/attachments"
    id: concept:page_352_svelte
    description: |
      Extracted from documentation: svelte/attachments
  - name: "createAttachmentKey"
    id: concept:page_353_svelte
    description: |
      Extracted from documentation: createAttachmentKey
  - name: "fromAction"
    id: concept:page_354_svelte
    description: |
      Extracted from documentation: fromAction
  - name: "Attachment"
    id: concept:page_355_svelte
    description: |
      Extracted from documentation: Attachment
  - name: "svelte/compiler"
    id: concept:page_356_svelte
    description: |
      Extracted from documentation: svelte/compiler
  - name: "VERSION"
    id: concept:page_357_svelte
    description: |
      Extracted from documentation: VERSION
  - name: "compile"
    id: concept:page_358_svelte
    description: |
      Extracted from documentation: compile
  - name: "compileModule"
    id: concept:page_359_svelte
    description: |
      Extracted from documentation: compileModule
  - name: "migrate"
    id: concept:page_360_svelte
    description: |
      Extracted from documentation: migrate
  - name: "parse"
    id: concept:page_361_svelte
    description: |
      Extracted from documentation: parse
  - name: "parseCss"
    id: concept:page_362_svelte
    description: |
      Extracted from documentation: parseCss
  - name: "preprocess"
    id: concept:page_363_svelte
    description: |
      Extracted from documentation: preprocess
  - name: "print"
    id: concept:page_364_svelte
    description: |
      Extracted from documentation: print
  - name: "walk"
    id: concept:page_365_svelte
    description: |
      Extracted from documentation: walk
  - name: "AST"
    id: concept:page_366_svelte
    description: |
      Extracted from documentation: AST
  - name: "CompileError"
    id: concept:page_367_svelte
    description: |
      Extracted from documentation: CompileError
  - name: "CompileOptions"
    id: concept:page_368_svelte
    description: |
      Extracted from documentation: CompileOptions
  - name: "CompileResult"
    id: concept:page_369_svelte
    description: |
      Extracted from documentation: CompileResult
  - name: "MarkupPreprocessor"
    id: concept:page_370_svelte
    description: |
      Extracted from documentation: MarkupPreprocessor
  - name: "ModuleCompileOptions"
    id: concept:page_371_svelte
    description: |
      Extracted from documentation: ModuleCompileOptions
  - name: "Preprocessor"
    id: concept:page_372_svelte
    description: |
      Extracted from documentation: Preprocessor
  - name: "PreprocessorGroup"
    id: concept:page_373_svelte
    description: |
      Extracted from documentation: PreprocessorGroup
  - name: "Processed"
    id: concept:page_374_svelte
    description: |
      Extracted from documentation: Processed
  - name: "Warning"
    id: concept:page_375_svelte
    description: |
      Extracted from documentation: Warning
  - name: "svelte/easing"
    id: concept:page_376_svelte
    description: |
      Extracted from documentation: svelte/easing
  - name: "backIn"
    id: concept:page_377_svelte
    description: |
      Extracted from documentation: backIn
  - name: "backInOut"
    id: concept:page_378_svelte
    description: |
      Extracted from documentation: backInOut
  - name: "backOut"
    id: concept:page_379_svelte
    description: |
      Extracted from documentation: backOut
  - name: "bounceIn"
    id: concept:page_380_svelte
    description: |
      Extracted from documentation: bounceIn
  - name: "bounceInOut"
    id: concept:page_381_svelte
    description: |
      Extracted from documentation: bounceInOut
  - name: "bounceOut"
    id: concept:page_382_svelte
    description: |
      Extracted from documentation: bounceOut
  - name: "circIn"
    id: concept:page_383_svelte
    description: |
      Extracted from documentation: circIn
  - name: "circInOut"
    id: concept:page_384_svelte
    description: |
      Extracted from documentation: circInOut
  - name: "circOut"
    id: concept:page_385_svelte
    description: |
      Extracted from documentation: circOut
  - name: "cubicIn"
    id: concept:page_386_svelte
    description: |
      Extracted from documentation: cubicIn
  - name: "cubicInOut"
    id: concept:page_387_svelte
    description: |
      Extracted from documentation: cubicInOut
  - name: "cubicOut"
    id: concept:page_388_svelte
    description: |
      Extracted from documentation: cubicOut
  - name: "elasticIn"
    id: concept:page_389_svelte
    description: |
      Extracted from documentation: elasticIn
  - name: "elasticInOut"
    id: concept:page_390_svelte
    description: |
      Extracted from documentation: elasticInOut
  - name: "elasticOut"
    id: concept:page_391_svelte
    description: |
      Extracted from documentation: elasticOut
  - name: "expoIn"
    id: concept:page_392_svelte
    description: |
      Extracted from documentation: expoIn
  - name: "expoInOut"
    id: concept:page_393_svelte
    description: |
      Extracted from documentation: expoInOut
  - name: "expoOut"
    id: concept:page_394_svelte
    description: |
      Extracted from documentation: expoOut
  - name: "linear"
    id: concept:page_395_svelte
    description: |
      Extracted from documentation: linear
  - name: "quadIn"
    id: concept:page_396_svelte
    description: |
      Extracted from documentation: quadIn
  - name: "quadInOut"
    id: concept:page_397_svelte
    description: |
      Extracted from documentation: quadInOut
  - name: "quadOut"
    id: concept:page_398_svelte
    description: |
      Extracted from documentation: quadOut
  - name: "quartIn"
    id: concept:page_399_svelte
    description: |
      Extracted from documentation: quartIn
  - name: "quartInOut"
    id: concept:page_400_svelte
    description: |
      Extracted from documentation: quartInOut
  - name: "quartOut"
    id: concept:page_401_svelte
    description: |
      Extracted from documentation: quartOut
  - name: "quintIn"
    id: concept:page_402_svelte
    description: |
      Extracted from documentation: quintIn
  - name: "quintInOut"
    id: concept:page_403_svelte
    description: |
      Extracted from documentation: quintInOut
  - name: "quintOut"
    id: concept:page_404_svelte
    description: |
      Extracted from documentation: quintOut
  - name: "sineIn"
    id: concept:page_405_svelte
    description: |
      Extracted from documentation: sineIn
  - name: "sineInOut"
    id: concept:page_406_svelte
    description: |
      Extracted from documentation: sineInOut
  - name: "sineOut"
    id: concept:page_407_svelte
    description: |
      Extracted from documentation: sineOut
  - name: "svelte/events"
    id: concept:page_408_svelte
    description: |
      Extracted from documentation: svelte/events
  - name: "on"
    id: concept:page_409_svelte
    description: |
      Extracted from documentation: on
  - name: "svelte/legacy"
    id: concept:page_410_svelte
    description: |
      Extracted from documentation: svelte/legacy
  - name: "asClassComponent"
    id: concept:page_411_svelte
    description: |
      Extracted from documentation: asClassComponent
  - name: "createBubbler"
    id: concept:page_412_svelte
    description: |
      Extracted from documentation: createBubbler
  - name: "createClassComponent"
    id: concept:page_413_svelte
    description: |
      Extracted from documentation: createClassComponent
  - name: "handlers"
    id: concept:page_414_svelte
    description: |
      Extracted from documentation: handlers
  - name: "nonpassive"
    id: concept:page_415_svelte
    description: |
      Extracted from documentation: nonpassive
  - name: "once"
    id: concept:page_416_svelte
    description: |
      Extracted from documentation: once
  - name: "passive"
    id: concept:page_417_svelte
    description: |
      Extracted from documentation: passive
  - name: "preventDefault"
    id: concept:page_418_svelte
    description: |
      Extracted from documentation: preventDefault
  - name: "self"
    id: concept:page_419_svelte
    description: |
      Extracted from documentation: self
  - name: "stopImmediatePropagation"
    id: concept:page_420_svelte
    description: |
      Extracted from documentation: stopImmediatePropagation
  - name: "stopPropagation"
    id: concept:page_421_svelte
    description: |
      Extracted from documentation: stopPropagation
  - name: "trusted"
    id: concept:page_422_svelte
    description: |
      Extracted from documentation: trusted
  - name: "LegacyComponentType"
    id: concept:page_423_svelte
    description: |
      Extracted from documentation: LegacyComponentType
  - name: "svelte/motion"
    id: concept:page_424_svelte
    description: |
      Extracted from documentation: svelte/motion
  - name: "Spring"
    id: concept:page_425_svelte
    description: |
      Extracted from documentation: Spring
  - name: "Tween"
    id: concept:page_426_svelte
    description: |
      Extracted from documentation: Tween
  - name: "prefersReducedMotion"
    id: concept:page_427_svelte
    description: |
      Extracted from documentation: prefersReducedMotion
  - name: "spring"
    id: concept:page_428_svelte
    description: |
      Extracted from documentation: spring
  - name: "tweened"
    id: concept:page_429_svelte
    description: |
      Extracted from documentation: tweened
  - name: "SpringOptions"
    id: concept:page_430_svelte
    description: |
      Extracted from documentation: SpringOptions
  - name: "SpringUpdateOptions"
    id: concept:page_431_svelte
    description: |
      Extracted from documentation: SpringUpdateOptions
  - name: "TweenOptions"
    id: concept:page_432_svelte
    description: |
      Extracted from documentation: TweenOptions
  - name: "Tweened"
    id: concept:page_433_svelte
    description: |
      Extracted from documentation: Tweened
  - name: "Updater"
    id: concept:page_434_svelte
    description: |
      Extracted from documentation: Updater
  - name: "svelte/reactivity/window"
    id: concept:page_435_svelte
    description: |
      Extracted from documentation: svelte/reactivity/window
  - name: "devicePixelRatio"
    id: concept:page_436_svelte
    description: |
      Extracted from documentation: devicePixelRatio
  - name: "innerHeight"
    id: concept:page_437_svelte
    description: |
      Extracted from documentation: innerHeight
  - name: "innerWidth"
    id: concept:page_438_svelte
    description: |
      Extracted from documentation: innerWidth
  - name: "online"
    id: concept:page_439_svelte
    description: |
      Extracted from documentation: online
  - name: "outerHeight"
    id: concept:page_440_svelte
    description: |
      Extracted from documentation: outerHeight
  - name: "outerWidth"
    id: concept:page_441_svelte
    description: |
      Extracted from documentation: outerWidth
  - name: "screenLeft"
    id: concept:page_442_svelte
    description: |
      Extracted from documentation: screenLeft
  - name: "screenTop"
    id: concept:page_443_svelte
    description: |
      Extracted from documentation: screenTop
  - name: "scrollX"
    id: concept:page_444_svelte
    description: |
      Extracted from documentation: scrollX
  - name: "scrollY"
    id: concept:page_445_svelte
    description: |
      Extracted from documentation: scrollY
  - name: "svelte/reactivity"
    id: concept:page_446_svelte
    description: |
      Extracted from documentation: svelte/reactivity
  - name: "MediaQuery"
    id: concept:page_447_svelte
    description: |
      Extracted from documentation: MediaQuery
  - name: "SvelteDate"
    id: concept:page_448_svelte
    description: |
      Extracted from documentation: SvelteDate
  - name: "SvelteMap"
    id: concept:page_449_svelte
    description: |
      Extracted from documentation: SvelteMap
  - name: "SvelteSet"
    id: concept:page_450_svelte
    description: |
      Extracted from documentation: SvelteSet
  - name: "SvelteURL"
    id: concept:page_451_svelte
    description: |
      Extracted from documentation: SvelteURL
  - name: "SvelteURLSearchParams"
    id: concept:page_452_svelte
    description: |
      Extracted from documentation: SvelteURLSearchParams
  - name: "createSubscriber"
    id: concept:page_453_svelte
    description: |
      Extracted from documentation: createSubscriber
  - name: "query;"
    id: concept:page_454_svelte
    description: |
      Extracted from documentation: query;
  - name: "subscribe;"
    id: concept:page_455_svelte
    description: |
      Extracted from documentation: subscribe;
  - name: "svelte/server"
    id: concept:page_456_svelte
    description: |
      Extracted from documentation: svelte/server
  - name: "render"
    id: concept:page_457_svelte
    description: |
      Extracted from documentation: render
  - name: "derived"
    id: concept:page_458_svelte
    description: |
      Extracted from documentation: derived
  - name: "fromStore"
    id: concept:page_459_svelte
    description: |
      Extracted from documentation: fromStore
  - name: "get"
    id: concept:page_460_svelte
    description: |
      Extracted from documentation: get
  - name: "readable"
    id: concept:page_461_svelte
    description: |
      Extracted from documentation: readable
  - name: "readonly"
    id: concept:page_462_svelte
    description: |
      Extracted from documentation: readonly
  - name: "toStore"
    id: concept:page_463_svelte
    description: |
      Extracted from documentation: toStore
  - name: "writable"
    id: concept:page_464_svelte
    description: |
      Extracted from documentation: writable
  - name: "Readable"
    id: concept:page_465_svelte
    description: |
      Extracted from documentation: Readable
  - name: "StartStopNotifier"
    id: concept:page_466_svelte
    description: |
      Extracted from documentation: StartStopNotifier
  - name: "Subscriber"
    id: concept:page_467_svelte
    description: |
      Extracted from documentation: Subscriber
  - name: "Unsubscriber"
    id: concept:page_468_svelte
    description: |
      Extracted from documentation: Unsubscriber
  - name: "Writable"
    id: concept:page_469_svelte
    description: |
      Extracted from documentation: Writable
  - name: "svelte/transition"
    id: concept:page_470_svelte
    description: |
      Extracted from documentation: svelte/transition
  - name: "blur"
    id: concept:page_471_svelte
    description: |
      Extracted from documentation: blur
  - name: "crossfade"
    id: concept:page_472_svelte
    description: |
      Extracted from documentation: crossfade
  - name: "draw"
    id: concept:page_473_svelte
    description: |
      Extracted from documentation: draw
  - name: "fade"
    id: concept:page_474_svelte
    description: |
      Extracted from documentation: fade
  - name: "fly"
    id: concept:page_475_svelte
    description: |
      Extracted from documentation: fly
  - name: "scale"
    id: concept:page_476_svelte
    description: |
      Extracted from documentation: scale
  - name: "slide"
    id: concept:page_477_svelte
    description: |
      Extracted from documentation: slide
  - name: "BlurParams"
    id: concept:page_478_svelte
    description: |
      Extracted from documentation: BlurParams
  - name: "CrossfadeParams"
    id: concept:page_479_svelte
    description: |
      Extracted from documentation: CrossfadeParams
  - name: "DrawParams"
    id: concept:page_480_svelte
    description: |
      Extracted from documentation: DrawParams
  - name: "EasingFunction"
    id: concept:page_481_svelte
    description: |
      Extracted from documentation: EasingFunction
  - name: "FadeParams"
    id: concept:page_482_svelte
    description: |
      Extracted from documentation: FadeParams
  - name: "FlyParams"
    id: concept:page_483_svelte
    description: |
      Extracted from documentation: FlyParams
  - name: "ScaleParams"
    id: concept:page_484_svelte
    description: |
      Extracted from documentation: ScaleParams
  - name: "SlideParams"
    id: concept:page_485_svelte
    description: |
      Extracted from documentation: SlideParams
  - name: "TransitionConfig"
    id: concept:page_486_svelte
    description: |
      Extracted from documentation: TransitionConfig
  - name: "Compiler errors"
    id: concept:page_487_svelte
    description: |
      Extracted from documentation: Compiler errors
  - name: "animation_duplicate"
    id: concept:page_488_svelte
    description: |
      Extracted from documentation: animation_duplicate
  - name: "animation_invalid_placement"
    id: concept:page_489_svelte
    description: |
      Extracted from documentation: animation_invalid_placement
  - name: "animation_missing_key"
    id: concept:page_490_svelte
    description: |
      Extracted from documentation: animation_missing_key
  - name: "attribute_contenteditable_dynamic"
    id: concept:page_491_svelte
    description: |
      Extracted from documentation: attribute_contenteditable_dynamic
  - name: "attribute_contenteditable_missing"
    id: concept:page_492_svelte
    description: |
      Extracted from documentation: attribute_contenteditable_missing
  - name: "attribute_duplicate"
    id: concept:page_493_svelte
    description: |
      Extracted from documentation: attribute_duplicate
  - name: "attribute_empty_shorthand"
    id: concept:page_494_svelte
    description: |
      Extracted from documentation: attribute_empty_shorthand
  - name: "attribute_invalid_event_handler"
    id: concept:page_495_svelte
    description: |
      Extracted from documentation: attribute_invalid_event_handler
  - name: "attribute_invalid_multiple"
    id: concept:page_496_svelte
    description: |
      Extracted from documentation: attribute_invalid_multiple
  - name: "attribute_invalid_name"
    id: concept:page_497_svelte
    description: |
      Extracted from documentation: attribute_invalid_name
  - name: "attribute_invalid_sequence_expression"
    id: concept:page_498_svelte
    description: |
      Extracted from documentation: attribute_invalid_sequence_expression
  - name: "attribute_invalid_type"
    id: concept:page_499_svelte
    description: |
      Extracted from documentation: attribute_invalid_type
  - name: "attribute_unquoted_sequence"
    id: concept:page_500_svelte
    description: |
      Extracted from documentation: attribute_unquoted_sequence
  - name: "bind_group_invalid_expression"
    id: concept:page_501_svelte
    description: |
      Extracted from documentation: bind_group_invalid_expression
  - name: "bind_group_invalid_snippet_parameter"
    id: concept:page_502_svelte
    description: |
      Extracted from documentation: bind_group_invalid_snippet_parameter
  - name: "bind_invalid_expression"
    id: concept:page_503_svelte
    description: |
      Extracted from documentation: bind_invalid_expression
  - name: "bind_invalid_name"
    id: concept:page_504_svelte
    description: |
      Extracted from documentation: bind_invalid_name
  - name: "bind_invalid_parens"
    id: concept:page_505_svelte
    description: |
      Extracted from documentation: bind_invalid_parens
  - name: "bind_invalid_target"
    id: concept:page_506_svelte
    description: |
      Extracted from documentation: bind_invalid_target
  - name: "bind_invalid_value"
    id: concept:page_507_svelte
    description: |
      Extracted from documentation: bind_invalid_value
  - name: "bindable_invalid_location"
    id: concept:page_508_svelte
    description: |
      Extracted from documentation: bindable_invalid_location
  - name: "block_duplicate_clause"
    id: concept:page_509_svelte
    description: |
      Extracted from documentation: block_duplicate_clause
  - name: "block_invalid_continuation_placement"
    id: concept:page_510_svelte
    description: |
      Extracted from documentation: block_invalid_continuation_placement
  - name: "block_invalid_elseif"
    id: concept:page_511_svelte
    description: |
      Extracted from documentation: block_invalid_elseif
  - name: "block_invalid_placement"
    id: concept:page_512_svelte
    description: |
      Extracted from documentation: block_invalid_placement
  - name: "block_unclosed"
    id: concept:page_513_svelte
    description: |
      Extracted from documentation: block_unclosed
  - name: "block_unexpected_character"
    id: concept:page_514_svelte
    description: |
      Extracted from documentation: block_unexpected_character
  - name: "block_unexpected_close"
    id: concept:page_515_svelte
    description: |
      Extracted from documentation: block_unexpected_close
  - name: "component_invalid_directive"
    id: concept:page_516_svelte
    description: |
      Extracted from documentation: component_invalid_directive
  - name: "const_tag_cycle"
    id: concept:page_517_svelte
    description: |
      Extracted from documentation: const_tag_cycle
  - name: "const_tag_invalid_expression"
    id: concept:page_518_svelte
    description: |
      Extracted from documentation: const_tag_invalid_expression
  - name: "const_tag_invalid_placement"
    id: concept:page_519_svelte
    description: |
      Extracted from documentation: const_tag_invalid_placement
  - name: "const_tag_invalid_reference"
    id: concept:page_520_svelte
    description: |
      Extracted from documentation: const_tag_invalid_reference
  - name: "constant_assignment"
    id: concept:page_521_svelte
    description: |
      Extracted from documentation: constant_assignment
  - name: "constant_binding"
    id: concept:page_522_svelte
    description: |
      Extracted from documentation: constant_binding
  - name: "css_empty_declaration"
    id: concept:page_523_svelte
    description: |
      Extracted from documentation: css_empty_declaration
  - name: "css_expected_identifier"
    id: concept:page_524_svelte
    description: |
      Extracted from documentation: css_expected_identifier
  - name: "css_global_block_invalid_combinator"
    id: concept:page_525_svelte
    description: |
      Extracted from documentation: css_global_block_invalid_combinator
  - name: "css_global_block_invalid_declaration"
    id: concept:page_526_svelte
    description: |
      Extracted from documentation: css_global_block_invalid_declaration
  - name: "css_global_block_invalid_list"
    id: concept:page_527_svelte
    description: |
      Extracted from documentation: css_global_block_invalid_list
  - name: "css_global_block_invalid_modifier"
    id: concept:page_528_svelte
    description: |
      Extracted from documentation: css_global_block_invalid_modifier
  - name: "css_global_block_invalid_modifier_start"
    id: concept:page_529_svelte
    description: |
      Extracted from documentation: css_global_block_invalid_modifier_start
  - name: "css_global_block_invalid_placement"
    id: concept:page_530_svelte
    description: |
      Extracted from documentation: css_global_block_invalid_placement
  - name: "css_global_invalid_placement"
    id: concept:page_531_svelte
    description: |
      Extracted from documentation: css_global_invalid_placement
  - name: "css_global_invalid_selector"
    id: concept:page_532_svelte
    description: |
      Extracted from documentation: css_global_invalid_selector
  - name: "css_global_invalid_selector_list"
    id: concept:page_533_svelte
    description: |
      Extracted from documentation: css_global_invalid_selector_list
  - name: "css_nesting_selector_invalid_placement"
    id: concept:page_534_svelte
    description: |
      Extracted from documentation: css_nesting_selector_invalid_placement
  - name: "css_selector_invalid"
    id: concept:page_535_svelte
    description: |
      Extracted from documentation: css_selector_invalid
  - name: "css_type_selector_invalid_placement"
    id: concept:page_536_svelte
    description: |
      Extracted from documentation: css_type_selector_invalid_placement
  - name: "debug_tag_invalid_arguments"
    id: concept:page_537_svelte
    description: |
      Extracted from documentation: debug_tag_invalid_arguments
  - name: "declaration_duplicate"
    id: concept:page_538_svelte
    description: |
      Extracted from documentation: declaration_duplicate
  - name: "declaration_duplicate_module_import"
    id: concept:page_539_svelte
    description: |
      Extracted from documentation: declaration_duplicate_module_import
  - name: "declaration_tag_invalid_type"
    id: concept:page_540_svelte
    description: |
      Extracted from documentation: declaration_tag_invalid_type
  - name: "declaration_tag_no_legacy_mode"
    id: concept:page_541_svelte
    description: |
      Extracted from documentation: declaration_tag_no_legacy_mode
  - name: "derived_invalid_export"
    id: concept:page_542_svelte
    description: |
      Extracted from documentation: derived_invalid_export
  - name: "directive_invalid_value"
    id: concept:page_543_svelte
    description: |
      Extracted from documentation: directive_invalid_value
  - name: "directive_missing_name"
    id: concept:page_544_svelte
    description: |
      Extracted from documentation: directive_missing_name
  - name: "dollar_binding_invalid"
    id: concept:page_545_svelte
    description: |
      Extracted from documentation: dollar_binding_invalid
  - name: "dollar_prefix_invalid"
    id: concept:page_546_svelte
    description: |
      Extracted from documentation: dollar_prefix_invalid
  - name: "duplicate_class_field"
    id: concept:page_547_svelte
    description: |
      Extracted from documentation: duplicate_class_field
  - name: "each_item_invalid_assignment"
    id: concept:page_548_svelte
    description: |
      Extracted from documentation: each_item_invalid_assignment
  - name: "each_key_without_as"
    id: concept:page_549_svelte
    description: |
      Extracted from documentation: each_key_without_as
  - name: "effect_invalid_placement"
    id: concept:page_550_svelte
    description: |
      Extracted from documentation: effect_invalid_placement
  - name: "element_invalid_closing_tag"
    id: concept:page_551_svelte
    description: |
      Extracted from documentation: element_invalid_closing_tag
  - name: "element_invalid_closing_tag_autoclosed"
    id: concept:page_552_svelte
    description: |
      Extracted from documentation: element_invalid_closing_tag_autoclosed
  - name: "element_unclosed"
    id: concept:page_553_svelte
    description: |
      Extracted from documentation: element_unclosed
  - name: "event_handler_invalid_component_modifier"
    id: concept:page_554_svelte
    description: |
      Extracted from documentation: event_handler_invalid_component_modifier
  - name: "event_handler_invalid_modifier"
    id: concept:page_555_svelte
    description: |
      Extracted from documentation: event_handler_invalid_modifier
  - name: "event_handler_invalid_modifier_combination"
    id: concept:page_556_svelte
    description: |
      Extracted from documentation: event_handler_invalid_modifier_combination
  - name: "expected_attribute_value"
    id: concept:page_557_svelte
    description: |
      Extracted from documentation: expected_attribute_value
  - name: "expected_block_type"
    id: concept:page_558_svelte
    description: |
      Extracted from documentation: expected_block_type
  - name: "expected_identifier"
    id: concept:page_559_svelte
    description: |
      Extracted from documentation: expected_identifier
  - name: "expected_pattern"
    id: concept:page_560_svelte
    description: |
      Extracted from documentation: expected_pattern
  - name: "expected_tag"
    id: concept:page_561_svelte
    description: |
      Extracted from documentation: expected_tag
  - name: "expected_token"
    id: concept:page_562_svelte
    description: |
      Extracted from documentation: expected_token
  - name: "expected_whitespace"
    id: concept:page_563_svelte
    description: |
      Extracted from documentation: expected_whitespace
  - name: "experimental_async"
    id: concept:page_564_svelte
    description: |
      Extracted from documentation: experimental_async
  - name: "export_undefined"
    id: concept:page_565_svelte
    description: |
      Extracted from documentation: export_undefined
  - name: "global_reference_invalid"
    id: concept:page_566_svelte
    description: |
      Extracted from documentation: global_reference_invalid
  - name: "host_invalid_placement"
    id: concept:page_567_svelte
    description: |
      Extracted from documentation: host_invalid_placement
  - name: "illegal_await_expression"
    id: concept:page_568_svelte
    description: |
      Extracted from documentation: illegal_await_expression
  - name: "illegal_element_attribute"
    id: concept:page_569_svelte
    description: |
      Extracted from documentation: illegal_element_attribute
  - name: "import_svelte_internal_forbidden"
    id: concept:page_570_svelte
    description: |
      Extracted from documentation: import_svelte_internal_forbidden
  - name: "inspect_trace_generator"
    id: concept:page_571_svelte
    description: |
      Extracted from documentation: inspect_trace_generator
  - name: "inspect_trace_invalid_placement"
    id: concept:page_572_svelte
    description: |
      Extracted from documentation: inspect_trace_invalid_placement
  - name: "invalid_arguments_usage"
    id: concept:page_573_svelte
    description: |
      Extracted from documentation: invalid_arguments_usage
  - name: "js_parse_error"
    id: concept:page_574_svelte
    description: |
      Extracted from documentation: js_parse_error
  - name: "legacy_await_invalid"
    id: concept:page_575_svelte
    description: |
      Extracted from documentation: legacy_await_invalid
  - name: "legacy_export_invalid"
    id: concept:page_576_svelte
    description: |
      Extracted from documentation: legacy_export_invalid
  - name: "legacy_props_invalid"
    id: concept:page_577_svelte
    description: |
      Extracted from documentation: legacy_props_invalid
  - name: "legacy_reactive_statement_invalid"
    id: concept:page_578_svelte
    description: |
      Extracted from documentation: legacy_reactive_statement_invalid
  - name: "legacy_rest_props_invalid"
    id: concept:page_579_svelte
    description: |
      Extracted from documentation: legacy_rest_props_invalid
  - name: "let_directive_invalid_placement"
    id: concept:page_580_svelte
    description: |
      Extracted from documentation: let_directive_invalid_placement
  - name: "mixed_event_handler_syntaxes"
    id: concept:page_581_svelte
    description: |
      Extracted from documentation: mixed_event_handler_syntaxes
  - name: "module_illegal_default_export"
    id: concept:page_582_svelte
    description: |
      Extracted from documentation: module_illegal_default_export
  - name: "node_invalid_placement"
    id: concept:page_583_svelte
    description: |
      Extracted from documentation: node_invalid_placement
  - name: "options_invalid_value"
    id: concept:page_584_svelte
    description: |
      Extracted from documentation: options_invalid_value
  - name: "options_removed"
    id: concept:page_585_svelte
    description: |
      Extracted from documentation: options_removed
  - name: "options_unrecognised"
    id: concept:page_586_svelte
    description: |
      Extracted from documentation: options_unrecognised
  - name: "props_duplicate"
    id: concept:page_587_svelte
    description: |
      Extracted from documentation: props_duplicate
  - name: "props_id_invalid_placement"
    id: concept:page_588_svelte
    description: |
      Extracted from documentation: props_id_invalid_placement
  - name: "props_illegal_name"
    id: concept:page_589_svelte
    description: |
      Extracted from documentation: props_illegal_name
  - name: "props_invalid_identifier"
    id: concept:page_590_svelte
    description: |
      Extracted from documentation: props_invalid_identifier
  - name: "props_invalid_pattern"
    id: concept:page_591_svelte
    description: |
      Extracted from documentation: props_invalid_pattern
  - name: "props_invalid_placement"
    id: concept:page_592_svelte
    description: |
      Extracted from documentation: props_invalid_placement
  - name: "reactive_declaration_cycle"
    id: concept:page_593_svelte
    description: |
      Extracted from documentation: reactive_declaration_cycle
  - name: "render_tag_invalid_call_expression"
    id: concept:page_594_svelte
    description: |
      Extracted from documentation: render_tag_invalid_call_expression
  - name: "render_tag_invalid_expression"
    id: concept:page_595_svelte
    description: |
      Extracted from documentation: render_tag_invalid_expression
  - name: "render_tag_invalid_spread_argument"
    id: concept:page_596_svelte
    description: |
      Extracted from documentation: render_tag_invalid_spread_argument
  - name: "rune_invalid_arguments"
    id: concept:page_597_svelte
    description: |
      Extracted from documentation: rune_invalid_arguments
  - name: "rune_invalid_arguments_length"
    id: concept:page_598_svelte
    description: |
      Extracted from documentation: rune_invalid_arguments_length
  - name: "rune_invalid_computed_property"
    id: concept:page_599_svelte
    description: |
      Extracted from documentation: rune_invalid_computed_property
  - name: "rune_invalid_name"
    id: concept:page_600_svelte
    description: |
      Extracted from documentation: rune_invalid_name
  - name: "rune_invalid_spread"
    id: concept:page_601_svelte
    description: |
      Extracted from documentation: rune_invalid_spread
  - name: "rune_invalid_usage"
    id: concept:page_602_svelte
    description: |
      Extracted from documentation: rune_invalid_usage
  - name: "rune_missing_parentheses"
    id: concept:page_603_svelte
    description: |
      Extracted from documentation: rune_missing_parentheses
  - name: "rune_removed"
    id: concept:page_604_svelte
    description: |
      Extracted from documentation: rune_removed
  - name: "rune_renamed"
    id: concept:page_605_svelte
    description: |
      Extracted from documentation: rune_renamed
  - name: "runes_mode_invalid_import"
    id: concept:page_606_svelte
    description: |
      Extracted from documentation: runes_mode_invalid_import
  - name: "script_duplicate"
    id: concept:page_607_svelte
    description: |
      Extracted from documentation: script_duplicate
  - name: "script_invalid_attribute_value"
    id: concept:page_608_svelte
    description: |
      Extracted from documentation: script_invalid_attribute_value
  - name: "script_invalid_context"
    id: concept:page_609_svelte
    description: |
      Extracted from documentation: script_invalid_context
  - name: "script_reserved_attribute"
    id: concept:page_610_svelte
    description: |
      Extracted from documentation: script_reserved_attribute
  - name: "slot_attribute_duplicate"
    id: concept:page_611_svelte
    description: |
      Extracted from documentation: slot_attribute_duplicate
  - name: "slot_attribute_invalid"
    id: concept:page_612_svelte
    description: |
      Extracted from documentation: slot_attribute_invalid
  - name: "slot_attribute_invalid_placement"
    id: concept:page_613_svelte
    description: |
      Extracted from documentation: slot_attribute_invalid_placement
  - name: "slot_default_duplicate"
    id: concept:page_614_svelte
    description: |
      Extracted from documentation: slot_default_duplicate
  - name: "slot_element_invalid_attribute"
    id: concept:page_615_svelte
    description: |
      Extracted from documentation: slot_element_invalid_attribute
  - name: "slot_element_invalid_name"
    id: concept:page_616_svelte
    description: |
      Extracted from documentation: slot_element_invalid_name
  - name: "slot_element_invalid_name_default"
    id: concept:page_617_svelte
    description: |
      Extracted from documentation: slot_element_invalid_name_default
  - name: "slot_snippet_conflict"
    id: concept:page_618_svelte
    description: |
      Extracted from documentation: slot_snippet_conflict
  - name: "snippet_conflict"
    id: concept:page_619_svelte
    description: |
      Extracted from documentation: snippet_conflict
  - name: "snippet_invalid_export"
    id: concept:page_620_svelte
    description: |
      Extracted from documentation: snippet_invalid_export
  - name: "snippet_invalid_rest_parameter"
    id: concept:page_621_svelte
    description: |
      Extracted from documentation: snippet_invalid_rest_parameter
  - name: "snippet_parameter_assignment"
    id: concept:page_622_svelte
    description: |
      Extracted from documentation: snippet_parameter_assignment
  - name: "snippet_shadowing_prop"
    id: concept:page_623_svelte
    description: |
      Extracted from documentation: snippet_shadowing_prop
  - name: "state_field_duplicate"
    id: concept:page_624_svelte
    description: |
      Extracted from documentation: state_field_duplicate
  - name: "state_field_invalid_assignment"
    id: concept:page_625_svelte
    description: |
      Extracted from documentation: state_field_invalid_assignment
  - name: "state_invalid_export"
    id: concept:page_626_svelte
    description: |
      Extracted from documentation: state_invalid_export
  - name: "state_invalid_placement"
    id: concept:page_627_svelte
    description: |
      Extracted from documentation: state_invalid_placement
  - name: "store_invalid_scoped_subscription"
    id: concept:page_628_svelte
    description: |
      Extracted from documentation: store_invalid_scoped_subscription
  - name: "store_invalid_subscription"
    id: concept:page_629_svelte
    description: |
      Extracted from documentation: store_invalid_subscription
  - name: "store_invalid_subscription_module"
    id: concept:page_630_svelte
    description: |
      Extracted from documentation: store_invalid_subscription_module
  - name: "style_directive_invalid_modifier"
    id: concept:page_631_svelte
    description: |
      Extracted from documentation: style_directive_invalid_modifier
  - name: "style_duplicate"
    id: concept:page_632_svelte
    description: |
      Extracted from documentation: style_duplicate
  - name: "svelte_body_illegal_attribute"
    id: concept:page_633_svelte
    description: |
      Extracted from documentation: svelte_body_illegal_attribute
  - name: "svelte_boundary_invalid_attribute"
    id: concept:page_634_svelte
    description: |
      Extracted from documentation: svelte_boundary_invalid_attribute
  - name: "svelte_boundary_invalid_attribute_value"
    id: concept:page_635_svelte
    description: |
      Extracted from documentation: svelte_boundary_invalid_attribute_value
  - name: "svelte_component_invalid_this"
    id: concept:page_636_svelte
    description: |
      Extracted from documentation: svelte_component_invalid_this
  - name: "svelte_component_missing_this"
    id: concept:page_637_svelte
    description: |
      Extracted from documentation: svelte_component_missing_this
  - name: "svelte_element_missing_this"
    id: concept:page_638_svelte
    description: |
      Extracted from documentation: svelte_element_missing_this
  - name: "svelte_fragment_invalid_attribute"
    id: concept:page_639_svelte
    description: |
      Extracted from documentation: svelte_fragment_invalid_attribute
  - name: "svelte_fragment_invalid_placement"
    id: concept:page_640_svelte
    description: |
      Extracted from documentation: svelte_fragment_invalid_placement
  - name: "svelte_head_illegal_attribute"
    id: concept:page_641_svelte
    description: |
      Extracted from documentation: svelte_head_illegal_attribute
  - name: "svelte_meta_duplicate"
    id: concept:page_642_svelte
    description: |
      Extracted from documentation: svelte_meta_duplicate
  - name: "svelte_meta_invalid_content"
    id: concept:page_643_svelte
    description: |
      Extracted from documentation: svelte_meta_invalid_content
  - name: "svelte_meta_invalid_placement"
    id: concept:page_644_svelte
    description: |
      Extracted from documentation: svelte_meta_invalid_placement
  - name: "svelte_meta_invalid_tag"
    id: concept:page_645_svelte
    description: |
      Extracted from documentation: svelte_meta_invalid_tag
  - name: "svelte_options_deprecated_tag"
    id: concept:page_646_svelte
    description: |
      Extracted from documentation: svelte_options_deprecated_tag
  - name: "svelte_options_invalid_attribute"
    id: concept:page_647_svelte
    description: |
      Extracted from documentation: svelte_options_invalid_attribute
  - name: "svelte_options_invalid_attribute_value"
    id: concept:page_648_svelte
    description: |
      Extracted from documentation: svelte_options_invalid_attribute_value
  - name: "svelte_options_invalid_customelement"
    id: concept:page_649_svelte
    description: |
      Extracted from documentation: svelte_options_invalid_customelement
  - name: "svelte_options_invalid_customelement_props"
    id: concept:page_650_svelte
    description: |
      Extracted from documentation: svelte_options_invalid_customelement_props
  - name: "svelte_options_invalid_customelement_shadow"
    id: concept:page_651_svelte
    description: |
      Extracted from documentation: svelte_options_invalid_customelement_shadow
  - name: "svelte_options_invalid_tagname"
    id: concept:page_652_svelte
    description: |
      Extracted from documentation: svelte_options_invalid_tagname
  - name: "svelte_options_reserved_tagname"
    id: concept:page_653_svelte
    description: |
      Extracted from documentation: svelte_options_reserved_tagname
  - name: "svelte_options_unknown_attribute"
    id: concept:page_654_svelte
    description: |
      Extracted from documentation: svelte_options_unknown_attribute
  - name: "svelte_self_invalid_placement"
    id: concept:page_655_svelte
    description: |
      Extracted from documentation: svelte_self_invalid_placement
  - name: "tag_invalid_name"
    id: concept:page_656_svelte
    description: |
      Extracted from documentation: tag_invalid_name
  - name: "tag_invalid_placement"
    id: concept:page_657_svelte
    description: |
      Extracted from documentation: tag_invalid_placement
  - name: "textarea_invalid_content"
    id: concept:page_658_svelte
    description: |
      Extracted from documentation: textarea_invalid_content
  - name: "title_illegal_attribute"
    id: concept:page_659_svelte
    description: |
      Extracted from documentation: title_illegal_attribute
  - name: "title_invalid_content"
    id: concept:page_660_svelte
    description: |
      Extracted from documentation: title_invalid_content
  - name: "transition_conflict"
    id: concept:page_661_svelte
    description: |
      Extracted from documentation: transition_conflict
  - name: "transition_duplicate"
    id: concept:page_662_svelte
    description: |
      Extracted from documentation: transition_duplicate
  - name: "typescript_invalid_feature"
    id: concept:page_663_svelte
    description: |
      Extracted from documentation: typescript_invalid_feature
  - name: "unexpected_eof"
    id: concept:page_664_svelte
    description: |
      Extracted from documentation: unexpected_eof
  - name: "unexpected_reserved_word"
    id: concept:page_665_svelte
    description: |
      Extracted from documentation: unexpected_reserved_word
  - name: "unterminated_string_constant"
    id: concept:page_666_svelte
    description: |
      Extracted from documentation: unterminated_string_constant
  - name: "void_element_invalid_content"
    id: concept:page_667_svelte
    description: |
      Extracted from documentation: void_element_invalid_content
  - name: "Compiler warnings"
    id: concept:page_668_svelte
    description: |
      Extracted from documentation: Compiler warnings
  - name: "a11y_accesskey"
    id: concept:page_669_svelte
    description: |
      Extracted from documentation: a11y_accesskey
  - name: "a11y_aria_activedescendant_has_tabindex"
    id: concept:page_670_svelte
    description: |
      Extracted from documentation: a11y_aria_activedescendant_has_tabindex
  - name: "a11y_aria_attributes"
    id: concept:page_671_svelte
    description: |
      Extracted from documentation: a11y_aria_attributes
  - name: "a11y_autocomplete_valid"
    id: concept:page_672_svelte
    description: |
      Extracted from documentation: a11y_autocomplete_valid
  - name: "a11y_autofocus"
    id: concept:page_673_svelte
    description: |
      Extracted from documentation: a11y_autofocus
  - name: "a11y_click_events_have_key_events"
    id: concept:page_674_svelte
    description: |
      Extracted from documentation: a11y_click_events_have_key_events
  - name: "a11y_consider_explicit_label"
    id: concept:page_675_svelte
    description: |
      Extracted from documentation: a11y_consider_explicit_label
  - name: "a11y_distracting_elements"
    id: concept:page_676_svelte
    description: |
      Extracted from documentation: a11y_distracting_elements
  - name: "a11y_figcaption_index"
    id: concept:page_677_svelte
    description: |
      Extracted from documentation: a11y_figcaption_index
  - name: "a11y_figcaption_parent"
    id: concept:page_678_svelte
    description: |
      Extracted from documentation: a11y_figcaption_parent
  - name: "a11y_hidden"
    id: concept:page_679_svelte
    description: |
      Extracted from documentation: a11y_hidden
  - name: "a11y_img_redundant_alt"
    id: concept:page_680_svelte
    description: |
      Extracted from documentation: a11y_img_redundant_alt
  - name: "a11y_incorrect_aria_attribute_type"
    id: concept:page_681_svelte
    description: |
      Extracted from documentation: a11y_incorrect_aria_attribute_type
  - name: "a11y_incorrect_aria_attribute_type_boolean"
    id: concept:page_682_svelte
    description: |
      Extracted from documentation: a11y_incorrect_aria_attribute_type_boolean
  - name: "a11y_incorrect_aria_attribute_type_id"
    id: concept:page_683_svelte
    description: |
      Extracted from documentation: a11y_incorrect_aria_attribute_type_id
  - name: "a11y_incorrect_aria_attribute_type_idlist"
    id: concept:page_684_svelte
    description: |
      Extracted from documentation: a11y_incorrect_aria_attribute_type_idlist
  - name: "a11y_incorrect_aria_attribute_type_integer"
    id: concept:page_685_svelte
    description: |
      Extracted from documentation: a11y_incorrect_aria_attribute_type_integer
  - name: "a11y_incorrect_aria_attribute_type_token"
    id: concept:page_686_svelte
    description: |
      Extracted from documentation: a11y_incorrect_aria_attribute_type_token
  - name: "a11y_incorrect_aria_attribute_type_tokenlist"
    id: concept:page_687_svelte
    description: |
      Extracted from documentation: a11y_incorrect_aria_attribute_type_tokenlist
  - name: "a11y_incorrect_aria_attribute_type_tristate"
    id: concept:page_688_svelte
    description: |
      Extracted from documentation: a11y_incorrect_aria_attribute_type_tristate
  - name: "a11y_interactive_supports_focus"
    id: concept:page_689_svelte
    description: |
      Extracted from documentation: a11y_interactive_supports_focus
  - name: "a11y_invalid_attribute"
    id: concept:page_690_svelte
    description: |
      Extracted from documentation: a11y_invalid_attribute
  - name: "a11y_label_has_associated_control"
    id: concept:page_691_svelte
    description: |
      Extracted from documentation: a11y_label_has_associated_control
  - name: "a11y_media_has_caption"
    id: concept:page_692_svelte
    description: |
      Extracted from documentation: a11y_media_has_caption
  - name: "a11y_misplaced_role"
    id: concept:page_693_svelte
    description: |
      Extracted from documentation: a11y_misplaced_role
  - name: "a11y_misplaced_scope"
    id: concept:page_694_svelte
    description: |
      Extracted from documentation: a11y_misplaced_scope
  - name: "a11y_missing_attribute"
    id: concept:page_695_svelte
    description: |
      Extracted from documentation: a11y_missing_attribute
  - name: "a11y_missing_content"
    id: concept:page_696_svelte
    description: |
      Extracted from documentation: a11y_missing_content
  - name: "a11y_mouse_events_have_key_events"
    id: concept:page_697_svelte
    description: |
      Extracted from documentation: a11y_mouse_events_have_key_events
  - name: "a11y_no_abstract_role"
    id: concept:page_698_svelte
    description: |
      Extracted from documentation: a11y_no_abstract_role
  - name: "a11y_no_interactive_element_to_noninteractive_role"
    id: concept:page_699_svelte
    description: |
      Extracted from documentation: a11y_no_interactive_element_to_noninteractive_role
  - name: "a11y_no_noninteractive_element_interactions"
    id: concept:page_700_svelte
    description: |
      Extracted from documentation: a11y_no_noninteractive_element_interactions
  - name: "a11y_no_noninteractive_element_to_interactive_role"
    id: concept:page_701_svelte
    description: |
      Extracted from documentation: a11y_no_noninteractive_element_to_interactive_role
  - name: "a11y_no_noninteractive_tabindex"
    id: concept:page_702_svelte
    description: |
      Extracted from documentation: a11y_no_noninteractive_tabindex
  - name: "a11y_no_redundant_roles"
    id: concept:page_703_svelte
    description: |
      Extracted from documentation: a11y_no_redundant_roles
  - name: "a11y_no_static_element_interactions"
    id: concept:page_704_svelte
    description: |
      Extracted from documentation: a11y_no_static_element_interactions
  - name: "a11y_positive_tabindex"
    id: concept:page_705_svelte
    description: |
      Extracted from documentation: a11y_positive_tabindex
  - name: "a11y_role_has_required_aria_props"
    id: concept:page_706_svelte
    description: |
      Extracted from documentation: a11y_role_has_required_aria_props
  - name: "a11y_role_supports_aria_props"
    id: concept:page_707_svelte
    description: |
      Extracted from documentation: a11y_role_supports_aria_props
  - name: "a11y_role_supports_aria_props_implicit"
    id: concept:page_708_svelte
    description: |
      Extracted from documentation: a11y_role_supports_aria_props_implicit
  - name: "a11y_unknown_aria_attribute"
    id: concept:page_709_svelte
    description: |
      Extracted from documentation: a11y_unknown_aria_attribute
  - name: "a11y_unknown_role"
    id: concept:page_710_svelte
    description: |
      Extracted from documentation: a11y_unknown_role
  - name: "attribute_avoid_is"
    id: concept:page_711_svelte
    description: |
      Extracted from documentation: attribute_avoid_is
  - name: "attribute_global_event_reference"
    id: concept:page_712_svelte
    description: |
      Extracted from documentation: attribute_global_event_reference
  - name: "attribute_illegal_colon"
    id: concept:page_713_svelte
    description: |
      Extracted from documentation: attribute_illegal_colon
  - name: "attribute_invalid_property_name"
    id: concept:page_714_svelte
    description: |
      Extracted from documentation: attribute_invalid_property_name
  - name: "attribute_quoted"
    id: concept:page_715_svelte
    description: |
      Extracted from documentation: attribute_quoted
  - name: "bidirectional_control_characters"
    id: concept:page_716_svelte
    description: |
      Extracted from documentation: bidirectional_control_characters
  - name: "bind_invalid_each_rest"
    id: concept:page_717_svelte
    description: |
      Extracted from documentation: bind_invalid_each_rest
  - name: "block_empty"
    id: concept:page_718_svelte
    description: |
      Extracted from documentation: block_empty
  - name: "component_name_lowercase"
    id: concept:page_719_svelte
    description: |
      Extracted from documentation: component_name_lowercase
  - name: "css_unused_selector"
    id: concept:page_720_svelte
    description: |
      Extracted from documentation: css_unused_selector
  - name: "custom_element_props_identifier"
    id: concept:page_721_svelte
    description: |
      Extracted from documentation: custom_element_props_identifier
  - name: "element_implicitly_closed"
    id: concept:page_722_svelte
    description: |
      Extracted from documentation: element_implicitly_closed
  - name: "element_invalid_self_closing_tag"
    id: concept:page_723_svelte
    description: |
      Extracted from documentation: element_invalid_self_closing_tag
  - name: "event_directive_deprecated"
    id: concept:page_724_svelte
    description: |
      Extracted from documentation: event_directive_deprecated
  - name: "export_let_unused"
    id: concept:page_725_svelte
    description: |
      Extracted from documentation: export_let_unused
  - name: "legacy_code"
    id: concept:page_726_svelte
    description: |
      Extracted from documentation: legacy_code
  - name: "legacy_component_creation"
    id: concept:page_727_svelte
    description: |
      Extracted from documentation: legacy_component_creation
  - name: "node_invalid_placement_ssr"
    id: concept:page_728_svelte
    description: |
      Extracted from documentation: node_invalid_placement_ssr
  - name: "non_reactive_update"
    id: concept:page_729_svelte
    description: |
      Extracted from documentation: non_reactive_update
  - name: "options_deprecated_accessors"
    id: concept:page_730_svelte
    description: |
      Extracted from documentation: options_deprecated_accessors
  - name: "options_deprecated_immutable"
    id: concept:page_731_svelte
    description: |
      Extracted from documentation: options_deprecated_immutable
  - name: "options_missing_custom_element"
    id: concept:page_732_svelte
    description: |
      Extracted from documentation: options_missing_custom_element
  - name: "options_removed_enable_sourcemap"
    id: concept:page_733_svelte
    description: |
      Extracted from documentation: options_removed_enable_sourcemap
  - name: "options_removed_hydratable"
    id: concept:page_734_svelte
    description: |
      Extracted from documentation: options_removed_hydratable
  - name: "options_removed_loop_guard_timeout"
    id: concept:page_735_svelte
    description: |
      Extracted from documentation: options_removed_loop_guard_timeout
  - name: "options_renamed_ssr_dom"
    id: concept:page_736_svelte
    description: |
      Extracted from documentation: options_renamed_ssr_dom
  - name: "perf_avoid_inline_class"
    id: concept:page_737_svelte
    description: |
      Extracted from documentation: perf_avoid_inline_class
  - name: "perf_avoid_nested_class"
    id: concept:page_738_svelte
    description: |
      Extracted from documentation: perf_avoid_nested_class
  - name: "reactive_declaration_invalid_placement"
    id: concept:page_739_svelte
    description: |
      Extracted from documentation: reactive_declaration_invalid_placement
  - name: "reactive_declaration_module_script_dependency"
    id: concept:page_740_svelte
    description: |
      Extracted from documentation: reactive_declaration_module_script_dependency
  - name: "script_context_deprecated"
    id: concept:page_741_svelte
    description: |
      Extracted from documentation: script_context_deprecated
  - name: "script_unknown_attribute"
    id: concept:page_742_svelte
    description: |
      Extracted from documentation: script_unknown_attribute
  - name: "slot_element_deprecated"
    id: concept:page_743_svelte
    description: |
      Extracted from documentation: slot_element_deprecated
  - name: "state_referenced_locally"
    id: concept:page_744_svelte
    description: |
      Extracted from documentation: state_referenced_locally
  - name: "store_rune_conflict"
    id: concept:page_745_svelte
    description: |
      Extracted from documentation: store_rune_conflict
  - name: "svelte_component_deprecated"
    id: concept:page_746_svelte
    description: |
      Extracted from documentation: svelte_component_deprecated
  - name: "svelte_element_invalid_this"
    id: concept:page_747_svelte
    description: |
      Extracted from documentation: svelte_element_invalid_this
  - name: "svelte_self_deprecated"
    id: concept:page_748_svelte
    description: |
      Extracted from documentation: svelte_self_deprecated
  - name: "unknown_code"
    id: concept:page_749_svelte
    description: |
      Extracted from documentation: unknown_code
  - name: "Runtime errors"
    id: concept:page_750_svelte
    description: |
      Extracted from documentation: Runtime errors
  - name: "Client errors"
    id: concept:page_751_svelte
    description: |
      Extracted from documentation: Client errors
  - name: "async_derived_orphan"
    id: concept:page_752_svelte
    description: |
      Extracted from documentation: async_derived_orphan
  - name: "bind_invalid_checkbox_value"
    id: concept:page_753_svelte
    description: |
      Extracted from documentation: bind_invalid_checkbox_value
  - name: "bind_invalid_export"
    id: concept:page_754_svelte
    description: |
      Extracted from documentation: bind_invalid_export
  - name: "bind_not_bindable"
    id: concept:page_755_svelte
    description: |
      Extracted from documentation: bind_not_bindable
  - name: "component_api_changed"
    id: concept:page_756_svelte
    description: |
      Extracted from documentation: component_api_changed
  - name: "component_api_invalid_new"
    id: concept:page_757_svelte
    description: |
      Extracted from documentation: component_api_invalid_new
  - name: "derived_references_self"
    id: concept:page_758_svelte
    description: |
      Extracted from documentation: derived_references_self
  - name: "each_key_duplicate"
    id: concept:page_759_svelte
    description: |
      Extracted from documentation: each_key_duplicate
  - name: "each_key_volatile"
    id: concept:page_760_svelte
    description: |
      Extracted from documentation: each_key_volatile
  - name: "effect_in_teardown"
    id: concept:page_761_svelte
    description: |
      Extracted from documentation: effect_in_teardown
  - name: "effect_in_unowned_derived"
    id: concept:page_762_svelte
    description: |
      Extracted from documentation: effect_in_unowned_derived
  - name: "effect_orphan"
    id: concept:page_763_svelte
    description: |
      Extracted from documentation: effect_orphan
  - name: "effect_pending_outside_reaction"
    id: concept:page_764_svelte
    description: |
      Extracted from documentation: effect_pending_outside_reaction
  - name: "effect_update_depth_exceeded"
    id: concept:page_765_svelte
    description: |
      Extracted from documentation: effect_update_depth_exceeded
  - name: "flush_sync_in_effect"
    id: concept:page_766_svelte
    description: |
      Extracted from documentation: flush_sync_in_effect
  - name: "fork_discarded"
    id: concept:page_767_svelte
    description: |
      Extracted from documentation: fork_discarded
  - name: "fork_timing"
    id: concept:page_768_svelte
    description: |
      Extracted from documentation: fork_timing
  - name: "get_abort_signal_outside_reaction"
    id: concept:page_769_svelte
    description: |
      Extracted from documentation: get_abort_signal_outside_reaction
  - name: "hydratable_missing_but_required"
    id: concept:page_770_svelte
    description: |
      Extracted from documentation: hydratable_missing_but_required
  - name: "hydration_failed"
    id: concept:page_771_svelte
    description: |
      Extracted from documentation: hydration_failed
  - name: "invalid_snippet"
    id: concept:page_772_svelte
    description: |
      Extracted from documentation: invalid_snippet
  - name: "lifecycle_legacy_only"
    id: concept:page_773_svelte
    description: |
      Extracted from documentation: lifecycle_legacy_only
  - name: "props_invalid_value"
    id: concept:page_774_svelte
    description: |
      Extracted from documentation: props_invalid_value
  - name: "props_rest_readonly"
    id: concept:page_775_svelte
    description: |
      Extracted from documentation: props_rest_readonly
  - name: "rune_outside_svelte"
    id: concept:page_776_svelte
    description: |
      Extracted from documentation: rune_outside_svelte
  - name: "set_context_after_init"
    id: concept:page_777_svelte
    description: |
      Extracted from documentation: set_context_after_init
  - name: "state_descriptors_fixed"
    id: concept:page_778_svelte
    description: |
      Extracted from documentation: state_descriptors_fixed
  - name: "state_prototype_fixed"
    id: concept:page_779_svelte
    description: |
      Extracted from documentation: state_prototype_fixed
  - name: "state_unsafe_mutation"
    id: concept:page_780_svelte
    description: |
      Extracted from documentation: state_unsafe_mutation
  - name: "svelte_boundary_reset_onerror"
    id: concept:page_781_svelte
    description: |
      Extracted from documentation: svelte_boundary_reset_onerror
  - name: "Server errors"
    id: concept:page_782_svelte
    description: |
      Extracted from documentation: Server errors
  - name: "async_local_storage_unavailable"
    id: concept:page_783_svelte
    description: |
      Extracted from documentation: async_local_storage_unavailable
  - name: "await_invalid"
    id: concept:page_784_svelte
    description: |
      Extracted from documentation: await_invalid
  - name: "dynamic_element_invalid_tag"
    id: concept:page_785_svelte
    description: |
      Extracted from documentation: dynamic_element_invalid_tag
  - name: "html_deprecated"
    id: concept:page_786_svelte
    description: |
      Extracted from documentation: html_deprecated
  - name: "hydratable_clobbering"
    id: concept:page_787_svelte
    description: |
      Extracted from documentation: hydratable_clobbering
  - name: "hydratable_serialization_failed"
    id: concept:page_788_svelte
    description: |
      Extracted from documentation: hydratable_serialization_failed
  - name: "invalid_csp"
    id: concept:page_789_svelte
    description: |
      Extracted from documentation: invalid_csp
  - name: "invalid_id_prefix"
    id: concept:page_790_svelte
    description: |
      Extracted from documentation: invalid_id_prefix
  - name: "lifecycle_function_unavailable"
    id: concept:page_791_svelte
    description: |
      Extracted from documentation: lifecycle_function_unavailable
  - name: "server_context_required"
    id: concept:page_792_svelte
    description: |
      Extracted from documentation: server_context_required
  - name: "Shared errors"
    id: concept:page_793_svelte
    description: |
      Extracted from documentation: Shared errors
  - name: "experimental_async_required"
    id: concept:page_794_svelte
    description: |
      Extracted from documentation: experimental_async_required
  - name: "invalid_default_snippet"
    id: concept:page_795_svelte
    description: |
      Extracted from documentation: invalid_default_snippet
  - name: "invalid_snippet_arguments"
    id: concept:page_796_svelte
    description: |
      Extracted from documentation: invalid_snippet_arguments
  - name: "invariant_violation"
    id: concept:page_797_svelte
    description: |
      Extracted from documentation: invariant_violation
  - name: "lifecycle_outside_component"
    id: concept:page_798_svelte
    description: |
      Extracted from documentation: lifecycle_outside_component
  - name: "missing_context"
    id: concept:page_799_svelte
    description: |
      Extracted from documentation: missing_context
  - name: "snippet_without_render_tag"
    id: concept:page_800_svelte
    description: |
      Extracted from documentation: snippet_without_render_tag
  - name: "store_invalid_shape"
    id: concept:page_801_svelte
    description: |
      Extracted from documentation: store_invalid_shape
  - name: "svelte_element_invalid_this_value"
    id: concept:page_802_svelte
    description: |
      Extracted from documentation: svelte_element_invalid_this_value
  - name: "Runtime warnings"
    id: concept:page_803_svelte
    description: |
      Extracted from documentation: Runtime warnings
  - name: "Client warnings"
    id: concept:page_804_svelte
    description: |
      Extracted from documentation: Client warnings
  - name: "assignment_value_stale"
    id: concept:page_805_svelte
    description: |
      Extracted from documentation: assignment_value_stale
  - name: "await_reactivity_loss"
    id: concept:page_806_svelte
    description: |
      Extracted from documentation: await_reactivity_loss
  - name: "await_waterfall"
    id: concept:page_807_svelte
    description: |
      Extracted from documentation: await_waterfall
  - name: "binding_property_non_reactive"
    id: concept:page_808_svelte
    description: |
      Extracted from documentation: binding_property_non_reactive
  - name: "console_log_state"
    id: concept:page_809_svelte
    description: |
      Extracted from documentation: console_log_state
  - name: "derived_inert"
    id: concept:page_810_svelte
    description: |
      Extracted from documentation: derived_inert
  - name: "event_handler_invalid"
    id: concept:page_811_svelte
    description: |
      Extracted from documentation: event_handler_invalid
  - name: "hydratable_missing_but_expected"
    id: concept:page_812_svelte
    description: |
      Extracted from documentation: hydratable_missing_but_expected
  - name: "hydration_attribute_changed"
    id: concept:page_813_svelte
    description: |
      Extracted from documentation: hydration_attribute_changed
  - name: "hydration_html_changed"
    id: concept:page_814_svelte
    description: |
      Extracted from documentation: hydration_html_changed
  - name: "hydration_mismatch"
    id: concept:page_815_svelte
    description: |
      Extracted from documentation: hydration_mismatch
  - name: "invalid_raw_snippet_render"
    id: concept:page_816_svelte
    description: |
      Extracted from documentation: invalid_raw_snippet_render
  - name: "legacy_recursive_reactive_block"
    id: concept:page_817_svelte
    description: |
      Extracted from documentation: legacy_recursive_reactive_block
  - name: "lifecycle_double_unmount"
    id: concept:page_818_svelte
    description: |
      Extracted from documentation: lifecycle_double_unmount
  - name: "ownership_invalid_binding"
    id: concept:page_819_svelte
    description: |
      Extracted from documentation: ownership_invalid_binding
  - name: "ownership_invalid_mutation"
    id: concept:page_820_svelte
    description: |
      Extracted from documentation: ownership_invalid_mutation
  - name: "select_multiple_invalid_value"
    id: concept:page_821_svelte
    description: |
      Extracted from documentation: select_multiple_invalid_value
  - name: "state_proxy_equality_mismatch"
    id: concept:page_822_svelte
    description: |
      Extracted from documentation: state_proxy_equality_mismatch
  - name: "state_proxy_unmount"
    id: concept:page_823_svelte
    description: |
      Extracted from documentation: state_proxy_unmount
  - name: "svelte_boundary_reset_noop"
    id: concept:page_824_svelte
    description: |
      Extracted from documentation: svelte_boundary_reset_noop
  - name: "transition_slide_display"
    id: concept:page_825_svelte
    description: |
      Extracted from documentation: transition_slide_display
  - name: "Shared warnings"
    id: concept:page_826_svelte
    description: |
      Extracted from documentation: Shared warnings
  - name: "dynamic_void_element_content"
    id: concept:page_827_svelte
    description: |
      Extracted from documentation: dynamic_void_element_content
  - name: "state_snapshot_uncloneable"
    id: concept:page_828_svelte
    description: |
      Extracted from documentation: state_snapshot_uncloneable
  - name: "Reactive let/var declarations"
    id: concept:page_829_svelte
    description: |
      Extracted from documentation: Reactive let/var declarations
  - name: "Reactive $: statements"
    id: concept:page_830_svelte
    description: |
      Extracted from documentation: Reactive $: statements
  - name: "Browser-only code"
    id: concept:page_831_svelte
    description: |
      Extracted from documentation: Browser-only code
  - name: "export let"
    id: concept:page_832_svelte
    description: |
      Extracted from documentation: export let
  - name: "Component exports"
    id: concept:page_833_svelte
    description: |
      Extracted from documentation: Component exports
  - name: "$$props and $$restProps"
    id: concept:page_834_svelte
    description: |
      Extracted from documentation: $$props and $$restProps
  - name: "on:"
    id: concept:page_835_svelte
    description: |
      Extracted from documentation: on:
  - name: "`<slot>`"
    id: concept:page_836_svelte
    description: |
      Extracted from documentation: `<slot>`
  - name: "Named slots"
    id: concept:page_837_svelte
    description: |
      Extracted from documentation: Named slots
  - name: "Fallback content"
    id: concept:page_838_svelte
    description: |
      Extracted from documentation: Fallback content
  - name: "Passing data to slotted content"
    id: concept:page_839_svelte
    description: |
      Extracted from documentation: Passing data to slotted content
  - name: "$$slots"
    id: concept:page_840_svelte
    description: |
      Extracted from documentation: $$slots
  - name: "`<svelte:fragment>`"
    id: concept:page_841_svelte
    description: |
      Extracted from documentation: `<svelte:fragment>`
  - name: "`<svelte:component>`"
    id: concept:page_842_svelte
    description: |
      Extracted from documentation: `<svelte:component>`
  - name: "`<svelte:self>`"
    id: concept:page_843_svelte
    description: |
      Extracted from documentation: `<svelte:self>`
  - name: "Creating a component"
    id: concept:page_844_svelte
    description: |
      Extracted from documentation: Creating a component
  - name: "`$set`"
    id: concept:page_845_svelte
    description: |
      Extracted from documentation: `$set`
  - name: "`$on`"
    id: concept:page_846_svelte
    description: |
      Extracted from documentation: `$on`
  - name: "`$destroy`"
    id: concept:page_847_svelte
    description: |
      Extracted from documentation: `$destroy`
  - name: "Server-side component API"
    id: concept:page_848_svelte
    description: |
      Extracted from documentation: Server-side component API
  - name: "Start of SvelteKit documentation"
    id: concept:page_849_svelte
    description: |
      Extracted from documentation: Start of SvelteKit documentation
  - name: "Introduction"
    id: concept:page_850_svelte
    description: |
      Extracted from documentation: Introduction
  - name: "Before we begin"
    id: concept:page_851_svelte
    description: |
      Extracted from documentation: Before we begin
  - name: "What is SvelteKit?"
    id: concept:page_852_svelte
    description: |
      Extracted from documentation: What is SvelteKit?
  - name: "What is Svelte?"
    id: concept:page_853_svelte
    description: |
      Extracted from documentation: What is Svelte?
  - name: "SvelteKit vs Svelte"
    id: concept:page_854_svelte
    description: |
      Extracted from documentation: SvelteKit vs Svelte
  - name: "Creating a project"
    id: concept:page_855_svelte
    description: |
      Extracted from documentation: Creating a project
  - name: "Editor setup"
    id: concept:page_856_svelte
    description: |
      Extracted from documentation: Editor setup
  - name: "Project types"
    id: concept:page_857_svelte
    description: |
      Extracted from documentation: Project types
  - name: "Default rendering"
    id: concept:page_858_svelte
    description: |
      Extracted from documentation: Default rendering
  - name: "Static site generation"
    id: concept:page_859_svelte
    description: |
      Extracted from documentation: Static site generation
  - name: "Single-page app"
    id: concept:page_860_svelte
    description: |
      Extracted from documentation: Single-page app
  - name: "Multi-page app"
    id: concept:page_861_svelte
    description: |
      Extracted from documentation: Multi-page app
  - name: "Separate backend"
    id: concept:page_862_svelte
    description: |
      Extracted from documentation: Separate backend
  - name: "Serverless app"
    id: concept:page_863_svelte
    description: |
      Extracted from documentation: Serverless app
  - name: "Your own server"
    id: concept:page_864_svelte
    description: |
      Extracted from documentation: Your own server
  - name: "Container"
    id: concept:page_865_svelte
    description: |
      Extracted from documentation: Container
  - name: "Library"
    id: concept:page_866_svelte
    description: |
      Extracted from documentation: Library
  - name: "Offline app"
    id: concept:page_867_svelte
    description: |
      Extracted from documentation: Offline app
  - name: "Mobile app"
    id: concept:page_868_svelte
    description: |
      Extracted from documentation: Mobile app
  - name: "Desktop app"
    id: concept:page_869_svelte
    description: |
      Extracted from documentation: Desktop app
  - name: "Browser extension"
    id: concept:page_870_svelte
    description: |
      Extracted from documentation: Browser extension
  - name: "Embedded device"
    id: concept:page_871_svelte
    description: |
      Extracted from documentation: Embedded device
  - name: "Project structure"
    id: concept:page_872_svelte
    description: |
      Extracted from documentation: Project structure
  - name: "Project files"
    id: concept:page_873_svelte
    description: |
      Extracted from documentation: Project files
  - name: "src"
    id: concept:page_874_svelte
    description: |
      Extracted from documentation: src
  - name: "static"
    id: concept:page_875_svelte
    description: |
      Extracted from documentation: static
  - name: "tests"
    id: concept:page_876_svelte
    description: |
      Extracted from documentation: tests
  - name: "package.json"
    id: concept:page_877_svelte
    description: |
      Extracted from documentation: package.json
  - name: "svelte.config.js"
    id: concept:page_878_svelte
    description: |
      Extracted from documentation: svelte.config.js
  - name: "tsconfig.json"
    id: concept:page_879_svelte
    description: |
      Extracted from documentation: tsconfig.json
  - name: "vite.config.js"
    id: concept:page_880_svelte
    description: |
      Extracted from documentation: vite.config.js
  - name: "Other files"
    id: concept:page_881_svelte
    description: |
      Extracted from documentation: Other files
  - name: ".svelte-kit"
    id: concept:page_882_svelte
    description: |
      Extracted from documentation: .svelte-kit
  - name: "Web standards"
    id: concept:page_883_svelte
    description: |
      Extracted from documentation: Web standards
  - name: "Fetch APIs"
    id: concept:page_884_svelte
    description: |
      Extracted from documentation: Fetch APIs
  - name: "Request"
    id: concept:page_885_svelte
    description: |
      Extracted from documentation: Request
  - name: "Response"
    id: concept:page_886_svelte
    description: |
      Extracted from documentation: Response
  - name: "Headers"
    id: concept:page_887_svelte
    description: |
      Extracted from documentation: Headers
  - name: "FormData"
    id: concept:page_888_svelte
    description: |
      Extracted from documentation: FormData
  - name: "Stream APIs"
    id: concept:page_889_svelte
    description: |
      Extracted from documentation: Stream APIs
  - name: "URL APIs"
    id: concept:page_890_svelte
    description: |
      Extracted from documentation: URL APIs
  - name: "URLSearchParams"
    id: concept:page_891_svelte
    description: |
      Extracted from documentation: URLSearchParams
  - name: "Web Crypto"
    id: concept:page_892_svelte
    description: |
      Extracted from documentation: Web Crypto
  - name: "Routing"
    id: concept:page_893_svelte
    description: |
      Extracted from documentation: Routing
  - name: "+page"
    id: concept:page_894_svelte
    description: |
      Extracted from documentation: +page
  - name: "+page.svelte"
    id: concept:page_895_svelte
    description: |
      Extracted from documentation: +page.svelte
  - name: "+page.js"
    id: concept:page_896_svelte
    description: |
      Extracted from documentation: +page.js
  - name: "+page.server.js"
    id: concept:page_897_svelte
    description: |
      Extracted from documentation: +page.server.js
  - name: "+error"
    id: concept:page_898_svelte
    description: |
      Extracted from documentation: +error
  - name: "+layout"
    id: concept:page_899_svelte
    description: |
      Extracted from documentation: +layout
  - name: "+layout.svelte"
    id: concept:page_900_svelte
    description: |
      Extracted from documentation: +layout.svelte
  - name: "+layout.js"
    id: concept:page_901_svelte
    description: |
      Extracted from documentation: +layout.js
  - name: "+layout.server.js"
    id: concept:page_902_svelte
    description: |
      Extracted from documentation: +layout.server.js
  - name: "+server"
    id: concept:page_903_svelte
    description: |
      Extracted from documentation: +server
  - name: "Receiving data"
    id: concept:page_904_svelte
    description: |
      Extracted from documentation: Receiving data
  - name: "Fallback method handler"
    id: concept:page_905_svelte
    description: |
      Extracted from documentation: Fallback method handler
  - name: "Content negotiation"
    id: concept:page_906_svelte
    description: |
      Extracted from documentation: Content negotiation
  - name: "$types"
    id: concept:page_907_svelte
    description: |
      Extracted from documentation: $types
  - name: "Further reading"
    id: concept:page_908_svelte
    description: |
      Extracted from documentation: Further reading
  - name: "Loading data"
    id: concept:page_909_svelte
    description: |
      Extracted from documentation: Loading data
  - name: "Page data"
    id: concept:page_910_svelte
    description: |
      Extracted from documentation: Page data
  - name: "Layout data"
    id: concept:page_911_svelte
    description: |
      Extracted from documentation: Layout data
  - name: "page.data"
    id: concept:page_912_svelte
    description: |
      Extracted from documentation: page.data
  - name: "Universal vs server"
    id: concept:page_913_svelte
    description: |
      Extracted from documentation: Universal vs server
  - name: "When does which load function run?"
    id: concept:page_914_svelte
    description: |
      Extracted from documentation: When does which load function run?
  - name: "Input"
    id: concept:page_915_svelte
    description: |
      Extracted from documentation: Input
  - name: "Output"
    id: concept:page_916_svelte
    description: |
      Extracted from documentation: Output
  - name: "When to use which"
    id: concept:page_917_svelte
    description: |
      Extracted from documentation: When to use which
  - name: "Using URL data"
    id: concept:page_918_svelte
    description: |
      Extracted from documentation: Using URL data
  - name: "url"
    id: concept:page_919_svelte
    description: |
      Extracted from documentation: url
  - name: "route"
    id: concept:page_920_svelte
    description: |
      Extracted from documentation: route
  - name: "params"
    id: concept:page_921_svelte
    description: |
      Extracted from documentation: params
  - name: "Making fetch requests"
    id: concept:page_922_svelte
    description: |
      Extracted from documentation: Making fetch requests
  - name: "Cookies"
    id: concept:page_923_svelte
    description: |
      Extracted from documentation: Cookies
  - name: "Using parent data"
    id: concept:page_924_svelte
    description: |
      Extracted from documentation: Using parent data
  - name: "Errors"
    id: concept:page_925_svelte
    description: |
      Extracted from documentation: Errors
  - name: "Redirects"
    id: concept:page_926_svelte
    description: |
      Extracted from documentation: Redirects
  - name: "Streaming with promises"
    id: concept:page_927_svelte
    description: |
      Extracted from documentation: Streaming with promises
  - name: "Parallel loading"
    id: concept:page_928_svelte
    description: |
      Extracted from documentation: Parallel loading
  - name: "Rerunning load functions"
    id: concept:page_929_svelte
    description: |
      Extracted from documentation: Rerunning load functions
  - name: "Untracking dependencies"
    id: concept:page_930_svelte
    description: |
      Extracted from documentation: Untracking dependencies
  - name: "Manual invalidation"
    id: concept:page_931_svelte
    description: |
      Extracted from documentation: Manual invalidation
  - name: "When do load functions rerun?"
    id: concept:page_932_svelte
    description: |
      Extracted from documentation: When do load functions rerun?
  - name: "Implications for authentication"
    id: concept:page_933_svelte
    description: |
      Extracted from documentation: Implications for authentication
  - name: "Using `getRequestEvent`"
    id: concept:page_934_svelte
    description: |
      Extracted from documentation: Using `getRequestEvent`
  - name: "Form actions"
    id: concept:page_935_svelte
    description: |
      Extracted from documentation: Form actions
  - name: "Default actions"
    id: concept:page_936_svelte
    description: |
      Extracted from documentation: Default actions
  - name: "Named actions"
    id: concept:page_937_svelte
    description: |
      Extracted from documentation: Named actions
  - name: "Anatomy of an action"
    id: concept:page_938_svelte
    description: |
      Extracted from documentation: Anatomy of an action
  - name: "Validation errors"
    id: concept:page_939_svelte
    description: |
      Extracted from documentation: Validation errors
  - name: "Progressive enhancement"
    id: concept:page_940_svelte
    description: |
      Extracted from documentation: Progressive enhancement
  - name: "use:enhance"
    id: concept:page_941_svelte
    description: |
      Extracted from documentation: use:enhance
  - name: "Customising use:enhance"
    id: concept:page_942_svelte
    description: |
      Extracted from documentation: Customising use:enhance
  - name: "Custom event listener"
    id: concept:page_943_svelte
    description: |
      Extracted from documentation: Custom event listener
  - name: "Alternatives"
    id: concept:page_944_svelte
    description: |
      Extracted from documentation: Alternatives
  - name: "GET vs POST"
    id: concept:page_945_svelte
    description: |
      Extracted from documentation: GET vs POST
  - name: "Page options"
    id: concept:page_946_svelte
    description: |
      Extracted from documentation: Page options
  - name: "prerender"
    id: concept:page_947_svelte
    description: |
      Extracted from documentation: prerender
  - name: "Prerendering server routes"
    id: concept:page_948_svelte
    description: |
      Extracted from documentation: Prerendering server routes
  - name: "When not to prerender"
    id: concept:page_949_svelte
    description: |
      Extracted from documentation: When not to prerender
  - name: "Route conflicts"
    id: concept:page_950_svelte
    description: |
      Extracted from documentation: Route conflicts
  - name: "Troubleshooting"
    id: concept:page_951_svelte
    description: |
      Extracted from documentation: Troubleshooting
  - name: "entries"
    id: concept:page_952_svelte
    description: |
      Extracted from documentation: entries
  - name: "ssr"
    id: concept:page_953_svelte
    description: |
      Extracted from documentation: ssr
  - name: "csr"
    id: concept:page_954_svelte
    description: |
      Extracted from documentation: csr
  - name: "trailingSlash"
    id: concept:page_955_svelte
    description: |
      Extracted from documentation: trailingSlash
  - name: "config"
    id: concept:page_956_svelte
    description: |
      Extracted from documentation: config
  - name: "State management"
    id: concept:page_957_svelte
    description: |
      Extracted from documentation: State management
  - name: "Avoid shared state on the server"
    id: concept:page_958_svelte
    description: |
      Extracted from documentation: Avoid shared state on the server
  - name: "No side-effects in load"
    id: concept:page_959_svelte
    description: |
      Extracted from documentation: No side-effects in load
  - name: "Using state and stores with context"
    id: concept:page_960_svelte
    description: |
      Extracted from documentation: Using state and stores with context
  - name: "Component and page state is preserved"
    id: concept:page_961_svelte
    description: |
      Extracted from documentation: Component and page state is preserved
  - name: "Storing state in the URL"
    id: concept:page_962_svelte
    description: |
      Extracted from documentation: Storing state in the URL
  - name: "Storing ephemeral state in snapshots"
    id: concept:page_963_svelte
    description: |
      Extracted from documentation: Storing ephemeral state in snapshots
  - name: "Remote functions"
    id: concept:page_964_svelte
    description: |
      Extracted from documentation: Remote functions
  - name: "query"
    id: concept:page_965_svelte
    description: |
      Extracted from documentation: query
  - name: "Query arguments"
    id: concept:page_966_svelte
    description: |
      Extracted from documentation: Query arguments
  - name: "Deduplication"
    id: concept:page_967_svelte
    description: |
      Extracted from documentation: Deduplication
  - name: "Refreshing queries"
    id: concept:page_968_svelte
    description: |
      Extracted from documentation: Refreshing queries
  - name: "query.batch"
    id: concept:page_969_svelte
    description: |
      Extracted from documentation: query.batch
  - name: "query.live"
    id: concept:page_970_svelte
    description: |
      Extracted from documentation: query.live
  - name: "form"
    id: concept:page_971_svelte
    description: |
      Extracted from documentation: form
  - name: "Fields"
    id: concept:page_972_svelte
    description: |
      Extracted from documentation: Fields
  - name: "Programmatic validation"
    id: concept:page_973_svelte
    description: |
      Extracted from documentation: Programmatic validation
  - name: "Validation"
    id: concept:page_974_svelte
    description: |
      Extracted from documentation: Validation
  - name: "Getting/setting inputs"
    id: concept:page_975_svelte
    description: |
      Extracted from documentation: Getting/setting inputs
  - name: "Handling sensitive data"
    id: concept:page_976_svelte
    description: |
      Extracted from documentation: Handling sensitive data
  - name: "Returns and redirects"
    id: concept:page_977_svelte
    description: |
      Extracted from documentation: Returns and redirects
  - name: "enhance"
    id: concept:page_978_svelte
    description: |
      Extracted from documentation: enhance
  - name: "Multiple instances of a form"
    id: concept:page_979_svelte
    description: |
      Extracted from documentation: Multiple instances of a form
  - name: "Multiple submit buttons"
    id: concept:page_980_svelte
    description: |
      Extracted from documentation: Multiple submit buttons
  - name: "command"
    id: concept:page_981_svelte
    description: |
      Extracted from documentation: command
  - name: "Single-flight mutations"
    id: concept:page_982_svelte
    description: |
      Extracted from documentation: Single-flight mutations
  - name: "Server-driven refreshes"
    id: concept:page_983_svelte
    description: |
      Extracted from documentation: Server-driven refreshes
  - name: "Reconnecting live queries in mutations"
    id: concept:page_984_svelte
    description: |
      Extracted from documentation: Reconnecting live queries in mutations
  - name: "Client-requested refreshes"
    id: concept:page_985_svelte
    description: |
      Extracted from documentation: Client-requested refreshes
  - name: "Prerender arguments"
    id: concept:page_986_svelte
    description: |
      Extracted from documentation: Prerender arguments
  - name: "Handling validation errors"
    id: concept:page_987_svelte
    description: |
      Extracted from documentation: Handling validation errors
  - name: "Environment variables"
    id: concept:page_988_svelte
    description: |
      Extracted from documentation: Environment variables
  - name: "Explicit environment variables"
    id: concept:page_989_svelte
    description: |
      Extracted from documentation: Explicit environment variables
  - name: "Setup"
    id: concept:page_990_svelte
    description: |
      Extracted from documentation: Setup
  - name: "Private variables"
    id: concept:page_991_svelte
    description: |
      Extracted from documentation: Private variables
  - name: "Public variables"
    id: concept:page_992_svelte
    description: |
      Extracted from documentation: Public variables
  - name: "Static variables"
    id: concept:page_993_svelte
    description: |
      Extracted from documentation: Static variables
  - name: "Documenting variables"
    id: concept:page_994_svelte
    description: |
      Extracted from documentation: Documenting variables
  - name: "Building your app"
    id: concept:page_995_svelte
    description: |
      Extracted from documentation: Building your app
  - name: "During the build"
    id: concept:page_996_svelte
    description: |
      Extracted from documentation: During the build
  - name: "Preview your app"
    id: concept:page_997_svelte
    description: |
      Extracted from documentation: Preview your app
  - name: "Adapters"
    id: concept:page_998_svelte
    description: |
      Extracted from documentation: Adapters
  - name: "Using adapters"
    id: concept:page_999_svelte
    description: |
      Extracted from documentation: Using adapters
  - name: "Platform-specific context"
    id: concept:page_1000_svelte
    description: |
      Extracted from documentation: Platform-specific context
  - name: "Zero-config deployments"
    id: concept:page_1001_svelte
    description: |
      Extracted from documentation: Zero-config deployments
  - name: "Environment-specific configuration"
    id: concept:page_1002_svelte
    description: |
      Extracted from documentation: Environment-specific configuration
  - name: "Adding community adapters"
    id: concept:page_1003_svelte
    description: |
      Extracted from documentation: Adding community adapters
  - name: "Node servers"
    id: concept:page_1004_svelte
    description: |
      Extracted from documentation: Node servers
  - name: "Usage"
    id: concept:page_1005_svelte
    description: |
      Extracted from documentation: Usage
  - name: "Deploying"
    id: concept:page_1006_svelte
    description: |
      Extracted from documentation: Deploying
  - name: "Compressing responses"
    id: concept:page_1007_svelte
    description: |
      Extracted from documentation: Compressing responses
  - name: "`PORT`, `HOST` and `SOCKET_PATH`"
    id: concept:page_1008_svelte
    description: |
      Extracted from documentation: `PORT`, `HOST` and `SOCKET_PATH`
  - name: "`ORIGIN`, `PROTOCOL_HEADER`, `HOST_HEADER`, and `PORT_HEADER`"
    id: concept:page_1009_svelte
    description: |
      Extracted from documentation: `ORIGIN`, `PROTOCOL_HEADER`, `HOST_HEADER`, and `PORT_HEADER`
  - name: "or e.g. for local previewing and testing"
    id: concept:page_1010_svelte
    description: |
      Extracted from documentation: or e.g. for local previewing and testing
  - name: "`ADDRESS_HEADER` and `XFF_DEPTH`"
    id: concept:page_1011_svelte
    description: |
      Extracted from documentation: `ADDRESS_HEADER` and `XFF_DEPTH`
  - name: "`BODY_SIZE_LIMIT`"
    id: concept:page_1012_svelte
    description: |
      Extracted from documentation: `BODY_SIZE_LIMIT`
  - name: "`SHUTDOWN_TIMEOUT`"
    id: concept:page_1013_svelte
    description: |
      Extracted from documentation: `SHUTDOWN_TIMEOUT`
  - name: "`IDLE_TIMEOUT`"
    id: concept:page_1014_svelte
    description: |
      Extracted from documentation: `IDLE_TIMEOUT`
  - name: "`KEEP_ALIVE_TIMEOUT` and `HEADERS_TIMEOUT`"
    id: concept:page_1015_svelte
    description: |
      Extracted from documentation: `KEEP_ALIVE_TIMEOUT` and `HEADERS_TIMEOUT`
  - name: "Options"
    id: concept:page_1016_svelte
    description: |
      Extracted from documentation: Options
  - name: "out"
    id: concept:page_1017_svelte
    description: |
      Extracted from documentation: out
  - name: "precompress"
    id: concept:page_1018_svelte
    description: |
      Extracted from documentation: precompress
  - name: "envPrefix"
    id: concept:page_1019_svelte
    description: |
      Extracted from documentation: envPrefix
  - name: "Graceful shutdown"
    id: concept:page_1020_svelte
    description: |
      Extracted from documentation: Graceful shutdown
  - name: "Socket activation"
    id: concept:page_1021_svelte
    description: |
      Extracted from documentation: Socket activation
  - name: "Custom server"
    id: concept:page_1022_svelte
    description: |
      Extracted from documentation: Custom server
  - name: "Zero-config support"
    id: concept:page_1023_svelte
    description: |
      Extracted from documentation: Zero-config support
  - name: "pages"
    id: concept:page_1024_svelte
    description: |
      Extracted from documentation: pages
  - name: "assets"
    id: concept:page_1025_svelte
    description: |
      Extracted from documentation: assets
  - name: "fallback"
    id: concept:page_1026_svelte
    description: |
      Extracted from documentation: fallback
  - name: "strict"
    id: concept:page_1027_svelte
    description: |
      Extracted from documentation: strict
  - name: "GitHub Pages"
    id: concept:page_1028_svelte
    description: |
      Extracted from documentation: GitHub Pages
  - name: "file: .github/workflows/deploy.yml"
    id: concept:page_1029_svelte
    description: |
      Extracted from documentation: file: .github/workflows/deploy.yml
  - name: "If you're using pnpm, add this step then change the commands and cache key below to use `pnpm`"
    id: concept:page_1030_svelte
    description: |
      Extracted from documentation: If you're using pnpm, add this step then change the commands and cache key below to use `pnpm`
  - name: "- name: Install pnpm"
    id: concept:page_1031_svelte
    description: |
      Extracted from documentation: - name: Install pnpm
  - name: "uses: pnpm/action-setup@v6"
    id: concept:page_1032_svelte
    description: |
      Extracted from documentation: uses: pnpm/action-setup@v6
  - name: "with:"
    id: concept:page_1033_svelte
    description: |
      Extracted from documentation: with:
  - name: "version: 8"
    id: concept:page_1034_svelte
    description: |
      Extracted from documentation: version: 8
  - name: "this should match the `pages` option in your adapter-static options"
    id: concept:page_1035_svelte
    description: |
      Extracted from documentation: this should match the `pages` option in your adapter-static options
  - name: "Single-page apps"
    id: concept:page_1036_svelte
    description: |
      Extracted from documentation: Single-page apps
  - name: "Prerendering individual pages"
    id: concept:page_1037_svelte
    description: |
      Extracted from documentation: Prerendering individual pages
  - name: "Apache"
    id: concept:page_1038_svelte
    description: |
      Extracted from documentation: Apache
  - name: "Cloudflare"
    id: concept:page_1039_svelte
    description: |
      Extracted from documentation: Cloudflare
  - name: "Comparisons"
    id: concept:page_1040_svelte
    description: |
      Extracted from documentation: Comparisons
  - name: "platformProxy"
    id: concept:page_1041_svelte
    description: |
      Extracted from documentation: platformProxy
  - name: "routes"
    id: concept:page_1042_svelte
    description: |
      Extracted from documentation: routes
  - name: "Cloudflare Workers"
    id: concept:page_1043_svelte
    description: |
      Extracted from documentation: Cloudflare Workers
  - name: "Basic configuration"
    id: concept:page_1044_svelte
    description: |
      Extracted from documentation: Basic configuration
  - name: "Deployment"
    id: concept:page_1045_svelte
    description: |
      Extracted from documentation: Deployment
  - name: "Cloudflare Pages"
    id: concept:page_1046_svelte
    description: |
      Extracted from documentation: Cloudflare Pages
  - name: "Runtime APIs"
    id: concept:page_1047_svelte
    description: |
      Extracted from documentation: Runtime APIs
  - name: "Testing locally"
    id: concept:page_1048_svelte
    description: |
      Extracted from documentation: Testing locally
  - name: "Headers and redirects"
    id: concept:page_1049_svelte
    description: |
      Extracted from documentation: Headers and redirects
  - name: "Node.js compatibility"
    id: concept:page_1050_svelte
    description: |
      Extracted from documentation: Node.js compatibility
  - name: "Worker size limits"
    id: concept:page_1051_svelte
    description: |
      Extracted from documentation: Worker size limits
  - name: "Accessing the file system"
    id: concept:page_1052_svelte
    description: |
      Extracted from documentation: Accessing the file system
  - name: "Migrating from Workers Sites"
    id: concept:page_1053_svelte
    description: |
      Extracted from documentation: Migrating from Workers Sites
  - name: "wrangler.toml"
    id: concept:page_1054_svelte
    description: |
      Extracted from documentation: wrangler.toml
  - name: "wrangler.jsonc"
    id: concept:page_1055_svelte
    description: |
      Extracted from documentation: wrangler.jsonc
  - name: "Basic Configuration"
    id: concept:page_1056_svelte
    description: |
      Extracted from documentation: Basic Configuration
  - name: "Testing Locally"
    id: concept:page_1057_svelte
    description: |
      Extracted from documentation: Testing Locally
  - name: "Netlify"
    id: concept:page_1058_svelte
    description: |
      Extracted from documentation: Netlify
  - name: "Node version"
    id: concept:page_1059_svelte
    description: |
      Extracted from documentation: Node version
  - name: "Netlify Edge Functions"
    id: concept:page_1060_svelte
    description: |
      Extracted from documentation: Netlify Edge Functions
  - name: "Netlify alternatives to SvelteKit functionality"
    id: concept:page_1061_svelte
    description: |
      Extracted from documentation: Netlify alternatives to SvelteKit functionality
  - name: "`_headers` and `_redirects`"
    id: concept:page_1062_svelte
    description: |
      Extracted from documentation: `_headers` and `_redirects`
  - name: "Netlify Forms"
    id: concept:page_1063_svelte
    description: |
      Extracted from documentation: Netlify Forms
  - name: "Netlify Functions"
    id: concept:page_1064_svelte
    description: |
      Extracted from documentation: Netlify Functions
  - name: "Vercel"
    id: concept:page_1065_svelte
    description: |
      Extracted from documentation: Vercel
  - name: "Deployment configuration"
    id: concept:page_1066_svelte
    description: |
      Extracted from documentation: Deployment configuration
  - name: "Image Optimization"
    id: concept:page_1067_svelte
    description: |
      Extracted from documentation: Image Optimization
  - name: "Incremental Static Regeneration"
    id: concept:page_1068_svelte
    description: |
      Extracted from documentation: Incremental Static Regeneration
  - name: "expiration"
    id: concept:page_1069_svelte
    description: |
      Extracted from documentation: expiration
  - name: "bypassToken"
    id: concept:page_1070_svelte
    description: |
      Extracted from documentation: bypassToken
  - name: "allowQuery"
    id: concept:page_1071_svelte
    description: |
      Extracted from documentation: allowQuery
  - name: "Skew protection"
    id: concept:page_1072_svelte
    description: |
      Extracted from documentation: Skew protection
  - name: "Vercel utilities"
    id: concept:page_1073_svelte
    description: |
      Extracted from documentation: Vercel utilities
  - name: "Vercel functions"
    id: concept:page_1074_svelte
    description: |
      Extracted from documentation: Vercel functions
  - name: "Deployment protection"
    id: concept:page_1075_svelte
    description: |
      Extracted from documentation: Deployment protection
  - name: "Writing adapters"
    id: concept:page_1076_svelte
    description: |
      Extracted from documentation: Writing adapters
  - name: "Advanced routing"
    id: concept:page_1077_svelte
    description: |
      Extracted from documentation: Advanced routing
  - name: "Rest parameters"
    id: concept:page_1078_svelte
    description: |
      Extracted from documentation: Rest parameters
  - name: "404 pages"
    id: concept:page_1079_svelte
    description: |
      Extracted from documentation: 404 pages
  - name: "Optional parameters"
    id: concept:page_1080_svelte
    description: |
      Extracted from documentation: Optional parameters
  - name: "Matching"
    id: concept:page_1081_svelte
    description: |
      Extracted from documentation: Matching
  - name: "Sorting"
    id: concept:page_1082_svelte
    description: |
      Extracted from documentation: Sorting
  - name: "Encoding"
    id: concept:page_1083_svelte
    description: |
      Extracted from documentation: Encoding
  - name: "Advanced layouts"
    id: concept:page_1084_svelte
    description: |
      Extracted from documentation: Advanced layouts
  - name: "(group)"
    id: concept:page_1085_svelte
    description: |
      Extracted from documentation: (group)
  - name: "Breaking out of layouts"
    id: concept:page_1086_svelte
    description: |
      Extracted from documentation: Breaking out of layouts
  - name: "+page@"
    id: concept:page_1087_svelte
    description: |
      Extracted from documentation: +page@
  - name: "+layout@"
    id: concept:page_1088_svelte
    description: |
      Extracted from documentation: +layout@
  - name: "When to use layout groups"
    id: concept:page_1089_svelte
    description: |
      Extracted from documentation: When to use layout groups
  - name: "Hooks"
    id: concept:page_1090_svelte
    description: |
      Extracted from documentation: Hooks
  - name: "Server hooks"
    id: concept:page_1091_svelte
    description: |
      Extracted from documentation: Server hooks
  - name: "handle"
    id: concept:page_1092_svelte
    description: |
      Extracted from documentation: handle
  - name: "locals"
    id: concept:page_1093_svelte
    description: |
      Extracted from documentation: locals
  - name: "handleFetch"
    id: concept:page_1094_svelte
    description: |
      Extracted from documentation: handleFetch
  - name: "handleValidationError"
    id: concept:page_1095_svelte
    description: |
      Extracted from documentation: handleValidationError
  - name: "Shared hooks"
    id: concept:page_1096_svelte
    description: |
      Extracted from documentation: Shared hooks
  - name: "handleError"
    id: concept:page_1097_svelte
    description: |
      Extracted from documentation: handleError
  - name: "init"
    id: concept:page_1098_svelte
    description: |
      Extracted from documentation: init
  - name: "Universal hooks"
    id: concept:page_1099_svelte
    description: |
      Extracted from documentation: Universal hooks
  - name: "reroute"
    id: concept:page_1100_svelte
    description: |
      Extracted from documentation: reroute
  - name: "transport"
    id: concept:page_1101_svelte
    description: |
      Extracted from documentation: transport
  - name: "Error objects"
    id: concept:page_1102_svelte
    description: |
      Extracted from documentation: Error objects
  - name: "Expected errors"
    id: concept:page_1103_svelte
    description: |
      Extracted from documentation: Expected errors
  - name: "Unexpected errors"
    id: concept:page_1104_svelte
    description: |
      Extracted from documentation: Unexpected errors
  - name: "Rendering errors"
    id: concept:page_1105_svelte
    description: |
      Extracted from documentation: Rendering errors
  - name: "Responses"
    id: concept:page_1106_svelte
    description: |
      Extracted from documentation: Responses
  - name: "Link options"
    id: concept:page_1107_svelte
    description: |
      Extracted from documentation: Link options
  - name: "data-sveltekit-preload-data"
    id: concept:page_1108_svelte
    description: |
      Extracted from documentation: data-sveltekit-preload-data
  - name: "data-sveltekit-preload-code"
    id: concept:page_1109_svelte
    description: |
      Extracted from documentation: data-sveltekit-preload-code
  - name: "data-sveltekit-reload"
    id: concept:page_1110_svelte
    description: |
      Extracted from documentation: data-sveltekit-reload
  - name: "data-sveltekit-replacestate"
    id: concept:page_1111_svelte
    description: |
      Extracted from documentation: data-sveltekit-replacestate
  - name: "data-sveltekit-keepfocus"
    id: concept:page_1112_svelte
    description: |
      Extracted from documentation: data-sveltekit-keepfocus
  - name: "data-sveltekit-noscroll"
    id: concept:page_1113_svelte
    description: |
      Extracted from documentation: data-sveltekit-noscroll
  - name: "Disabling options"
    id: concept:page_1114_svelte
    description: |
      Extracted from documentation: Disabling options
  - name: "Service workers"
    id: concept:page_1115_svelte
    description: |
      Extracted from documentation: Service workers
  - name: "Inside the service worker"
    id: concept:page_1116_svelte
    description: |
      Extracted from documentation: Inside the service worker
  - name: "Manual registration"
    id: concept:page_1117_svelte
    description: |
      Extracted from documentation: Manual registration
  - name: "Other solutions"
    id: concept:page_1118_svelte
    description: |
      Extracted from documentation: Other solutions
  - name: "References"
    id: concept:page_1119_svelte
    description: |
      Extracted from documentation: References
  - name: "Server-only modules"
    id: concept:page_1120_svelte
    description: |
      Extracted from documentation: Server-only modules
  - name: "Private environment variables"
    id: concept:page_1121_svelte
    description: |
      Extracted from documentation: Private environment variables
  - name: "Server-only utilities"
    id: concept:page_1122_svelte
    description: |
      Extracted from documentation: Server-only utilities
  - name: "Your modules"
    id: concept:page_1123_svelte
    description: |
      Extracted from documentation: Your modules
  - name: "How it works"
    id: concept:page_1124_svelte
    description: |
      Extracted from documentation: How it works
  - name: "Snapshots"
    id: concept:page_1125_svelte
    description: |
      Extracted from documentation: Snapshots
  - name: "Shallow routing"
    id: concept:page_1126_svelte
    description: |
      Extracted from documentation: Shallow routing
  - name: "API"
    id: concept:page_1127_svelte
    description: |
      Extracted from documentation: API
  - name: "Loading data for a route"
    id: concept:page_1128_svelte
    description: |
      Extracted from documentation: Loading data for a route
  - name: "Observability"
    id: concept:page_1129_svelte
    description: |
      Extracted from documentation: Observability
  - name: "Augmenting the built-in tracing"
    id: concept:page_1130_svelte
    description: |
      Extracted from documentation: Augmenting the built-in tracing
  - name: "Development quickstart"
    id: concept:page_1131_svelte
    description: |
      Extracted from documentation: Development quickstart
  - name: "`@opentelemetry/api`"
    id: concept:page_1132_svelte
    description: |
      Extracted from documentation: `@opentelemetry/api`
  - name: "Packaging"
    id: concept:page_1133_svelte
    description: |
      Extracted from documentation: Packaging
  - name: "Anatomy of a package.json"
    id: concept:page_1134_svelte
    description: |
      Extracted from documentation: Anatomy of a package.json
  - name: "name"
    id: concept:page_1135_svelte
    description: |
      Extracted from documentation: name
  - name: "license"
    id: concept:page_1136_svelte
    description: |
      Extracted from documentation: license
  - name: "files"
    id: concept:page_1137_svelte
    description: |
      Extracted from documentation: files
  - name: "exports"
    id: concept:page_1138_svelte
    description: |
      Extracted from documentation: exports
  - name: "sideEffects"
    id: concept:page_1139_svelte
    description: |
      Extracted from documentation: sideEffects
  - name: "Source maps"
    id: concept:page_1140_svelte
    description: |
      Extracted from documentation: Source maps
  - name: "Publishing"
    id: concept:page_1141_svelte
    description: |
      Extracted from documentation: Publishing
  - name: "Auth"
    id: concept:page_1142_svelte
    description: |
      Extracted from documentation: Auth
  - name: "Sessions vs tokens"
    id: concept:page_1143_svelte
    description: |
      Extracted from documentation: Sessions vs tokens
  - name: "Integration points"
    id: concept:page_1144_svelte
    description: |
      Extracted from documentation: Integration points
  - name: "Libraries"
    id: concept:page_1145_svelte
    description: |
      Extracted from documentation: Libraries
  - name: "Guides"
    id: concept:page_1146_svelte
    description: |
      Extracted from documentation: Guides
  - name: "Performance"
    id: concept:page_1147_svelte
    description: |
      Extracted from documentation: Performance
  - name: "Diagnosing issues"
    id: concept:page_1148_svelte
    description: |
      Extracted from documentation: Diagnosing issues
  - name: "Instrumenting"
    id: concept:page_1149_svelte
    description: |
      Extracted from documentation: Instrumenting
  - name: "Optimizing assets"
    id: concept:page_1150_svelte
    description: |
      Extracted from documentation: Optimizing assets
  - name: "Images"
    id: concept:page_1151_svelte
    description: |
      Extracted from documentation: Images
  - name: "Videos"
    id: concept:page_1152_svelte
    description: |
      Extracted from documentation: Videos
  - name: "Fonts"
    id: concept:page_1153_svelte
    description: |
      Extracted from documentation: Fonts
  - name: "Reducing code size"
    id: concept:page_1154_svelte
    description: |
      Extracted from documentation: Reducing code size
  - name: "Svelte version"
    id: concept:page_1155_svelte
    description: |
      Extracted from documentation: Svelte version
  - name: "Packages"
    id: concept:page_1156_svelte
    description: |
      Extracted from documentation: Packages
  - name: "External scripts"
    id: concept:page_1157_svelte
    description: |
      Extracted from documentation: External scripts
  - name: "Selective loading"
    id: concept:page_1158_svelte
    description: |
      Extracted from documentation: Selective loading
  - name: "Navigation"
    id: concept:page_1159_svelte
    description: |
      Extracted from documentation: Navigation
  - name: "Preloading"
    id: concept:page_1160_svelte
    description: |
      Extracted from documentation: Preloading
  - name: "Non-essential data"
    id: concept:page_1161_svelte
    description: |
      Extracted from documentation: Non-essential data
  - name: "Preventing waterfalls"
    id: concept:page_1162_svelte
    description: |
      Extracted from documentation: Preventing waterfalls
  - name: "Hosting"
    id: concept:page_1163_svelte
    description: |
      Extracted from documentation: Hosting
  - name: "Icons"
    id: concept:page_1164_svelte
    description: |
      Extracted from documentation: Icons
  - name: "CSS"
    id: concept:page_1165_svelte
    description: |
      Extracted from documentation: CSS
  - name: "Svelte"
    id: concept:page_1166_svelte
    description: |
      Extracted from documentation: Svelte
  - name: "Vite's built-in handling"
    id: concept:page_1167_svelte
    description: |
      Extracted from documentation: Vite's built-in handling
  - name: "@sveltejs/enhanced-img"
    id: concept:page_1168_svelte
    description: |
      Extracted from documentation: @sveltejs/enhanced-img
  - name: "Basic usage"
    id: concept:page_1169_svelte
    description: |
      Extracted from documentation: Basic usage
  - name: "Dynamically choosing an image"
    id: concept:page_1170_svelte
    description: |
      Extracted from documentation: Dynamically choosing an image
  - name: "Intrinsic Dimensions"
    id: concept:page_1171_svelte
    description: |
      Extracted from documentation: Intrinsic Dimensions
  - name: "`srcset` and `sizes`"
    id: concept:page_1172_svelte
    description: |
      Extracted from documentation: `srcset` and `sizes`
  - name: "Per-image transforms"
    id: concept:page_1173_svelte
    description: |
      Extracted from documentation: Per-image transforms
  - name: "Loading images dynamically from a CDN"
    id: concept:page_1174_svelte
    description: |
      Extracted from documentation: Loading images dynamically from a CDN
  - name: "Accessibility"
    id: concept:page_1175_svelte
    description: |
      Extracted from documentation: Accessibility
  - name: "Route announcements"
    id: concept:page_1176_svelte
    description: |
      Extracted from documentation: Route announcements
  - name: "Focus management"
    id: concept:page_1177_svelte
    description: |
      Extracted from documentation: Focus management
  - name: "The \"lang\" attribute"
    id: concept:page_1178_svelte
    description: |
      Extracted from documentation: The "lang" attribute
  - name: "SEO"
    id: concept:page_1179_svelte
    description: |
      Extracted from documentation: SEO
  - name: "Out of the box"
    id: concept:page_1180_svelte
    description: |
      Extracted from documentation: Out of the box
  - name: "SSR"
    id: concept:page_1181_svelte
    description: |
      Extracted from documentation: SSR
  - name: "Normalized URLs"
    id: concept:page_1182_svelte
    description: |
      Extracted from documentation: Normalized URLs
  - name: "Manual setup"
    id: concept:page_1183_svelte
    description: |
      Extracted from documentation: Manual setup
  - name: "&lt;title&gt; and &lt;meta&gt;"
    id: concept:page_1184_svelte
    description: |
      Extracted from documentation: &lt;title&gt; and &lt;meta&gt;
  - name: "Sitemaps"
    id: concept:page_1185_svelte
    description: |
      Extracted from documentation: Sitemaps
  - name: "AMP"
    id: concept:page_1186_svelte
    description: |
      Extracted from documentation: AMP
  - name: "Other resources"
    id: concept:page_1187_svelte
    description: |
      Extracted from documentation: Other resources
  - name: "What can I make with SvelteKit?"
    id: concept:page_1188_svelte
    description: |
      Extracted from documentation: What can I make with SvelteKit?
  - name: "How do I include details from package.json in my application?"
    id: concept:page_1189_svelte
    description: |
      Extracted from documentation: How do I include details from package.json in my application?
  - name: "How do I fix the error I'm getting trying to include a package?"
    id: concept:page_1190_svelte
    description: |
      Extracted from documentation: How do I fix the error I'm getting trying to include a package?
  - name: "How do I use the view transitions API?"
    id: concept:page_1191_svelte
    description: |
      Extracted from documentation: How do I use the view transitions API?
  - name: "How do I set up a database?"
    id: concept:page_1192_svelte
    description: |
      Extracted from documentation: How do I set up a database?
  - name: "How do I use a client-side library accessing `document` or `window`?"
    id: concept:page_1193_svelte
    description: |
      Extracted from documentation: How do I use a client-side library accessing `document` or `window`?
  - name: "How do I use a different backend API server?"
    id: concept:page_1194_svelte
    description: |
      Extracted from documentation: How do I use a different backend API server?
  - name: "How do I use middleware?"
    id: concept:page_1195_svelte
    description: |
      Extracted from documentation: How do I use middleware?
  - name: "How do I use Yarn?"
    id: concept:page_1196_svelte
    description: |
      Extracted from documentation: How do I use Yarn?
  - name: "Does it work with Yarn 2?"
    id: concept:page_1197_svelte
    description: |
      Extracted from documentation: Does it work with Yarn 2?
  - name: "How do I use with Yarn 3?"
    id: concept:page_1198_svelte
    description: |
      Extracted from documentation: How do I use with Yarn 3?
  - name: "Integrations"
    id: concept:page_1199_svelte
    description: |
      Extracted from documentation: Integrations
  - name: "`vitePreprocess`"
    id: concept:page_1200_svelte
    description: |
      Extracted from documentation: `vitePreprocess`
  - name: "`style`"
    id: concept:page_1201_svelte
    description: |
      Extracted from documentation: `style`
  - name: "`script`"
    id: concept:page_1202_svelte
    description: |
      Extracted from documentation: `script`
  - name: "Add-ons"
    id: concept:page_1203_svelte
    description: |
      Extracted from documentation: Add-ons
  - name: "Additional integrations"
    id: concept:page_1204_svelte
    description: |
      Extracted from documentation: Additional integrations
  - name: "`svelte-preprocess`"
    id: concept:page_1205_svelte
    description: |
      Extracted from documentation: `svelte-preprocess`
  - name: "Vite plugins"
    id: concept:page_1206_svelte
    description: |
      Extracted from documentation: Vite plugins
  - name: "Integration FAQs"
    id: concept:page_1207_svelte
    description: |
      Extracted from documentation: Integration FAQs
  - name: "Breakpoint Debugging"
    id: concept:page_1208_svelte
    description: |
      Extracted from documentation: Breakpoint Debugging
  - name: "Visual Studio Code"
    id: concept:page_1209_svelte
    description: |
      Extracted from documentation: Visual Studio Code
  - name: "Launch via debug pane"
    id: concept:page_1210_svelte
    description: |
      Extracted from documentation: Launch via debug pane
  - name: "Other Editors"
    id: concept:page_1211_svelte
    description: |
      Extracted from documentation: Other Editors
  - name: "Google Chrome and Microsoft Edge Developer Tools"
    id: concept:page_1212_svelte
    description: |
      Extracted from documentation: Google Chrome and Microsoft Edge Developer Tools
  - name: "Migrating to SvelteKit v2"
    id: concept:page_1213_svelte
    description: |
      Extracted from documentation: Migrating to SvelteKit v2
  - name: "`redirect` and `error` are no longer thrown by you"
    id: concept:page_1214_svelte
    description: |
      Extracted from documentation: `redirect` and `error` are no longer thrown by you
  - name: "path is required when setting cookies"
    id: concept:page_1215_svelte
    description: |
      Extracted from documentation: path is required when setting cookies
  - name: "Top-level promises are no longer awaited"
    id: concept:page_1216_svelte
    description: |
      Extracted from documentation: Top-level promises are no longer awaited
  - name: "goto(...) changes"
    id: concept:page_1217_svelte
    description: |
      Extracted from documentation: goto(...) changes
  - name: "paths are now relative by default"
    id: concept:page_1218_svelte
    description: |
      Extracted from documentation: paths are now relative by default
  - name: "Server fetches are not trackable anymore"
    id: concept:page_1219_svelte
    description: |
      Extracted from documentation: Server fetches are not trackable anymore
  - name: "`preloadCode` arguments must be prefixed with `base`"
    id: concept:page_1220_svelte
    description: |
      Extracted from documentation: `preloadCode` arguments must be prefixed with `base`
  - name: "`resolvePath` has been removed"
    id: concept:page_1221_svelte
    description: |
      Extracted from documentation: `resolvePath` has been removed
  - name: "Improved error handling"
    id: concept:page_1222_svelte
    description: |
      Extracted from documentation: Improved error handling
  - name: "Dynamic environment variables cannot be used during prerendering"
    id: concept:page_1223_svelte
    description: |
      Extracted from documentation: Dynamic environment variables cannot be used during prerendering
  - name: "`form` and `data` have been removed from `use:enhance` callbacks"
    id: concept:page_1224_svelte
    description: |
      Extracted from documentation: `form` and `data` have been removed from `use:enhance` callbacks
  - name: "Forms containing file inputs must use `multipart/form-data`"
    id: concept:page_1225_svelte
    description: |
      Extracted from documentation: Forms containing file inputs must use `multipart/form-data`
  - name: "Generated `tsconfig.json` is more strict"
    id: concept:page_1226_svelte
    description: |
      Extracted from documentation: Generated `tsconfig.json` is more strict
  - name: "`getRequest` no longer throws errors"
    id: concept:page_1227_svelte
    description: |
      Extracted from documentation: `getRequest` no longer throws errors
  - name: "`vitePreprocess` is no longer exported from `@sveltejs/kit/vite`"
    id: concept:page_1228_svelte
    description: |
      Extracted from documentation: `vitePreprocess` is no longer exported from `@sveltejs/kit/vite`
  - name: "Updated dependency requirements"
    id: concept:page_1229_svelte
    description: |
      Extracted from documentation: Updated dependency requirements
  - name: "SvelteKit 2.12: $app/stores deprecated"
    id: concept:page_1230_svelte
    description: |
      Extracted from documentation: SvelteKit 2.12: $app/stores deprecated
  - name: "Migrating from Sapper"
    id: concept:page_1231_svelte
    description: |
      Extracted from documentation: Migrating from Sapper
  - name: "type: \"module\""
    id: concept:page_1232_svelte
    description: |
      Extracted from documentation: type: "module"
  - name: "dependencies"
    id: concept:page_1233_svelte
    description: |
      Extracted from documentation: dependencies
  - name: "devDependencies"
    id: concept:page_1234_svelte
    description: |
      Extracted from documentation: devDependencies
  - name: "scripts"
    id: concept:page_1235_svelte
    description: |
      Extracted from documentation: scripts
  - name: "Configuration"
    id: concept:page_1236_svelte
    description: |
      Extracted from documentation: Configuration
  - name: "src/client.js"
    id: concept:page_1237_svelte
    description: |
      Extracted from documentation: src/client.js
  - name: "src/server.js"
    id: concept:page_1238_svelte
    description: |
      Extracted from documentation: src/server.js
  - name: "src/service-worker.js"
    id: concept:page_1239_svelte
    description: |
      Extracted from documentation: src/service-worker.js
  - name: "src/template.html"
    id: concept:page_1240_svelte
    description: |
      Extracted from documentation: src/template.html
  - name: "src/node_modules"
    id: concept:page_1241_svelte
    description: |
      Extracted from documentation: src/node_modules
  - name: "Pages and layouts"
    id: concept:page_1242_svelte
    description: |
      Extracted from documentation: Pages and layouts
  - name: "Renamed files"
    id: concept:page_1243_svelte
    description: |
      Extracted from documentation: Renamed files
  - name: "Imports"
    id: concept:page_1244_svelte
    description: |
      Extracted from documentation: Imports
  - name: "Preload"
    id: concept:page_1245_svelte
    description: |
      Extracted from documentation: Preload
  - name: "Segments"
    id: concept:page_1246_svelte
    description: |
      Extracted from documentation: Segments
  - name: "URLs"
    id: concept:page_1247_svelte
    description: |
      Extracted from documentation: URLs
  - name: "&lt;a&gt; attributes"
    id: concept:page_1248_svelte
    description: |
      Extracted from documentation: &lt;a&gt; attributes
  - name: "Endpoints"
    id: concept:page_1249_svelte
    description: |
      Extracted from documentation: Endpoints
  - name: "HTML minifier"
    id: concept:page_1250_svelte
    description: |
      Extracted from documentation: HTML minifier
  - name: "Additional resources"
    id: concept:page_1251_svelte
    description: |
      Extracted from documentation: Additional resources
  - name: "FAQs"
    id: concept:page_1252_svelte
    description: |
      Extracted from documentation: FAQs
  - name: "Examples"
    id: concept:page_1253_svelte
    description: |
      Extracted from documentation: Examples
  - name: "Support"
    id: concept:page_1254_svelte
    description: |
      Extracted from documentation: Support
  - name: "Glossary"
    id: concept:page_1255_svelte
    description: |
      Extracted from documentation: Glossary
  - name: "CSR"
    id: concept:page_1256_svelte
    description: |
      Extracted from documentation: CSR
  - name: "Edge"
    id: concept:page_1257_svelte
    description: |
      Extracted from documentation: Edge
  - name: "Hybrid app"
    id: concept:page_1258_svelte
    description: |
      Extracted from documentation: Hybrid app
  - name: "Hydration"
    id: concept:page_1259_svelte
    description: |
      Extracted from documentation: Hydration
  - name: "ISR"
    id: concept:page_1260_svelte
    description: |
      Extracted from documentation: ISR
  - name: "MPA"
    id: concept:page_1261_svelte
    description: |
      Extracted from documentation: MPA
  - name: "Prerendering"
    id: concept:page_1262_svelte
    description: |
      Extracted from documentation: Prerendering
  - name: "PWA"
    id: concept:page_1263_svelte
    description: |
      Extracted from documentation: PWA
  - name: "SPA"
    id: concept:page_1264_svelte
    description: |
      Extracted from documentation: SPA
  - name: "SSG"
    id: concept:page_1265_svelte
    description: |
      Extracted from documentation: SSG
  - name: "@sveltejs/kit"
    id: concept:page_1266_svelte
    description: |
      Extracted from documentation: @sveltejs/kit
  - name: "Server"
    id: concept:page_1267_svelte
    description: |
      Extracted from documentation: Server
  - name: "error"
    id: concept:page_1268_svelte
    description: |
      Extracted from documentation: error
  - name: "fail"
    id: concept:page_1269_svelte
    description: |
      Extracted from documentation: fail
  - name: "invalid"
    id: concept:page_1270_svelte
    description: |
      Extracted from documentation: invalid
  - name: "isActionFailure"
    id: concept:page_1271_svelte
    description: |
      Extracted from documentation: isActionFailure
  - name: "isHttpError"
    id: concept:page_1272_svelte
    description: |
      Extracted from documentation: isHttpError
  - name: "isRedirect"
    id: concept:page_1273_svelte
    description: |
      Extracted from documentation: isRedirect
  - name: "isValidationError"
    id: concept:page_1274_svelte
    description: |
      Extracted from documentation: isValidationError
  - name: "json"
    id: concept:page_1275_svelte
    description: |
      Extracted from documentation: json
  - name: "normalizeUrl"
    id: concept:page_1276_svelte
    description: |
      Extracted from documentation: normalizeUrl
  - name: "redirect"
    id: concept:page_1277_svelte
    description: |
      Extracted from documentation: redirect
  - name: "text"
    id: concept:page_1278_svelte
    description: |
      Extracted from documentation: text
  - name: "ActionFailure"
    id: concept:page_1279_svelte
    description: |
      Extracted from documentation: ActionFailure
  - name: "ActionResult"
    id: concept:page_1280_svelte
    description: |
      Extracted from documentation: ActionResult
  - name: "Actions"
    id: concept:page_1281_svelte
    description: |
      Extracted from documentation: Actions
  - name: "Adapter"
    id: concept:page_1282_svelte
    description: |
      Extracted from documentation: Adapter
  - name: "AfterNavigate"
    id: concept:page_1283_svelte
    description: |
      Extracted from documentation: AfterNavigate
  - name: "AwaitedActions"
    id: concept:page_1284_svelte
    description: |
      Extracted from documentation: AwaitedActions
  - name: "BeforeNavigate"
    id: concept:page_1285_svelte
    description: |
      Extracted from documentation: BeforeNavigate
  - name: "Builder"
    id: concept:page_1286_svelte
    description: |
      Extracted from documentation: Builder
  - name: "ClientInit"
    id: concept:page_1287_svelte
    description: |
      Extracted from documentation: ClientInit
  - name: "Config"
    id: concept:page_1288_svelte
    description: |
      Extracted from documentation: Config
  - name: "Emulator"
    id: concept:page_1289_svelte
    description: |
      Extracted from documentation: Emulator
  - name: "EnvVarConfig"
    id: concept:page_1290_svelte
    description: |
      Extracted from documentation: EnvVarConfig
  - name: "Handle"
    id: concept:page_1291_svelte
    description: |
      Extracted from documentation: Handle
  - name: "HandleClientError"
    id: concept:page_1292_svelte
    description: |
      Extracted from documentation: HandleClientError
  - name: "HandleFetch"
    id: concept:page_1293_svelte
    description: |
      Extracted from documentation: HandleFetch
  - name: "HandleServerError"
    id: concept:page_1294_svelte
    description: |
      Extracted from documentation: HandleServerError
  - name: "HandleValidationError"
    id: concept:page_1295_svelte
    description: |
      Extracted from documentation: HandleValidationError
  - name: "HttpError"
    id: concept:page_1296_svelte
    description: |
      Extracted from documentation: HttpError
  - name: "InvalidField"
    id: concept:page_1297_svelte
    description: |
      Extracted from documentation: InvalidField
  - name: "KitConfig"
    id: concept:page_1298_svelte
    description: |
      Extracted from documentation: KitConfig
  - name: "LessThan"
    id: concept:page_1299_svelte
    description: |
      Extracted from documentation: LessThan
  - name: "LiveQueryRequestedResult"
    id: concept:page_1300_svelte
    description: |
      Extracted from documentation: LiveQueryRequestedResult
  - name: "LiveRequestedEntry"
    id: concept:page_1301_svelte
    description: |
      Extracted from documentation: LiveRequestedEntry
  - name: "Load"
    id: concept:page_1302_svelte
    description: |
      Extracted from documentation: Load
  - name: "LoadEvent"
    id: concept:page_1303_svelte
    description: |
      Extracted from documentation: LoadEvent
  - name: "LoadProperties"
    id: concept:page_1304_svelte
    description: |
      Extracted from documentation: LoadProperties
  - name: "NavigationBase"
    id: concept:page_1305_svelte
    description: |
      Extracted from documentation: NavigationBase
  - name: "NavigationEnter"
    id: concept:page_1306_svelte
    description: |
      Extracted from documentation: NavigationEnter
  - name: "NavigationEvent"
    id: concept:page_1307_svelte
    description: |
      Extracted from documentation: NavigationEvent
  - name: "NavigationExternal"
    id: concept:page_1308_svelte
    description: |
      Extracted from documentation: NavigationExternal
  - name: "NavigationFormSubmit"
    id: concept:page_1309_svelte
    description: |
      Extracted from documentation: NavigationFormSubmit
  - name: "NavigationGoto"
    id: concept:page_1310_svelte
    description: |
      Extracted from documentation: NavigationGoto
  - name: "NavigationLeave"
    id: concept:page_1311_svelte
    description: |
      Extracted from documentation: NavigationLeave
  - name: "NavigationLink"
    id: concept:page_1312_svelte
    description: |
      Extracted from documentation: NavigationLink
  - name: "NavigationPopState"
    id: concept:page_1313_svelte
    description: |
      Extracted from documentation: NavigationPopState
  - name: "NavigationTarget"
    id: concept:page_1314_svelte
    description: |
      Extracted from documentation: NavigationTarget
  - name: "NavigationType"
    id: concept:page_1315_svelte
    description: |
      Extracted from documentation: NavigationType
  - name: "NumericRange"
    id: concept:page_1316_svelte
    description: |
      Extracted from documentation: NumericRange
  - name: "OnNavigate"
    id: concept:page_1317_svelte
    description: |
      Extracted from documentation: OnNavigate
  - name: "Page"
    id: concept:page_1318_svelte
    description: |
      Extracted from documentation: Page
  - name: "ParamMatcher"
    id: concept:page_1319_svelte
    description: |
      Extracted from documentation: ParamMatcher
  - name: "PrerenderOption"
    id: concept:page_1320_svelte
    description: |
      Extracted from documentation: PrerenderOption
  - name: "QueryRequestedResult"
    id: concept:page_1321_svelte
    description: |
      Extracted from documentation: QueryRequestedResult
  - name: "Redirect"
    id: concept:page_1322_svelte
    description: |
      Extracted from documentation: Redirect
  - name: "RemoteCommand"
    id: concept:page_1323_svelte
    description: |
      Extracted from documentation: RemoteCommand
  - name: "RemoteForm"
    id: concept:page_1324_svelte
    description: |
      Extracted from documentation: RemoteForm
  - name: "RemoteFormEnhanceCallback"
    id: concept:page_1325_svelte
    description: |
      Extracted from documentation: RemoteFormEnhanceCallback
  - name: "RemoteFormEnhanceInstance"
    id: concept:page_1326_svelte
    description: |
      Extracted from documentation: RemoteFormEnhanceInstance
  - name: "RemoteFormField"
    id: concept:page_1327_svelte
    description: |
      Extracted from documentation: RemoteFormField
  - name: "RemoteFormFieldType"
    id: concept:page_1328_svelte
    description: |
      Extracted from documentation: RemoteFormFieldType
  - name: "RemoteFormFieldValue"
    id: concept:page_1329_svelte
    description: |
      Extracted from documentation: RemoteFormFieldValue
  - name: "RemoteFormFields"
    id: concept:page_1330_svelte
    description: |
      Extracted from documentation: RemoteFormFields
  - name: "RemoteFormInput"
    id: concept:page_1331_svelte
    description: |
      Extracted from documentation: RemoteFormInput
  - name: "RemoteFormIssue"
    id: concept:page_1332_svelte
    description: |
      Extracted from documentation: RemoteFormIssue
  - name: "RemoteLiveQuery"
    id: concept:page_1333_svelte
    description: |
      Extracted from documentation: RemoteLiveQuery
  - name: "RemoteLiveQueryFunction"
    id: concept:page_1334_svelte
    description: |
      Extracted from documentation: RemoteLiveQueryFunction
  - name: "RemotePrerenderFunction"
    id: concept:page_1335_svelte
    description: |
      Extracted from documentation: RemotePrerenderFunction
  - name: "RemoteQuery"
    id: concept:page_1336_svelte
    description: |
      Extracted from documentation: RemoteQuery
  - name: "RemoteQueryFunction"
    id: concept:page_1337_svelte
    description: |
      Extracted from documentation: RemoteQueryFunction
  - name: "RemoteQueryOverride"
    id: concept:page_1338_svelte
    description: |
      Extracted from documentation: RemoteQueryOverride
  - name: "RemoteQueryUpdate"
    id: concept:page_1339_svelte
    description: |
      Extracted from documentation: RemoteQueryUpdate
  - name: "RemoteResource"
    id: concept:page_1340_svelte
    description: |
      Extracted from documentation: RemoteResource
  - name: "RequestEvent"
    id: concept:page_1341_svelte
    description: |
      Extracted from documentation: RequestEvent
  - name: "RequestHandler"
    id: concept:page_1342_svelte
    description: |
      Extracted from documentation: RequestHandler
  - name: "RequestedEntry"
    id: concept:page_1343_svelte
    description: |
      Extracted from documentation: RequestedEntry
  - name: "RequestedResult"
    id: concept:page_1344_svelte
    description: |
      Extracted from documentation: RequestedResult
  - name: "Reroute"
    id: concept:page_1345_svelte
    description: |
      Extracted from documentation: Reroute
  - name: "ResolveOptions"
    id: concept:page_1346_svelte
    description: |
      Extracted from documentation: ResolveOptions
  - name: "RouteDefinition"
    id: concept:page_1347_svelte
    description: |
      Extracted from documentation: RouteDefinition
  - name: "SSRManifest"
    id: concept:page_1348_svelte
    description: |
      Extracted from documentation: SSRManifest
  - name: "ServerInit"
    id: concept:page_1349_svelte
    description: |
      Extracted from documentation: ServerInit
  - name: "ServerInitOptions"
    id: concept:page_1350_svelte
    description: |
      Extracted from documentation: ServerInitOptions
  - name: "ServerLoad"
    id: concept:page_1351_svelte
    description: |
      Extracted from documentation: ServerLoad
  - name: "ServerLoadEvent"
    id: concept:page_1352_svelte
    description: |
      Extracted from documentation: ServerLoadEvent
  - name: "Snapshot"
    id: concept:page_1353_svelte
    description: |
      Extracted from documentation: Snapshot
  - name: "SubmitFunction"
    id: concept:page_1354_svelte
    description: |
      Extracted from documentation: SubmitFunction
  - name: "Transport"
    id: concept:page_1355_svelte
    description: |
      Extracted from documentation: Transport
  - name: "Transporter"
    id: concept:page_1356_svelte
    description: |
      Extracted from documentation: Transporter
  - name: "ValidationError"
    id: concept:page_1357_svelte
    description: |
      Extracted from documentation: ValidationError
  - name: "Private types"
    id: concept:page_1358_svelte
    description: |
      Extracted from documentation: Private types
  - name: "AdapterEntry"
    id: concept:page_1359_svelte
    description: |
      Extracted from documentation: AdapterEntry
  - name: "Csp"
    id: concept:page_1360_svelte
    description: |
      Extracted from documentation: Csp
  - name: "CspDirectives"
    id: concept:page_1361_svelte
    description: |
      Extracted from documentation: CspDirectives
  - name: "DeepPartial"
    id: concept:page_1362_svelte
    description: |
      Extracted from documentation: DeepPartial
  - name: "HasNonOptionalBoolean"
    id: concept:page_1363_svelte
    description: |
      Extracted from documentation: HasNonOptionalBoolean
  - name: "HttpMethod"
    id: concept:page_1364_svelte
    description: |
      Extracted from documentation: HttpMethod
  - name: "IsAny"
    id: concept:page_1365_svelte
    description: |
      Extracted from documentation: IsAny
  - name: "Logger"
    id: concept:page_1366_svelte
    description: |
      Extracted from documentation: Logger
  - name: "MaybePromise"
    id: concept:page_1367_svelte
    description: |
      Extracted from documentation: MaybePromise
  - name: "PrerenderEntryGeneratorMismatchHandler"
    id: concept:page_1368_svelte
    description: |
      Extracted from documentation: PrerenderEntryGeneratorMismatchHandler
  - name: "PrerenderEntryGeneratorMismatchHandlerValue"
    id: concept:page_1369_svelte
    description: |
      Extracted from documentation: PrerenderEntryGeneratorMismatchHandlerValue
  - name: "PrerenderHttpErrorHandler"
    id: concept:page_1370_svelte
    description: |
      Extracted from documentation: PrerenderHttpErrorHandler
  - name: "PrerenderHttpErrorHandlerValue"
    id: concept:page_1371_svelte
    description: |
      Extracted from documentation: PrerenderHttpErrorHandlerValue
  - name: "PrerenderInvalidUrlHandler"
    id: concept:page_1372_svelte
    description: |
      Extracted from documentation: PrerenderInvalidUrlHandler
  - name: "PrerenderInvalidUrlHandlerValue"
    id: concept:page_1373_svelte
    description: |
      Extracted from documentation: PrerenderInvalidUrlHandlerValue
  - name: "PrerenderMap"
    id: concept:page_1374_svelte
    description: |
      Extracted from documentation: PrerenderMap
  - name: "PrerenderMissingIdHandler"
    id: concept:page_1375_svelte
    description: |
      Extracted from documentation: PrerenderMissingIdHandler
  - name: "PrerenderMissingIdHandlerValue"
    id: concept:page_1376_svelte
    description: |
      Extracted from documentation: PrerenderMissingIdHandlerValue
  - name: "PrerenderUnseenRoutesHandler"
    id: concept:page_1377_svelte
    description: |
      Extracted from documentation: PrerenderUnseenRoutesHandler
  - name: "PrerenderUnseenRoutesHandlerValue"
    id: concept:page_1378_svelte
    description: |
      Extracted from documentation: PrerenderUnseenRoutesHandlerValue
  - name: "Prerendered"
    id: concept:page_1379_svelte
    description: |
      Extracted from documentation: Prerendered
  - name: "RequestOptions"
    id: concept:page_1380_svelte
    description: |
      Extracted from documentation: RequestOptions
  - name: "RouteSegment"
    id: concept:page_1381_svelte
    description: |
      Extracted from documentation: RouteSegment
  - name: "TrailingSlash"
    id: concept:page_1382_svelte
    description: |
      Extracted from documentation: TrailingSlash
  - name: "@sveltejs/kit/hooks"
    id: concept:page_1383_svelte
    description: |
      Extracted from documentation: @sveltejs/kit/hooks
  - name: "defineEnvVars"
    id: concept:page_1384_svelte
    description: |
      Extracted from documentation: defineEnvVars
  - name: "sequence"
    id: concept:page_1385_svelte
    description: |
      Extracted from documentation: sequence
  - name: "@sveltejs/kit/node/polyfills"
    id: concept:page_1386_svelte
    description: |
      Extracted from documentation: @sveltejs/kit/node/polyfills
  - name: "installPolyfills"
    id: concept:page_1387_svelte
    description: |
      Extracted from documentation: installPolyfills
  - name: "@sveltejs/kit/node"
    id: concept:page_1388_svelte
    description: |
      Extracted from documentation: @sveltejs/kit/node
  - name: "createReadableStream"
    id: concept:page_1389_svelte
    description: |
      Extracted from documentation: createReadableStream
  - name: "getRequest"
    id: concept:page_1390_svelte
    description: |
      Extracted from documentation: getRequest
  - name: "setResponse"
    id: concept:page_1391_svelte
    description: |
      Extracted from documentation: setResponse
  - name: "@sveltejs/kit/vite"
    id: concept:page_1392_svelte
    description: |
      Extracted from documentation: @sveltejs/kit/vite
  - name: "sveltekit"
    id: concept:page_1393_svelte
    description: |
      Extracted from documentation: sveltekit
  - name: "$app/env"
    id: concept:page_1394_svelte
    description: |
      Extracted from documentation: $app/env
  - name: "browser"
    id: concept:page_1395_svelte
    description: |
      Extracted from documentation: browser
  - name: "building"
    id: concept:page_1396_svelte
    description: |
      Extracted from documentation: building
  - name: "dev"
    id: concept:page_1397_svelte
    description: |
      Extracted from documentation: dev
  - name: "version"
    id: concept:page_1398_svelte
    description: |
      Extracted from documentation: version
  - name: "$app/env/private"
    id: concept:page_1399_svelte
    description: |
      Extracted from documentation: $app/env/private
  - name: "$app/env/public"
    id: concept:page_1400_svelte
    description: |
      Extracted from documentation: $app/env/public
  - name: "$app/environment"
    id: concept:page_1401_svelte
    description: |
      Extracted from documentation: $app/environment
  - name: "$app/forms"
    id: concept:page_1402_svelte
    description: |
      Extracted from documentation: $app/forms
  - name: "applyAction"
    id: concept:page_1403_svelte
    description: |
      Extracted from documentation: applyAction
  - name: "deserialize"
    id: concept:page_1404_svelte
    description: |
      Extracted from documentation: deserialize
  - name: "$app/navigation"
    id: concept:page_1405_svelte
    description: |
      Extracted from documentation: $app/navigation
  - name: "afterNavigate"
    id: concept:page_1406_svelte
    description: |
      Extracted from documentation: afterNavigate
  - name: "beforeNavigate"
    id: concept:page_1407_svelte
    description: |
      Extracted from documentation: beforeNavigate
  - name: "disableScrollHandling"
    id: concept:page_1408_svelte
    description: |
      Extracted from documentation: disableScrollHandling
  - name: "goto"
    id: concept:page_1409_svelte
    description: |
      Extracted from documentation: goto
  - name: "invalidate"
    id: concept:page_1410_svelte
    description: |
      Extracted from documentation: invalidate
  - name: "invalidateAll"
    id: concept:page_1411_svelte
    description: |
      Extracted from documentation: invalidateAll
  - name: "onNavigate"
    id: concept:page_1412_svelte
    description: |
      Extracted from documentation: onNavigate
  - name: "preloadCode"
    id: concept:page_1413_svelte
    description: |
      Extracted from documentation: preloadCode
  - name: "preloadData"
    id: concept:page_1414_svelte
    description: |
      Extracted from documentation: preloadData
  - name: "pushState"
    id: concept:page_1415_svelte
    description: |
      Extracted from documentation: pushState
  - name: "refreshAll"
    id: concept:page_1416_svelte
    description: |
      Extracted from documentation: refreshAll
  - name: "replaceState"
    id: concept:page_1417_svelte
    description: |
      Extracted from documentation: replaceState
  - name: "$app/paths"
    id: concept:page_1418_svelte
    description: |
      Extracted from documentation: $app/paths
  - name: "asset"
    id: concept:page_1419_svelte
    description: |
      Extracted from documentation: asset
  - name: "base"
    id: concept:page_1420_svelte
    description: |
      Extracted from documentation: base
  - name: "match"
    id: concept:page_1421_svelte
    description: |
      Extracted from documentation: match
  - name: "resolve"
    id: concept:page_1422_svelte
    description: |
      Extracted from documentation: resolve
  - name: "resolveRoute"
    id: concept:page_1423_svelte
    description: |
      Extracted from documentation: resolveRoute
  - name: "$app/server"
    id: concept:page_1424_svelte
    description: |
      Extracted from documentation: $app/server
  - name: "getRequestEvent"
    id: concept:page_1425_svelte
    description: |
      Extracted from documentation: getRequestEvent
  - name: "read"
    id: concept:page_1426_svelte
    description: |
      Extracted from documentation: read
  - name: "requested"
    id: concept:page_1427_svelte
    description: |
      Extracted from documentation: requested
  - name: "$app/state"
    id: concept:page_1428_svelte
    description: |
      Extracted from documentation: $app/state
  - name: "navigating"
    id: concept:page_1429_svelte
    description: |
      Extracted from documentation: navigating
  - name: "page"
    id: concept:page_1430_svelte
    description: |
      Extracted from documentation: page
  - name: "updated"
    id: concept:page_1431_svelte
    description: |
      Extracted from documentation: updated
  - name: "$app/stores"
    id: concept:page_1432_svelte
    description: |
      Extracted from documentation: $app/stores
  - name: "getStores"
    id: concept:page_1433_svelte
    description: |
      Extracted from documentation: getStores
  - name: "$app/types"
    id: concept:page_1434_svelte
    description: |
      Extracted from documentation: $app/types
  - name: "Asset"
    id: concept:page_1435_svelte
    description: |
      Extracted from documentation: Asset
  - name: "RouteId"
    id: concept:page_1436_svelte
    description: |
      Extracted from documentation: RouteId
  - name: "Pathname"
    id: concept:page_1437_svelte
    description: |
      Extracted from documentation: Pathname
  - name: "ResolvedPathname"
    id: concept:page_1438_svelte
    description: |
      Extracted from documentation: ResolvedPathname
  - name: "RouteParams"
    id: concept:page_1439_svelte
    description: |
      Extracted from documentation: RouteParams
  - name: "LayoutParams"
    id: concept:page_1440_svelte
    description: |
      Extracted from documentation: LayoutParams
  - name: "$env/dynamic/private"
    id: concept:page_1441_svelte
    description: |
      Extracted from documentation: $env/dynamic/private
  - name: "$env/dynamic/public"
    id: concept:page_1442_svelte
    description: |
      Extracted from documentation: $env/dynamic/public
  - name: "$env/static/private"
    id: concept:page_1443_svelte
    description: |
      Extracted from documentation: $env/static/private
  - name: "$env/static/public"
    id: concept:page_1444_svelte
    description: |
      Extracted from documentation: $env/static/public
  - name: "$lib"
    id: concept:page_1445_svelte
    description: |
      Extracted from documentation: $lib
  - name: "$service-worker"
    id: concept:page_1446_svelte
    description: |
      Extracted from documentation: $service-worker
  - name: "build"
    id: concept:page_1447_svelte
    description: |
      Extracted from documentation: build
  - name: "prerendered"
    id: concept:page_1448_svelte
    description: |
      Extracted from documentation: prerendered
  - name: "adapter"
    id: concept:page_1449_svelte
    description: |
      Extracted from documentation: adapter
  - name: "alias"
    id: concept:page_1450_svelte
    description: |
      Extracted from documentation: alias
  - name: "appDir"
    id: concept:page_1451_svelte
    description: |
      Extracted from documentation: appDir
  - name: "csp"
    id: concept:page_1452_svelte
    description: |
      Extracted from documentation: csp
  - name: "csrf"
    id: concept:page_1453_svelte
    description: |
      Extracted from documentation: csrf
  - name: "embedded"
    id: concept:page_1454_svelte
    description: |
      Extracted from documentation: embedded
  - name: "env"
    id: concept:page_1455_svelte
    description: |
      Extracted from documentation: env
  - name: "experimental"
    id: concept:page_1456_svelte
    description: |
      Extracted from documentation: experimental
  - name: "inlineStyleThreshold"
    id: concept:page_1457_svelte
    description: |
      Extracted from documentation: inlineStyleThreshold
  - name: "moduleExtensions"
    id: concept:page_1458_svelte
    description: |
      Extracted from documentation: moduleExtensions
  - name: "outDir"
    id: concept:page_1459_svelte
    description: |
      Extracted from documentation: outDir
  - name: "output"
    id: concept:page_1460_svelte
    description: |
      Extracted from documentation: output
  - name: "paths"
    id: concept:page_1461_svelte
    description: |
      Extracted from documentation: paths
  - name: "router"
    id: concept:page_1462_svelte
    description: |
      Extracted from documentation: router
  - name: "serviceWorker"
    id: concept:page_1463_svelte
    description: |
      Extracted from documentation: serviceWorker
  - name: "typescript"
    id: concept:page_1464_svelte
    description: |
      Extracted from documentation: typescript
  - name: "Command Line Interface"
    id: concept:page_1465_svelte
    description: |
      Extracted from documentation: Command Line Interface
  - name: "svelte-kit sync"
    id: concept:page_1466_svelte
    description: |
      Extracted from documentation: svelte-kit sync
  - name: "Types"
    id: concept:page_1467_svelte
    description: |
      Extracted from documentation: Types
  - name: "Generated types"
    id: concept:page_1468_svelte
    description: |
      Extracted from documentation: Generated types
  - name: "Default tsconfig.json"
    id: concept:page_1469_svelte
    description: |
      Extracted from documentation: Default tsconfig.json
  - name: "$lib/server"
    id: concept:page_1470_svelte
    description: |
      Extracted from documentation: $lib/server
  - name: "app.d.ts"
    id: concept:page_1471_svelte
    description: |
      Extracted from documentation: app.d.ts
  - name: "Error"
    id: concept:page_1472_svelte
    description: |
      Extracted from documentation: Error
  - name: "Locals"
    id: concept:page_1473_svelte
    description: |
      Extracted from documentation: Locals
  - name: "PageData"
    id: concept:page_1474_svelte
    description: |
      Extracted from documentation: PageData
  - name: "PageState"
    id: concept:page_1475_svelte
    description: |
      Extracted from documentation: PageState
  - name: "Platform"
    id: concept:page_1476_svelte
    description: |
      Extracted from documentation: Platform
  - name: "Start of Svelte CLI documentation"
    id: concept:page_1477_svelte
    description: |
      Extracted from documentation: Start of Svelte CLI documentation
  - name: "Acknowledgements"
    id: concept:page_1478_svelte
    description: |
      Extracted from documentation: Acknowledgements
  - name: "How do I run the `sv` CLI?"
    id: concept:page_1479_svelte
    description: |
      Extracted from documentation: How do I run the `sv` CLI?
  - name: "`npx sv` is not working"
    id: concept:page_1480_svelte
    description: |
      Extracted from documentation: `npx sv` is not working
  - name: "sv create"
    id: concept:page_1481_svelte
    description: |
      Extracted from documentation: sv create
  - name: "`--from-playground <url>`"
    id: concept:page_1482_svelte
    description: |
      Extracted from documentation: `--from-playground <url>`
  - name: "`--template <name>`"
    id: concept:page_1483_svelte
    description: |
      Extracted from documentation: `--template <name>`
  - name: "`--types <option>`"
    id: concept:page_1484_svelte
    description: |
      Extracted from documentation: `--types <option>`
  - name: "`--no-types`"
    id: concept:page_1485_svelte
    description: |
      Extracted from documentation: `--no-types`
  - name: "`--add [add-ons...]`"
    id: concept:page_1486_svelte
    description: |
      Extracted from documentation: `--add [add-ons...]`
  - name: "`--no-add-ons`"
    id: concept:page_1487_svelte
    description: |
      Extracted from documentation: `--no-add-ons`
  - name: "`--install <package-manager>`"
    id: concept:page_1488_svelte
    description: |
      Extracted from documentation: `--install <package-manager>`
  - name: "`--no-install`"
    id: concept:page_1489_svelte
    description: |
      Extracted from documentation: `--no-install`
  - name: "`--no-dir-check`"
    id: concept:page_1490_svelte
    description: |
      Extracted from documentation: `--no-dir-check`
  - name: "sv add"
    id: concept:page_1491_svelte
    description: |
      Extracted from documentation: sv add
  - name: "`-C`, `--cwd`"
    id: concept:page_1492_svelte
    description: |
      Extracted from documentation: `-C`, `--cwd`
  - name: "`--no-git-check`"
    id: concept:page_1493_svelte
    description: |
      Extracted from documentation: `--no-git-check`
  - name: "`--no-download-check`"
    id: concept:page_1494_svelte
    description: |
      Extracted from documentation: `--no-download-check`
  - name: "Official add-ons"
    id: concept:page_1495_svelte
    description: |
      Extracted from documentation: Official add-ons
  - name: "Community add-ons"
    id: concept:page_1496_svelte
    description: |
      Extracted from documentation: Community add-ons
  - name: "Install a community add-on by org name (it will look at @org/sv)"
    id: concept:page_1497_svelte
    description: |
      Extracted from documentation: Install a community add-on by org name (it will look at @org/sv)
  - name: "Use a local add-on (for development or internal use)"
    id: concept:page_1498_svelte
    description: |
      Extracted from documentation: Use a local add-on (for development or internal use)
  - name: "Mix and match official and community add-ons"
    id: concept:page_1499_svelte
    description: |
      Extracted from documentation: Mix and match official and community add-ons
  - name: "Also works when creating a new project directly"
    id: concept:page_1500_svelte
    description: |
      Extracted from documentation: Also works when creating a new project directly
  - name: "sv check"
    id: concept:page_1501_svelte
    description: |
      Extracted from documentation: sv check
  - name: "Installation"
    id: concept:page_1502_svelte
    description: |
      Extracted from documentation: Installation
  - name: "`--workspace <path>`"
    id: concept:page_1503_svelte
    description: |
      Extracted from documentation: `--workspace <path>`
  - name: "`--output <format>`"
    id: concept:page_1504_svelte
    description: |
      Extracted from documentation: `--output <format>`
  - name: "`--watch`"
    id: concept:page_1505_svelte
    description: |
      Extracted from documentation: `--watch`
  - name: "`--preserveWatchOutput`"
    id: concept:page_1506_svelte
    description: |
      Extracted from documentation: `--preserveWatchOutput`
  - name: "`--tsconfig <path>`"
    id: concept:page_1507_svelte
    description: |
      Extracted from documentation: `--tsconfig <path>`
  - name: "`--no-tsconfig`"
    id: concept:page_1508_svelte
    description: |
      Extracted from documentation: `--no-tsconfig`
  - name: "`--ignore <paths>`"
    id: concept:page_1509_svelte
    description: |
      Extracted from documentation: `--ignore <paths>`
  - name: "`--fail-on-warnings`"
    id: concept:page_1510_svelte
    description: |
      Extracted from documentation: `--fail-on-warnings`
  - name: "`--compiler-warnings <warnings>`"
    id: concept:page_1511_svelte
    description: |
      Extracted from documentation: `--compiler-warnings <warnings>`
  - name: "`--diagnostic-sources <sources>`"
    id: concept:page_1512_svelte
    description: |
      Extracted from documentation: `--diagnostic-sources <sources>`
  - name: "`--threshold <level>`"
    id: concept:page_1513_svelte
    description: |
      Extracted from documentation: `--threshold <level>`
  - name: "Machine-readable output"
    id: concept:page_1514_svelte
    description: |
      Extracted from documentation: Machine-readable output
  - name: "Credits"
    id: concept:page_1515_svelte
    description: |
      Extracted from documentation: Credits
  - name: "FAQ"
    id: concept:page_1516_svelte
    description: |
      Extracted from documentation: FAQ
  - name: "Why is there no option to only check specific files (for example only staged files)?"
    id: concept:page_1517_svelte
    description: |
      Extracted from documentation: Why is there no option to only check specific files (for example only staged files)?
  - name: "sv migrate"
    id: concept:page_1518_svelte
    description: |
      Extracted from documentation: sv migrate
  - name: "Migrations"
    id: concept:page_1519_svelte
    description: |
      Extracted from documentation: Migrations
  - name: "`app-state`"
    id: concept:page_1520_svelte
    description: |
      Extracted from documentation: `app-state`
  - name: "`svelte-5`"
    id: concept:page_1521_svelte
    description: |
      Extracted from documentation: `svelte-5`
  - name: "`self-closing-tags`"
    id: concept:page_1522_svelte
    description: |
      Extracted from documentation: `self-closing-tags`
  - name: "`svelte-4`"
    id: concept:page_1523_svelte
    description: |
      Extracted from documentation: `svelte-4`
  - name: "`sveltekit-2`"
    id: concept:page_1524_svelte
    description: |
      Extracted from documentation: `sveltekit-2`
  - name: "`package`"
    id: concept:page_1525_svelte
    description: |
      Extracted from documentation: `package`
  - name: "`routes`"
    id: concept:page_1526_svelte
    description: |
      Extracted from documentation: `routes`
  - name: "better-auth"
    id: concept:page_1527_svelte
    description: |
      Extracted from documentation: better-auth
  - name: "What you get"
    id: concept:page_1528_svelte
    description: |
      Extracted from documentation: What you get
  - name: "demo"
    id: concept:page_1529_svelte
    description: |
      Extracted from documentation: demo
  - name: "Email & Password only (default)"
    id: concept:page_1530_svelte
    description: |
      Extracted from documentation: Email & Password only (default)
  - name: "GitHub OAuth only"
    id: concept:page_1531_svelte
    description: |
      Extracted from documentation: GitHub OAuth only
  - name: "Both Email & Password and GitHub OAuth"
    id: concept:page_1532_svelte
    description: |
      Extracted from documentation: Both Email & Password and GitHub OAuth
  - name: "drizzle"
    id: concept:page_1533_svelte
    description: |
      Extracted from documentation: drizzle
  - name: "database"
    id: concept:page_1534_svelte
    description: |
      Extracted from documentation: database
  - name: "client"
    id: concept:page_1535_svelte
    description: |
      Extracted from documentation: client
  - name: "docker"
    id: concept:page_1536_svelte
    description: |
      Extracted from documentation: docker
  - name: "eslint"
    id: concept:page_1537_svelte
    description: |
      Extracted from documentation: eslint
  - name: "versions"
    id: concept:page_1538_svelte
    description: |
      Extracted from documentation: versions
  - name: "features"
    id: concept:page_1539_svelte
    description: |
      Extracted from documentation: features
  - name: "mcp"
    id: concept:page_1540_svelte
    description: |
      Extracted from documentation: mcp
  - name: "ide"
    id: concept:page_1541_svelte
    description: |
      Extracted from documentation: ide
  - name: "setup"
    id: concept:page_1542_svelte
    description: |
      Extracted from documentation: setup
  - name: "mdsvex"
    id: concept:page_1543_svelte
    description: |
      Extracted from documentation: mdsvex
  - name: "paraglide"
    id: concept:page_1544_svelte
    description: |
      Extracted from documentation: paraglide
  - name: "languageTags"
    id: concept:page_1545_svelte
    description: |
      Extracted from documentation: languageTags
  - name: "playwright"
    id: concept:page_1546_svelte
    description: |
      Extracted from documentation: playwright
  - name: "prettier"
    id: concept:page_1547_svelte
    description: |
      Extracted from documentation: prettier
  - name: "storybook"
    id: concept:page_1548_svelte
    description: |
      Extracted from documentation: storybook
  - name: "sveltekit-adapter"
    id: concept:page_1549_svelte
    description: |
      Extracted from documentation: sveltekit-adapter
  - name: "cloudflare target"
    id: concept:page_1550_svelte
    description: |
      Extracted from documentation: cloudflare target
  - name: "tailwindcss"
    id: concept:page_1551_svelte
    description: |
      Extracted from documentation: tailwindcss
  - name: "plugins"
    id: concept:page_1552_svelte
    description: |
      Extracted from documentation: plugins
  - name: "vitest"
    id: concept:page_1553_svelte
    description: |
      Extracted from documentation: vitest
  - name: "usages"
    id: concept:page_1554_svelte
    description: |
      Extracted from documentation: usages
  - name: "[create your own]"
    id: concept:page_1555_svelte
    description: |
      Extracted from documentation: [create your own]
  - name: "Quick start"
    id: concept:page_1556_svelte
    description: |
      Extracted from documentation: Quick start
  - name: "Development"
    id: concept:page_1557_svelte
    description: |
      Extracted from documentation: Development
  - name: "Bundling"
    id: concept:page_1558_svelte
    description: |
      Extracted from documentation: Bundling
  - name: "`package.json`"
    id: concept:page_1559_svelte
    description: |
      Extracted from documentation: `package.json`
  - name: "Package names"
    id: concept:page_1560_svelte
    description: |
      Extracted from documentation: Package names
  - name: "✓ GOOD"
    id: concept:page_1561_svelte
    description: |
      Extracted from documentation: ✓ GOOD
  - name: "✗ BAD"
    id: concept:page_1562_svelte
    description: |
      Extracted from documentation: ✗ BAD
  - name: "Entry points"
    id: concept:page_1563_svelte
    description: |
      Extracted from documentation: Entry points
  - name: "Publish to npm"
    id: concept:page_1564_svelte
    description: |
      Extracted from documentation: Publish to npm
  - name: "Next steps"
    id: concept:page_1565_svelte
    description: |
      Extracted from documentation: Next steps
  - name: "Version compatibility"
    id: concept:page_1566_svelte
    description: |
      Extracted from documentation: Version compatibility
  - name: "sv"
    id: concept:page_1567_svelte
    description: |
      Extracted from documentation: sv
  - name: "`defineAddon`"
    id: concept:page_1568_svelte
    description: |
      Extracted from documentation: `defineAddon`
  - name: "`defineAddonOptions`"
    id: concept:page_1569_svelte
    description: |
      Extracted from documentation: `defineAddonOptions`
  - name: "`create`"
    id: concept:page_1570_svelte
    description: |
      Extracted from documentation: `create`
  - name: "`add`"
    id: concept:page_1571_svelte
    description: |
      Extracted from documentation: `add`
  - name: "sv-utils"
    id: concept:page_1572_svelte
    description: |
      Extracted from documentation: sv-utils
  - name: "transforms"
    id: concept:page_1573_svelte
    description: |
      Extracted from documentation: transforms
  - name: "`transforms.script`"
    id: concept:page_1574_svelte
    description: |
      Extracted from documentation: `transforms.script`
  - name: "`transforms.svelte`"
    id: concept:page_1575_svelte
    description: |
      Extracted from documentation: `transforms.svelte`
  - name: "`transforms.svelteScript`"
    id: concept:page_1576_svelte
    description: |
      Extracted from documentation: `transforms.svelteScript`
  - name: "`transforms.css`"
    id: concept:page_1577_svelte
    description: |
      Extracted from documentation: `transforms.css`
  - name: "`transforms.json`"
    id: concept:page_1578_svelte
    description: |
      Extracted from documentation: `transforms.json`
  - name: "`transforms.yaml` / `transforms.toml`"
    id: concept:page_1579_svelte
    description: |
      Extracted from documentation: `transforms.yaml` / `transforms.toml`
  - name: "`transforms.text`"
    id: concept:page_1580_svelte
    description: |
      Extracted from documentation: `transforms.text`
  - name: "Aborting a transform"
    id: concept:page_1581_svelte
    description: |
      Extracted from documentation: Aborting a transform
  - name: "Standalone usage & testing"
    id: concept:page_1582_svelte
    description: |
      Extracted from documentation: Standalone usage & testing
  - name: "Composability"
    id: concept:page_1583_svelte
    description: |
      Extracted from documentation: Composability
  - name: "Parsers (low-level)"
    id: concept:page_1584_svelte
    description: |
      Extracted from documentation: Parsers (low-level)
  - name: "Language tooling"
    id: concept:page_1585_svelte
    description: |
      Extracted from documentation: Language tooling
  - name: "Svelte config"
    id: concept:page_1586_svelte
    description: |
      Extracted from documentation: Svelte config
  - name: "`svelteConfig.edit`"
    id: concept:page_1587_svelte
    description: |
      Extracted from documentation: `svelteConfig.edit`
  - name: "`svelteConfig.find` / `svelteConfig.read`"
    id: concept:page_1588_svelte
    description: |
      Extracted from documentation: `svelteConfig.find` / `svelteConfig.read`
  - name: "Package manager helpers"
    id: concept:page_1589_svelte
    description: |
      Extracted from documentation: Package manager helpers
  - name: "`pnpm.allowBuilds`"
    id: concept:page_1590_svelte
    description: |
      Extracted from documentation: `pnpm.allowBuilds`
  - name: "Start of Svelte AI documentation"
    id: concept:page_1591_svelte
    description: |
      Extracted from documentation: Start of Svelte AI documentation
  - name: "AGENTS.md"
    id: concept:page_1592_svelte
    description: |
      Extracted from documentation: AGENTS.md
  - name: "Available Svelte MCP Tools:"
    id: concept:page_1593_svelte
    description: |
      Extracted from documentation: Available Svelte MCP Tools:
  - name: "1. list-sections"
    id: concept:page_1594_svelte
    description: |
      Extracted from documentation: 1. list-sections
  - name: "2. get-documentation"
    id: concept:page_1595_svelte
    description: |
      Extracted from documentation: 2. get-documentation
  - name: "3. svelte-autofixer"
    id: concept:page_1596_svelte
    description: |
      Extracted from documentation: 3. svelte-autofixer
  - name: "4. playground-link"
    id: concept:page_1597_svelte
    description: |
      Extracted from documentation: 4. playground-link
  - name: "Local setup"
    id: concept:page_1598_svelte
    description: |
      Extracted from documentation: Local setup
  - name: "Claude Code"
    id: concept:page_1599_svelte
    description: |
      Extracted from documentation: Claude Code
  - name: "Claude Desktop"
    id: concept:page_1600_svelte
    description: |
      Extracted from documentation: Claude Desktop
  - name: "Codex CLI"
    id: concept:page_1601_svelte
    description: |
      Extracted from documentation: Codex CLI
  - name: "Copilot CLI"
    id: concept:page_1602_svelte
    description: |
      Extracted from documentation: Copilot CLI
  - name: "Antigravity CLI"
    id: concept:page_1603_svelte
    description: |
      Extracted from documentation: Antigravity CLI
  - name: "OpenCode"
    id: concept:page_1604_svelte
    description: |
      Extracted from documentation: OpenCode
  - name: "VS Code"
    id: concept:page_1605_svelte
    description: |
      Extracted from documentation: VS Code
  - name: "Cursor"
    id: concept:page_1606_svelte
    description: |
      Extracted from documentation: Cursor
  - name: "Zed"
    id: concept:page_1607_svelte
    description: |
      Extracted from documentation: Zed
  - name: "Other clients"
    id: concept:page_1608_svelte
    description: |
      Extracted from documentation: Other clients
  - name: "Remote setup"
    id: concept:page_1609_svelte
    description: |
      Extracted from documentation: Remote setup
  - name: "GitHub Coding Agent"
    id: concept:page_1610_svelte
    description: |
      Extracted from documentation: GitHub Coding Agent
  - name: "Tools"
    id: concept:page_1611_svelte
    description: |
      Extracted from documentation: Tools
  - name: "list-sections"
    id: concept:page_1612_svelte
    description: |
      Extracted from documentation: list-sections
  - name: "get-documentation"
    id: concept:page_1613_svelte
    description: |
      Extracted from documentation: get-documentation
  - name: "svelte-autofixer"
    id: concept:page_1614_svelte
    description: |
      Extracted from documentation: svelte-autofixer
  - name: "playground-link"
    id: concept:page_1615_svelte
    description: |
      Extracted from documentation: playground-link
  - name: "Resources"
    id: concept:page_1616_svelte
    description: |
      Extracted from documentation: Resources
  - name: "doc-section"
    id: concept:page_1617_svelte
    description: |
      Extracted from documentation: doc-section
  - name: "Prompts"
    id: concept:page_1618_svelte
    description: |
      Extracted from documentation: Prompts
  - name: "svelte-task"
    id: concept:page_1619_svelte
    description: |
      Extracted from documentation: svelte-task
  - name: "CLI"
    id: concept:page_1620_svelte
    description: |
      Extracted from documentation: CLI
  - name: "`list-sections`"
    id: concept:page_1621_svelte
    description: |
      Extracted from documentation: `list-sections`
  - name: "`get-documentation`"
    id: concept:page_1622_svelte
    description: |
      Extracted from documentation: `get-documentation`
  - name: "or"
    id: concept:page_1623_svelte
    description: |
      Extracted from documentation: or
  - name: "`svelte-autofixer`"
    id: concept:page_1624_svelte
    description: |
      Extracted from documentation: `svelte-autofixer`
  - name: "`svelte-code-writer`"
    id: concept:page_1625_svelte
    description: |
      Extracted from documentation: `svelte-code-writer`
  - name: "Svelte 5 Code Writer"
    id: concept:page_1626_svelte
    description: |
      Extracted from documentation: Svelte 5 Code Writer
  - name: "CLI Tools"
    id: concept:page_1627_svelte
    description: |
      Extracted from documentation: CLI Tools
  - name: "List Documentation Sections"
    id: concept:page_1628_svelte
    description: |
      Extracted from documentation: List Documentation Sections
  - name: "Get Documentation"
    id: concept:page_1629_svelte
    description: |
      Extracted from documentation: Get Documentation
  - name: "Svelte Autofixer"
    id: concept:page_1630_svelte
    description: |
      Extracted from documentation: Svelte Autofixer
  - name: "Analyze inline code (escape $ as \\$)"
    id: concept:page_1631_svelte
    description: |
      Extracted from documentation: Analyze inline code (escape $ as \$)
  - name: "Analyze a file"
    id: concept:page_1632_svelte
    description: |
      Extracted from documentation: Analyze a file
  - name: "Target Svelte 4"
    id: concept:page_1633_svelte
    description: |
      Extracted from documentation: Target Svelte 4
  - name: "Workflow"
    id: concept:page_1634_svelte
    description: |
      Extracted from documentation: Workflow
  - name: "`svelte-core-bestpractices`"
    id: concept:page_1635_svelte
    description: |
      Extracted from documentation: `svelte-core-bestpractices`
  - name: "Available MCP Tools"
    id: concept:page_1636_svelte
    description: |
      Extracted from documentation: Available MCP Tools
  - name: "1. Gather Context (if needed)"
    id: concept:page_1637_svelte
    description: |
      Extracted from documentation: 1. Gather Context (if needed)
  - name: "2. Read the Target File"
    id: concept:page_1638_svelte
    description: |
      Extracted from documentation: 2. Read the Target File
  - name: "3. Make Changes"
    id: concept:page_1639_svelte
    description: |
      Extracted from documentation: 3. Make Changes
  - name: "4. Validate Changes"
    id: concept:page_1640_svelte
    description: |
      Extracted from documentation: 4. Validate Changes
  - name: "5. Fix Any Issues"
    id: concept:page_1641_svelte
    description: |
      Extracted from documentation: 5. Fix Any Issues
  - name: "Output Format"
    id: concept:page_1642_svelte
    description: |
      Extracted from documentation: Output Format
  - name: "GitHub Copilot CLI"
    id: concept:page_1643_svelte
    description: |
      Extracted from documentation: GitHub Copilot CLI
  - name: "web development for the rest of us"
    id: concept:page_1644_svelte
    description: |
      Extracted from documentation: web development for the rest of us
  - name: "used by companies you’ve heard of"
    id: concept:page_1645_svelte
    description: |
      Extracted from documentation: used by companies you’ve heard of
  - name: "join our friendly community"
    id: concept:page_1646_svelte
    description: |
      Extracted from documentation: join our friendly community
  - name: "Runes"
    id: concept:page_1647_svelte
    description: |
      Extracted from documentation: Runes
  - name: "Template syntax"
    id: concept:page_1648_svelte
    description: |
      Extracted from documentation: Template syntax
  - name: "Special elements"
    id: concept:page_1649_svelte
    description: |
      Extracted from documentation: Special elements
  - name: "Runtime"
    id: concept:page_1650_svelte
    description: |
      Extracted from documentation: Runtime
  - name: "Misc"
    id: concept:page_1651_svelte
    description: |
      Extracted from documentation: Misc
  - name: "Reference"
    id: concept:page_1652_svelte
    description: |
      Extracted from documentation: Reference
  - name: "Legacy APIs"
    id: concept:page_1653_svelte
    description: |
      Extracted from documentation: Legacy APIs
  - name: "On this page"
    id: concept:page_1654_svelte
    description: |
      Extracted from documentation: On this page
apis:
  - name: "<script>"
    id: api:crawl_0_svelte
    signature: |
      <script>
      	function greet() {
      		alert('Welcome to Svelte!');
      	}
      </script>
      
      <button onclick={greet}>click me</button>
      
      <style>
      	button {
      		font-size: 2em;
      	}
      </style>
    returns: See documentation
    description: |
      Extracted code example from documentation.
  - name: "<script lang=\"ts\">"
    id: api:crawl_1_svelte
    signature: |
      <script lang="ts">
      	function greet() {
      		alert('Welcome to Svelte!');
      	}
      </script>
      
      <button onclick={greet}>click me</button>
      
      <style>
      	button {
      		font-size: 2em;
      	}
      </style>
    returns: See documentation
    description: |
      Extracted code example from documentation.
failures:

---


_Content truncated at 400KB._


# svelte

Auto-generated knowledge package crawled from https://svelte.dev/docs.

**Pages crawled**: 5
**Source**: https://svelte.dev/docs

# Docs • Svelte

Head to the Svelte or SvelteKit reference docs,
		or choose your adventure:

We recommend starting with the interactive tutorial, which will teach you how to use Svelte
				right here in your browser.

If you’re already experienced with an older version of Svelte, the migration guide
				will bring you up to speed on the changes in Svelte 5.

Head over to the playground to see examples, create your own Svelte apps in the browser, and
				share them with other people.

If you’re an artificial intelligence, or trying to teach one how to use Svelte, we
				offer the documentation in plaintext format. Beep boop.

If you’re developing a site using only Svelte 3 or Svelte 4, the old site may be a
				helpful reference until you upgrade.

Join our Discord server where you can hang out with fellow Svelte users and ask them
				questions. It’s like an LLM but with people.

## Documentation

## I’m brand new here

## I’m migrating an app from Svelte 4

## I just want to try it out

## I’m a Large Language Model (LLM)

## I’m looking for the old docs

## Help! I’m stuck

## Svelte Documentation for LLMs

> Svelte is a UI framework that uses a compiler to let you write breathtakingly concise components that do minimal work in the browser, using languages you already know — HTML, CSS and JavaScript.


## Documentation Sets

- [Abridged documentation](https://svelte.dev/llms-medium.txt): A shorter version of the Svelte and SvelteKit documentation, with examples and non-essential content removed
- [Compressed documentation](https://svelte.dev/llms-small.txt): A minimal version of the Svelte and SvelteKit documentation, with many examples and non-essential content removed
- [Complete documentation](https://svelte.dev/llms-full.txt): The complete Svelte and SvelteKit documentation including all examples and additional content


## Individual Package Documentation

- [Svelte documentation](https://svelte.dev/docs/svelte/llms.txt): This is the developer documentation for Svelte.
- [SvelteKit documentation](https://svelte.dev/docs/kit/llms.txt): This is the developer documentation for SvelteKit.
- [Svelte CLI documentation](https://svelte.dev/docs/cli/llms.txt): This is the developer documentation for Svelte CLI.
- [Svelte AI documentation](https://svelte.dev/docs/ai/llms.txt): This is the developer documentation for Svelte AI.


## Notes

- The abridged and compressed documentation excludes legacy compatibility notes, detailed examples, and supplementary information
- The complete documentation includes all content from the official documentation
- Package-specific documentation files contain only the content relevant to that package
- The content is automatically generated from the same source as the official documentation

## Start of Svelte documentation



## Overview

Svelte is a framework for building user interfaces on the web. It uses a compiler to turn declarative components written in HTML, CSS and JavaScript...

```svelte
<!--- file: App.svelte --->
<script>
	function greet() {
		alert('Welcome to Svelte!');
	}
</script>

<button onclick={greet}>click me</button>

<style>
	button {
		font-size: 2em;
	}
</style>
```

...into lean, tightly optimized JavaScript.

You can use it to build anything on the web, from standalone components to ambitious full stack apps (using Svelte's companion application framework, [SvelteKit](../kit)) and everything in between.

These pages serve as reference documentation. If you're new to Svelte, we recommend starting with the [interactive tutorial](/tutorial) and coming back here when you have questions.

You can also try Svelte online in the playground.


## Getting started

We recommend using [SvelteKit](../kit), which lets you [build almost anything](../kit/project-types). It's the official application framework from the Svelte team and powered by [Vite](https://vite.dev/). Create a new project with:

```sh
npx sv create myapp
cd myapp
npm install
npm run dev
```

Don't worry if you don't know Svelte yet! You can ignore all the nice features SvelteKit brings on top for now and dive into it later.


## Alternatives to SvelteKit

You can also use Svelte directly with Vite via [vite-plugin-svelte](https://github.com/sveltejs/vite-plugin-svelte) by running `npm create vite@latest` and selecting the `svelte` option (or, if working with an existing project, adding the plugin to your `vite.config.js` file). With this, `npm run build` will generate HTML, JS, and CSS files inside the `dist` directory. In most cases, you will probably need to [choose a routing library](/packages#routing) as well.

>[!NOTE] Vite is often used in standalone mode to build [single page apps (SPAs)](../kit/glossary#SPA), which you can also [build with SvelteKit](../kit/single-page-apps).

There are also [plugins for other bundlers](/packages#bundler-plugins), but we recommend Vite.


## Editor tooling

The Svelte team maintains a [VS Code extension](https://marketplace.visualstudio.com/items?itemName=svelte.svelte-vscode), and there are integrations with various other [editors](https://sveltesociety.dev/collection/editor-support-c85c080efc292a34) and tools as well.

You can also check your code from the command line using [`npx sv check`](https://svelte.dev/docs/cli/sv-check).



## Getting help

Don't be shy about asking for help in the [Discord chatroom](/chat)! You can also find answers on [Stack Overflow](https://stackoverflow.com/questions/tagged/svelte).


## .svelte files

Components are the building blocks of Svelte applications. They are written into `.svelte` files, using a superset of HTML.

All three sections — script, styles and markup — are optional.

<!-- prettier-ignore -->
```svelte
/// file: MyComponent.svelte
<script module>
	// module-level logic goes here
	// (you will rarely use this)
</script>

<script>
	// instance-level logic goes here
</script>

<!-- markup (zero or more items) goes here -->

<style>
	/* styles go here */
</style>
```


## `<script>`

A `<script>` block contains JavaScript (or TypeScript, when adding the `lang="ts"` attribute) that runs when a component instance is created. Variables declared (or imported) at the top level can be referenced in the component's markup.

In addition to normal JavaScript, you can use _runes_ to declare [component props]($props) and add reactivity to your component. Runes are covered in the next section.

<!-- TODO describe behaviour of `export` -->


## `<script module>`

A `<script>` tag with a `module` attribute runs once when the module first evaluates, rather than for each component instance. Variables declared in this block can be referenced elsewhere in the component, but not vice versa.

```svelte
<script module>
	let total = 0;
</script>

<script>
	total += 1;
	console.log(`instantiated ${total} times`);
</script>
```

You can `export` bindings from this block, and they will become exports of the compiled module. You cannot `export default`, since the default export is the component itself.

> [!NOTE] If you are using TypeScript and import such exports from a `module` block into a `.ts` file, make sure to have your editor setup so that TypeScript knows about them. This is the case for our VS Code extension and the IntelliJ plugin, but in other cases you might need to setup our [TypeScript editor plugin](https://www.npmjs.com/package/typescript-svelte-plugin).

> [!LEGACY]
> In Svelte 4, this script tag was created using `<script context="module">`


## `<style>`

CSS inside a `<style>` block will be scoped to that component.

```svelte
<style>
	p {
		/* this will only affect <p> elements in this component */
		color: burlywood;
	}
</style>
```

For more information, head to the section on [styling](scoped-styles).


## .svelte.js and .svelte.ts files

Besides `.svelte` files, Svelte also operates on `.svelte.js` and `.svelte.ts` files.

These behave like any other `.js` or `.ts` module, except that you can use runes. This is useful for creating reusable reactive logic, or sharing reactive state across your app (though note that you [cannot export reassigned state]($state#Passing-state-across-modules)).

> [!LEGACY]
> This is a concept that didn't exist prior to Svelte 5


## What are runes?

> [!NOTE] **rune** /ruːn/ _noun_
>
> A letter or mark used as a mystical or magic symbol.

Runes are symbols that you use in `.svelte` and `.svelte.js`/`.svelte.ts` files to control the Svelte compiler. If you think of Svelte as a language, runes are part of the syntax — they are _keywords_.

Runes have a `$` prefix and look like functions:

```js
let message = $state('hello');
```

They differ from normal JavaScript functions in important ways, however:

- You don't need to import them — they are part of the language
- They're not values — you can't assign them to a variable or pass them as arguments to a function
- Just like JavaScript keywords, they are only valid in certain positions (the compiler will help you if you put them in the wrong place)

> [!LEGACY]
> Runes didn't exist prior to Svelte 5.


## $state

The `$state` rune allows you to create _reactive state_, which means that your UI _reacts_ when it changes.

```svelte
<script>
	let count = $state(0);
</script>

<button onclick={() => count++}>
	clicks: {count}
</button>
```

Unlike other frameworks you may have encountered, there is no API for interacting with state — `count` is just a number, rather than an object or a function, and you can update it like you would update any other variable.


## Deep state

If `$state` is used with an array or a simple object, the result is a deeply reactive _state proxy_. [Proxies](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Proxy) allow Svelte to run code when you read or write properties, including via methods like `array.push(...)`, triggering granular updates.

State is proxified recursively until Svelte finds something other than an array or simple object (like a class or an object created with `Object.create`). In a case like this...

```js
let todos = $state([
	{
		done: false,
		text: 'add more todos'
	}
]);
```

...modifying an individual todo's property will trigger updates to anything in your UI that depends on that specific property:

```js
let todos = [{ done: false, text: 'add more todos' }];
// ---cut---
todos[0].done = !todos[0].done;
```

If you push a new object to the array, it will also be proxified:

```js
let todos = [{ done: false, text: 'add more todos' }];
// ---cut---
todos.push({
	done: false,
	text: 'eat lunch'
});
```

> [!NOTE] When you update properties of proxies, the original object is _not_ mutated. If you need to use your own proxy handlers in a state proxy, you should wrap the object _after_ wrapping it in `$state`.

Note that if you destructure a reactive value, the references are not reactive — as in normal JavaScript, they are evaluated at the point of destructuring:

```js
let todos = [{ done: false, text: 'add more todos' }];
// ---cut---
let { done, text } = todos[0];

// this will not affect the value of `done`
todos[0].done = !todos[0].done;
```


## Classes

Class instances are not proxied. Instead, you can use `$state` in class fields (whether public or private), or as the first assignment to a property immediately inside the `constructor`:

```js
// @errors: 7006 2554
class Todo {
	done = $state(false);

	constructor(text) {
		this.text = $state(text);
	}

	reset() {
		this.text = '';
		this.done = false;
	}
}
```

> [!NOTE] The compiler transforms `done` and `text` into `get`/`set` methods on the class prototype referencing private fields. This means the properties are not enumerable.

When calling methods in JavaScript, the value of [`this`](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/this) matters. This won't work, because `this` inside the `reset` method will be the `<button>` rather than the `Todo`:

```svelte
<button onclick={todo.reset}>
	reset
</button>
```

You can either use an inline function...

```svelte
<button onclick=+++{() => todo.reset()}>+++
	reset
</button>
```

...or use an arrow function in the class definition:

```js
// @errors: 7006 2554
class Todo {
	done = $state(false);

	constructor(text) {
		this.text = $state(text);
	}

	+++reset = () => {+++
		this.text = '';
		this.done = false;
	}
}
```


## Built-in classes

Svelte provides reactive implementations of built-in classes like `Set`, `Map`, `Date` and `URL` that can be imported from [`svelte/reactivity`](svelte-reactivity).


## `$state.raw`

In cases where you don't want objects and arrays to be deeply reactive you can use `$state.raw`.

State declared with `$state.raw` cannot be mutated; it can only be _reassigned_. In other words, rather than assigning to a property of an object, or using an array method like `push`, replace the object or array altogether if you'd like to update it:

```js
let person = $state.raw({
	name: 'Heraclitus',
	age: 49
});

// this will have no effect
person.age += 1;

// this will work, because we're creating a new person
person = {
	name: 'Heraclitus',
	age: 50
};
```

This can improve performance with large arrays and objects that you weren't planning to mutate anyway, since it avoids the cost of making them reactive. Note that raw state can _contain_ reactive state (for example, a raw array of reactive objects).

As with `$state`, you can declare class fields using `$state.raw`.


## `$state.snapshot`

To take a static snapshot of a deeply reactive `$state` proxy, use `$state.snapshot`:

```svelte
<script>
	let counter = $state({ count: 0 });

	function onclick() {
		// Will log `{ count: ... }` rather than `Proxy { ... }`
		console.log($state.snapshot(counter));
	}
</script>
```

This is handy when you want to pass some state to an external library or API that doesn't expect a proxy, such as `structuredClone`.

If a value has a `toJSON` method, the snapshot will clone the value returned from `toJSON` instead of the original object.


## `$state.eager`

When state changes, it may not be reflected in the UI immediately if it is used by an `await` expression, because [updates are synchronized](await-expressions#Synchronized-updates).

In some cases, you may want to update the UI as soon as the state changes. For example, you might want to update a navigation bar when the user clicks on a link, so that they get visual feedback while waiting for the new page to load. To do this, use `$state.eager(value)`:

```svelte
<nav>
	<a href="/" aria-current={$state.eager(pathname) === '/' ? 'page' : null}>home</a>
	<a href="/about" aria-current={$state.eager(pathname) === '/about' ? 'page' : null}>about</a>
</nav>
```

Use this feature sparingly, and only to provide feedback in response to user action — in general, allowing Svelte to coordinate updates will provide a better user experience.


## Passing state into functions

JavaScript is a _pass-by-value_ language — when you call a function, the arguments are the _values_ rather than the _variables_. In other words:

```js
/// file: index.js
// @filename: index.js
// ---cut---
/**
 * @param {number} a
 * @param {number} b
 */
function add(a, b) {
	return a + b;
}

let a = 1;
let b = 2;
let total = add(a, b);
console.log(total); // 3

a = 3;
b = 4;
console.log(total); // still 3!
```

If `add` wanted to have access to the _current_ values of `a` and `b`, and to return the current `total` value, you would need to use functions instead:

```js
/// file: index.js
// @filename: index.js
// ---cut---
/**
 * @param {() => number} getA
 * @param {() => number} getB
 */
function add(+++getA, getB+++) {
	return +++() => getA() + getB()+++;
}

let a = 1;
let b = 2;
let total = add+++(() => a, () => b)+++;
console.log(+++total()+++); // 3

a = 3;
b = 4;
console.log(+++total()+++); // 7
```

State in Svelte is no different — when you reference something declared with the `$state` rune...

```js
let a = +++$state(1)+++;
let b = +++$state(2)+++;
```

...you're accessing its _current value_.

Note that 'functions' is broad — it encompasses properties of proxies and [`get`](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Functions/get)/[`set`](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Functions/set) properties...

```js
/// file: index.js
// @filename: index.js
// ---cut---
/**
 * @param {{ a: number, b: number }} input
 */
function add(input) {
	return {
		get value() {
			return input.a + input.b;
		}
	};
}

let input = $state({ a: 1, b: 2 });
let total = add(input);
console.log(total.value); // 3

input.a = 3;
input.b = 4;
console.log(total.value); // 7
```

...though if you find yourself writing code like that, consider using [classes](#Classes) instead.


## Passing state across modules

You can declare state in `.svelte.js` and `.svelte.ts` files, but you can only _export_ that state if it's not directly reassigned. In other words you can't do this:

```js
/// file: state.svelte.js
export let count = $state(0);

export function increment() {
	count += 1;
}
```

That's because every reference to `count` is transformed by the Svelte compiler — the code above is roughly equivalent to this:

```js
/// file: state.svelte.js (compiler output)
// @filename: index.ts
interface Signal<T> {
	value: T;
}

interface Svelte {
	state<T>(value?: T): Signal<T>;
	get<T>(source: Signal<T>): T;
	set<T>(source: Signal<T>, value: T): void;
}
declare const $: Svelte;
// ---cut---
export let count = $.state(0);

export function increment() {
	$.set(count, $.get(count) + 1);
}
```

> [!NOTE] You can see the code Svelte generates by clicking the 'JS Output' tab in the [playground](/playground).

Since the compiler only operates on one file at a time, if another file imports `count` Svelte doesn't know that it needs to wrap each reference in `$.get` and `$.set`:

```js
// @filename: state.svelte.js
export let count = 0;

// @filename: index.js
// ---cut---
import { count } from './state.svelte.js';

console.log(typeof count); // 'object', not 'number'
```

This leaves you with two options for sharing state between modules — either don't reassign it...

```js
// This is allowed — since we're updating
// `counter.count` rather than `counter`,
// Svelte doesn't wrap it in `$.state`
export const counter = $state({
	count: 0
});

export function increment() {
	counter.count += 1;
}
```

...or don't directly export it:

```js
let count = $state(0);

export function getCount() {
	return count;
}

export function increment() {
	count += 1;
}
```


## $derived

Derived state is declared with the `$derived` rune:

```svelte
<script>
	let count = $state(0);
	let doubled = $derived(count * 2);
</script>

<button onclick={() => count++}>
	{doubled}
</button>

<p>{count} doubled is {doubled}</p>
```

The expression inside `$derived(...)` should be free of side-effects. Svelte will disallow state changes (e.g. `count++`) inside derived expressions.

As with `$state`, you can mark class fields as `$derived`.

> [!NOTE] Code in Svelte components is only executed once at creation. Without the `$derived` rune, `doubled` would maintain its original value even when `count` changes.


## `$derived.by`

Sometimes you need to create complex derivations that don't fit inside a short expression. In these cases, you can use `$derived.by` which accepts a function as its argument.

```svelte
<script>
	let numbers = $state([1, 2, 3]);
	let total = $derived.by(() => {
		let total = 0;
		for (const n of numbers) {
			total += n;
		}
		return total;
	});
</script>

<button onclick={() => numbers.push(numbers.length + 1)}>
	{numbers.join(' + ')} = {total}
</button>
```

In essence, `$derived(expression)` is equivalent to `$derived.by(() => expression)`.


## Understanding dependencies

Anything read synchronously inside the `$derived` expression (or `$derived.by` function body) is considered a _dependency_ of the derived state. When the state changes, the derived will be marked as _dirty_ and recalculated when it is next read.

In addition, if an expression contains an [`await`](await-expressions), Svelte transforms it such that any state _after_ the `await` is also tracked — in other words, in a case like this...

```js
let a = Promise.resolve(1);
let b = 2;
// ---cut---
let total = $derived(await a + b);
```

...both `a` and `b` are tracked, even though `b` is only read once `a` has resolved, after the initial execution. (This does not apply to `await` in functions that are called by the expression, only the expression itself.)

To exempt a piece of state from being treated as a dependency, use [`untrack`](svelte#untrack).


## Overriding derived values

Derived expressions are recalculated when their dependencies change, but you can temporarily override their values by reassigning them (unless they are declared with `const`). This can be useful for things like _optimistic UI_, where a value is derived from the 'source of truth' (such as data from your server) but you'd like to show immediate feedback to the user:

```svelte
<script>
	let { post, like } = $props();

	let likes = $derived(post.likes);

	async function onclick() {
		// increment the `likes` count immediately...
		likes += 1;

		// and tell the server, which will eventually update `post`
		try {
			await like();
		} catch {
			// failed! roll back the change
			likes -= 1;
		}
	}
</script>

<button {onclick}>🧡 {likes}</button>
```

> [!NOTE] Prior to Svelte 5.25, deriveds were read-only.


## Deriveds and reactivity

Unlike `$state`, which converts objects and arrays to [deeply reactive proxies]($state#Deep-state), `$derived` values are left as-is. For example, in a case like this...

```js
// @errors: 7005
let items = $state([ /*...*/ ]);

let index = $state(0);
let selected = $derived(items[index]);
```

...you can change (or `bind:` to) properties of `selected` and it will affect the underlying `items` array. If `items` was _not_ deeply reactive, mutating `selected` would have no effect.


## Destructuring

If you use destructuring with a `$derived` declaration, the resulting variables will all be reactive — this...

```js
function stuff() { return { a: 1, b: 2, c: 3 } }
// ---cut---
let { a, b, c } = $derived(stuff());
```

...is roughly equivalent to this:

```js
function stuff() { return { a: 1, b: 2, c: 3 } }
// ---cut---
let _stuff = $derived(stuff());
let a = $derived(_stuff.a);
let b = $derived(_stuff.b);
let c = $derived(_stuff.c);
```


## Update propagation

Svelte uses something called _push-pull reactivity_ — when state is updated, everything that depends on the state (whether directly or indirectly) is immediately notified of the change (the 'push'), but derived values are not re-evaluated until they are actually read (the 'pull').

If the new value of a derived is referentially identical to its previous value, downstream updates will be skipped. In other words, Svelte will only update the text inside the button when `large` changes, not when `count` changes, even though `large` depends on `count`:

```svelte
<script>
	let count = $state(0);
	let large = $derived(count > 10);
</script>

<button onclick={() => count++}>
	{large}
</button>
```


## $effect

Effects are functions that run when state updates, and can be used for things like calling third-party libraries, drawing on `<canvas>` elements, or making network requests. They only run in the browser, not during server-side rendering.

Generally speaking, you should _not_ update state inside effects, as it will make code more convoluted and will often lead to never-ending update cycles. If you find yourself doing so, see [when not to use `$effect`](#When-not-to-use-$effect) to learn about alternative approaches.

You can create an effect with the `$effect` rune (demo:

```svelte
<script>
	let size = $state(50);
	let color = $state('#ff3e00');

	let canvas;

	$effect(() => {
		const context = canvas.getContext('2d');
		context.clearRect(0, 0, canvas.width, canvas.height);

		// this will re-run whenever `color` or `size` change
		context.fillStyle = color;
		context.fillRect(0, 0, size, size);
	});
</script>

<canvas bind:this={canvas} width="100" height="100"></canvas>
```

When Svelte runs an effect function, it tracks which pieces of state (and derived state) are accessed (unless accessed inside [`untrack`](svelte#untrack)), and re-runs the function when that state later changes.

> [!NOTE] If you're having difficulty understanding why your `$effect` is rerunning or is not running see [understanding dependencies](#Understanding-dependencies). Effects are triggered differently than the `$:` blocks you may be used to if coming from Svelte 4.


## Understanding lifecycle

Your effects run after the component has been mounted to the DOM, and in a [microtask](https://developer.mozilla.org/en-US/docs/Web/API/HTML_DOM_API/Microtask_guide) after state changes. Re-runs are batched (i.e. changing `color` and `size` in the same moment won't cause two separate runs), and happen after any DOM updates have been applied.

You can use `$effect` anywhere, not just at the top level of a component, as long as it is called while a parent effect is running.

> [!NOTE] Svelte uses effects internally to represent logic and expressions in your template — this is how `<h1>hello {name}!</h1>` updates when `name` changes.

An effect can return a _teardown function_ which will run immediately before the effect re-runs:

<!-- codeblock:start {"title":"Effect teardown"} -->
```svelte
<!--- file: App.svelte --->
<script>
	let count = $state(0);
	let milliseconds = $state(1000);

	$effect(() => {
		// This will be recreated whenever `milliseconds` changes
		const interval = setInterval(() => {
			count += 1;
		}, milliseconds);

		return () => {
			// if a teardown function is provided, it will run
			// a) immediately before the effect re-runs
			// b) when the component is destroyed
			clearInterval(interval);
		};
	});
</script>

<h1>{count}</h1>

<button onclick={() => (milliseconds *= 2)}>slower</button>
<button onclick={() => (milliseconds /= 2)}>faster</button>
```
<!-- codeblock:end -->

Teardown functions also run when the effect is destroyed, which happens when its parent is destroyed (for example, a component is unmounted) or the parent effect re-runs.


## Understanding dependencies

`$effect` automatically picks up any reactive values (`$state`, `$derived`, `$props`) that are _synchronously_ read inside its function body (including indirectly, via function calls) and registers them as dependencies. When those dependencies change, the `$effect` schedules a re-run.

If `$state` and `$derived` are used directly inside the `$effect` (for example, during creation of a [reactive class](https://svelte.dev/docs/svelte/$state#Classes)), those values will _not_ be treated as dependencies.

Values that are read _asynchronously_ — after an `await` or inside a `setTimeout`, for example — will not be tracked. Here, the canvas will be repainted when `color` changes, but not when `size` changes (demo:

```ts
// @filename: index.ts
declare let canvas: {
	width: number;
	height: number;
	getContext(type: '2d', options?: CanvasRenderingContext2DSettings): CanvasRenderingContext2D;
};
declare let color: string;
declare let size: number;

// ---cut---
$effect(() => {
	const context = canvas.getContext('2d');
	context.clearRect(0, 0, canvas.width, canvas.height);

	// this will re-run whenever `color` changes...
	context.fillStyle = color;

	setTimeout(() => {
		// ...but not when `size` changes
		context.fillRect(0, 0, size, size);
	}, 0);
});
```

An effect only reruns when the object it reads changes, not when a property inside it changes. (If you want to observe changes _inside_ an object at dev time, you can use [`$inspect`]($inspect).)

```svelte
<script>
	let state = $state({ value: 0 });
	let derived = $derived({ value: state.value * 2 });

	// this will run once, because `state` is never reassigned (only mutated)
	$effect(() => {
		state;
	});

	// this will run whenever `state.value` changes...
	$effect(() => {
		state.value;
	});

	// ...and so will this, because `derived` is a new object each time
	$effect(() => {
		derived;
	});
</script>

<button onclick={() => (state.value += 1)}>
	{state.value}
</button>

<p>{state.value} doubled is {derived.value}</p>
```

An effect only depends on the values that it read the last time it ran. This has interesting implications for effects that have conditional code.

For instance, if `condition` is `true` in the code snippet below, the code inside the `if` block will run and `color` will be evaluated. This means that changes to either `condition` or `color` will cause the effect to re-run.

Conversely, if `condition` is `false`, `color` will not be evaluated, and the effect will _only_ re-run again when `condition` changes.

```ts
// @filename: ambient.d.ts
declare module 'canvas-confetti' {
	interface ConfettiOptions {
		colors: string[];
	}

	function confetti(opts?: ConfettiOptions): void;
	export default confetti;
}

// @filename: index.js
// ---cut---
import confetti from 'canvas-confetti';

let condition = $state(true);
let color = $state('#ff3e00');

$effect(() => {
	if (condition) {
		confetti({ colors: [color] });
	} else {
		confetti();
	}
});
```


## `$effect.pre`

In rare cases, you may need to run code _before_ the DOM updates. For this we can use the `$effect.pre` rune:

```svelte
<script>
	import { tick } from 'svelte';

	let div = $state();
	let messages = $state([]);

	// ...

	$effect.pre(() => {
		if (!div) return; // not yet mounted

		// reference `messages` array length so that this code re-runs whenever it changes
		messages.length;

		// autoscroll when new messages are added
		if (div.offsetHeight + div.scrollTop > div.scrollHeight - 20) {
			tick().then(() => {
				div.scrollTo(0, div.scrollHeight);
			});
		}
	});
</script>

<div bind:this={div}>
	{#each messages as message}
		<p>{message}</p>
	{/each}
</div>
```

Apart from the timing, `$effect.pre` works exactly like `$effect`.


## `$effect.tracking`

The `$effect.tracking` rune is an advanced feature that tells you whether or not the code is running inside a tracking context, such as an effect or inside your template:

<!-- codeblock:start {"title":"$effect.tracking()"} -->
```svelte
<!--- file: App.svelte --->
<script>
	console.log('in component setup:', $effect.tracking()); // false

	$effect(() => {
		console.log('in effect:', $effect.tracking()); // true
	});
</script>

<p>in template: {$effect.tracking()}</p> <!-- true -->
```
<!-- codeblock:end -->

It is used to implement abstractions like [`createSubscriber`](/docs/svelte/svelte-reactivity#createSubscriber), which will create listeners to update reactive values but _only_ if those values are being tracked (rather than, for example, read inside an event handler).


## `$effect.pending`

When using [`await`](await-expressions) in components, the `$effect.pending()` rune tells you how many promises are pending in the current [boundary](svelte-boundary), not including child boundaries:

<!-- codeblock:start {"title":"$effect.pending"} -->
```svelte
<!--- file: App.svelte --->
<script>
	let a = $state(1);
	let b = $state(2);

	async function add(a, b) {
		await new Promise((f) => setTimeout(f, 500)); // artificial delay
		return a + b;
	}
</script>

<button onclick={() => a++}>a++</button>
<button onclick={() => b++}>b++</button>

<p>{a} + {b} = {await add(a, b)}</p>

{#if $effect.pending()}
	<p>pending promises: {$effect.pending()}</p>
{/if}
```
<!-- codeblock:end -->


## `$effect.root`

The `$effect.root` rune is an advanced feature that creates a non-tracked scope that doesn't auto-cleanup. This is useful for nested effects that you want to manually control. This rune also allows for the creation of effects outside of the component initialisation phase.

```js
const destroy = $effect.root(() => {
	$effect(() => {
		// setup
	});

	return () => {
		// cleanup
	};
});

// later...
destroy();
```


## When not to use `$effect`

In general, `$effect` is best considered something of an escape hatch — useful for things like analytics and direct DOM manipulation — rather than a tool you should use frequently. In particular, avoid using it to synchronise state. Instead of this...

```svelte
<script>
	let count = $state(0);
	let doubled = $state();

	// don't do this!
	$effect(() => {
		doubled = count * 2;
	});
</script>
```

...do this:

```svelte
<script>
	let count = $state(0);
	let doubled = $derived(count * 2);
</script>
```

> [!NOTE] For things that are more complicated than a simple expression like `count * 2`, you can also use `$derived.by`.

If you're using an effect because you want to be able to reassign the derived value (to build an optimistic UI, for example) note that [deriveds can be directly overridden]($derived#Overriding-derived-values) as of Svelte 5.25.

You might be tempted to do something convoluted with effects to link one value to another. The following example shows two inputs for "money spent" and "money left" that are connected to each other. If you update one, the other should update accordingly. Instead of using effects for this...

<!-- codeblock:start {"title":"Setting state in effects (don't do this!)"} -->
```svelte
<!--- file: App.svelte --->
<script>
	const total = 100;
	let spent = $state(0);
	let left = $state(total);

	$effect(() => {
		left = total - spent;
	});

	$effect(() => {
		spent = total - left;
	});
</script>

<label>
	<input type="range" bind:value={spent} max={total} />
	{spent}/{total} spent
</label>

<label>
	<input type="range" bind:value={left} max={total} />
	{left}/{total} left
</label>

<style>
	label {
		display: flex;
		gap: 0.5em;
	}
</style>
```
<!-- codeblock:end -->

...use `oninput` callbacks or — better still — [function bindings](bind#Function-bindings) where possible:

<!-- codeblock:start {"title":"Setting state with function bindings"} -->
```svelte
<!--- file: App.svelte --->
<script>
	const total = 100;
	let spent = $state(0);
	let left = $derived(total - spent);

+++	function updateLeft(left) {
		spent = total - left;
	}+++
</script>

<label>
	<input type="range" bind:value={spent} max={total} />
	{spent}/{total} spent
</label>

<label>
	<input type="range" +++bind:value={() => left, updateLeft}+++ max={total} />
	{left}/{total} left
</label>

<style>
	label {
		display: flex;
		gap: 0.5em;
	}
</style>
```
<!-- codeblock:end -->

If you absolutely have to update `$state` within an effect and run into an infinite loop because you read and write to the same `$state`, use [untrack](svelte#untrack).


## $props

The inputs to a component are referred to as _props_, which is short for _properties_. You pass props to components just like you pass attributes to elements:

```svelte
<!--- file: App.svelte --->
<script>
	import MyComponent from './MyComponent.svelte';
</script>

<MyComponent adjective="cool" />
```

On the other side, inside `MyComponent.svelte`, we can receive props with the `$props` rune...

```svelte
<!--- file: MyComponent.svelte --->
<script>
	let props = $props();
</script>

<p>this component is {props.adjective}</p>
```

...though more commonly, you'll [_destructure_](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/Destructuring_assignment) your props:

```svelte
<!--- file: MyComponent.svelte --->
<script>
	let +++{ adjective }+++ = $props();
</script>

<p>this component is {+++adjective+++}</p>
```


## Fallback values

Destructuring allows us to declare fallback values, which are used if the parent component does not set a given prop (or the value is `undefined`):

```js
let { adjective = 'happy' } = $props();
```

> [!NOTE] Fallback values are not turned into reactive state proxies (see [Updating props](#Updating-props) for more info)


## Renaming props

We can also use the destructuring assignment to rename props, which is necessary if they're invalid identifiers, or a JavaScript keyword like `super`:

```js
let { super: trouper = 'lights are gonna find me' } = $props();
```


## Rest props

Finally, we can use a _rest property_ to get, well, the rest of the props:

```js
let { a, b, c, ...others } = $props();
```


## Updating props

References to a prop inside a component update when the prop itself updates — when `count` changes in `App.svelte`, it will also change inside `Child.svelte`. But the child component is able to temporarily override the prop value, which can be useful for unsaved ephemeral state:

<!-- codeblock:start {"title":"Temporarily updating props","selected":"Child.svelte"} -->
```svelte
<!--- file: App.svelte --->
<script>
	import Child from './Child.svelte';

	let count = $state(0);
</script>

<button onclick={() => (count += 1)}>
	clicks (parent): {count}
</button>

<Child {count} />
```

```svelte
<!--- file: Child.svelte --->
<script>
	let { count } = $props();
</script>

<button onclick={() => (count += 1)}>
	clicks (child): {count}
</button>
```
<!-- codeblock:end -->

While you can temporarily _reassign_ props, you should not _mutate_ props unless they are [bindable]($bindable).

If the prop is a regular object, the mutation will have no effect:

<!-- codeblock:start {"title":"Non-reactive props","selected":"Child.svelte"} -->
```svelte
<!--- file: App.svelte --->
<script>
	import Child from './Child.svelte';
</script>

<Child object={{ count: 0 }} />
```

```svelte
<!--- file: Child.svelte --->
<script>
	let { object } = $props();
</script>

<button onclick={() => {
	// has no effect
	object.count += 1
}}>
	clicks: {object.count}
</button>
```
<!-- codeblock:end -->

If the prop is a reactive state proxy, however, then mutations _will_ have an effect but you will see an [`ownership_invalid_mutation`](runtime-warnings#Client-warnings-ownership_invalid_mutation) warning, because the component is mutating state that does not 'belong' to it:

<!-- codeblock:start {"title":"Invalid mutation","selected":"Child.svelte"} -->
```svelte
<!--- file: App.svelte --->
<script>
	import Child from './Child.svelte';

	let object = $state({count: 0});
</script>

<Child {object} />
```

```svelte
<!--- file: Child.svelte --->
<script>
	let { object } = $props();
</script>

<button onclick={() => {
	// will cause the count below to update,
	// but with a warning. Don't mutate
	// objects you don't own!
	object.count += 1
}}>
	clicks: {object.count}
</button>
```
<!-- codeblock:end -->

The fallback value of a prop not declared with `$bindable` is left untouched — it is not turned into a reactive state proxy — meaning mutations will not cause updates:

<!-- codeblock:start {"title":"Non-reactive fallback props","selected":"Child.svelte"} -->
```svelte
<!--- file: App.svelte --->
<script>
	import Child from './Child.svelte';
</script>

<Child />
```

```svelte
<!--- file: Child.svelte --->
<script>
	let { object = { count: 0 } } = $props();
</script>

<button onclick={() => {
	// has no effect if the fallback value is used
	object.count += 1
}}>
	clicks: {object.count}
</button>
```
<!-- codeblock:end -->

In summary: don't mutate props. Either use callback props to communicate changes, or — if parent and child should share the same object — use the [`$bindable`]($bindable) rune.


## Type safety

You can add type safety to your components by annotating your props, as you would with any other variable declaration. In TypeScript that might look like this...

```svelte
<script lang="ts">
	let { adjective }: { adjective: string } = $props();
</script>
```

...while in JSDoc you can do this:

```svelte
<script>
	/** @type {{ adjective: string }} */
	let { adjective } = $props();
</script>
```

You can, of course, separate the type declaration from the annotation:

```svelte
<script lang="ts">
	interface Props {
		adjective: string;
	}

	let { adjective }: Props = $props();
</script>
```

> [!NOTE] Interfaces for native DOM elements are provided in the `svelte/elements` module (see [Typing wrapper components](typescript#Typing-wrapper-components))

If your component exposes [snippet](snippet) props like `children`, these should be typed using the `Snippet` interface imported from `'svelte'` — see [Typing snippets](snippet#Typing-snippets) for examples.

Adding types is recommended, as it ensures that people using your component can easily discover which props they should provide.



## `$props.id()`

This rune, added in version 5.20.0, generates an ID that is unique to the current component instance. When hydrating a server-rendered component, the value will be consistent between server and client.

This is useful for linking elements via attributes like `for` and `aria-labelledby`.

```svelte
<script>
	const uid = $props.id();
</script>

<form>
	<label for="{uid}-firstname">First Name: </label>
	<input id="{uid}-firstname" type="text" />

	<label for="{uid}-lastname">Last Name: </label>
	<input id="{uid}-lastname" type="text" />
</form>
```


## $bindable

Ordinarily, props go one way, from parent to child. This makes it easy to understand how data flows around your app.

In Svelte, component props can be _bound_, which means that data can also flow _up_ from child to parent. This isn't something you should do often — overuse can make your data flow unpredictable and your components harder to maintain — but it can simplify your code if used sparingly and carefully.

It also means that a state proxy can be _mutated_ in the child.

> [!NOTE] Mutation is also possible with normal props, but is strongly discouraged — Svelte will warn you if it detects that a component is mutating state it does not 'own'.

To mark a prop as bindable, we use the `$bindable` rune:

<!-- prettier-ignore -->
```svelte
/// file: FancyInput.svelte
<script>
	let { value = $bindable(), ...props } = $props();
</script>

<input bind:value={value} {...props} />

<style>
	input {
		font-family: 'Comic Sans MS';
		color: deeppink;
	}
</style>
```

Now, a component that uses `<FancyInput>` can add the [`bind:`](bind) directive (demo:

<!-- prettier-ignore -->
```svelte
/// file: App.svelte
<script>
	import FancyInput from './FancyInput.svelte';

	let message = $state('hello');
</script>

<FancyInput bind:value={message} />
<p>{message}</p>
```

The parent component doesn't _have_ to use `bind:` — it can just pass a normal prop. Some parents don't want to listen to what their children have to say.

In this case, you can specify a fallback value for when no prop is passed at all:

```js
/// file: FancyInput.svelte
let { value = $bindable('fallback'), ...props } = $props();
```


## $inspect

> [!NOTE] `$inspect` only works during development. In a production build it becomes a noop.

The `$inspect` rune is roughly equivalent to `console.log`, with the exception that it will re-run whenever its argument changes. `$inspect` tracks reactive state deeply, meaning that updating something inside an object or array using fine-grained reactivity will cause it to re-fire:

<!-- codeblock:start {"title":"$inspect(...)"} -->
```svelte
<!--- file: App.svelte --->
<script>
	let count = $state(0);
	let message = $state('hello');

	$inspect(count, message); // will console.log when `count` or `message` change
</script>

<button onclick={() => count++}>Increment</button>
<input bind:value={message} />
```
<!-- codeblock:end -->

On updates, a stack trace will be printed, making it easy to find the origin of a state change (unless you're in the playground, due to technical limitations).


## $inspect(...).with

`$inspect(...)` returns an object with a `with` method, which you can invoke with a callback that will then be invoked instead of `console.log`. The first argument to the callback is either `"init"` or `"update"`; subsequent arguments are the values passed to `$inspect`:

<!-- codeblock:start {"title":"$inspect(...).with(...)"} -->
```svelte
<!--- file: App.svelte --->
<script>
	let count = $state(0);

	$inspect(count).with((type, count) => {
		if (type === 'update') {
			debugger; // or `console.trace`, or whatever you want
		}
	});
</script>

<button onclick={() => count++}>Increment</button>
```
<!-- codeblock:end -->


## $inspect.trace(...)

This rune, added in 5.14, causes the surrounding function to be _traced_ in development. Any time the function re-runs as part of an [effect]($effect) or a [derived]($derived), information will be printed to the console about which pieces of reactive state caused the effect to fire.

```svelte
<script>
	import { doSomeWork } from './elsewhere';

	$effect(() => {
		+++// $inspect.trace must be the first statement of a function body+++
		+++$inspect.trace();+++
		doSomeWork();
	});
</script>
```

`$inspect.trace` takes an optional first argument which will be used as the label.


## $host

When compiling a component as a [custom element](custom-elements), the `$host` rune provides access to the host element, allowing you to (for example) dispatch custom events (demo:

<!-- prettier-ignore -->
```svelte
/// file: Stepper.svelte
<svelte:options customElement="my-stepper" />

<script>
	function dispatch(type) {
		+++$host()+++.dispatchEvent(new CustomEvent(type));
	}
</script>

<button onclick={() => dispatch('decrement')}>decrement</button>
<button onclick={() => dispatch('increment')}>increment</button>
```

<!-- prettier-ignore -->
```svelte
/// file: App.svelte
<script>
	import './Stepper.svelte';

	let count = $state(0);
</script>

<my-stepper
	ondecrement={() => count -= 1}
	onincrement={() => count += 1}
></my-stepper>

<p>count: {count}</p>
```


## Basic markup

Markup inside a Svelte component can be thought of as HTML++.


## Tags

A lowercase tag, like `<div>`, denotes a regular HTML element. A capitalised tag or a tag that uses dot notation, such as `<Widget>` or `<my.stuff>`, indicates a _component_.

```svelte
<script>
	import Widget from './Widget.svelte';
</script>

<div>
	<Widget />
</div>
```


## Element attributes

By default, attributes work exactly like their HTML counterparts.

```svelte
<div class="foo">
	<button disabled>can't touch this</button>
</div>
```

As in HTML, values may be unquoted.

<!-- prettier-ignore -->
```svelte
<input type=checkbox />
```

Attribute values can contain JavaScript expressions.

```svelte
<a href="page/{p}">page {p}</a>
```

Or they can _be_ JavaScript expressions.

```svelte
<button disabled={!clickable}>...</button>
```

Boolean attributes are included on the element if their value is [truthy](https://developer.mozilla.org/en-US/docs/Glossary/Truthy) and excluded if it's [falsy](https://developer.mozilla.org/en-US/docs/Glossary/Falsy).

All other attributes are included unless their value is [nullish](https://developer.mozilla.org/en-US/docs/Glossary/Nullish) (`null` or `undefined`).

```svelte
<input required={false} placeholder="This input field is not required" />
<div title={null}>This div has no title attribute</div>
```

> [!NOTE] Quoting a singular expression does not affect how the value is parsed, but in Svelte 6 it will cause the value to be coerced to a string:
>
> <!-- prettier-ignore -->
> ```svelte
> <button disabled="{number !== 42}">...</button>
> ```

When the attribute name and value match (`name={name}`), they can be replaced with `{name}`.

```svelte
<button {disabled}>...</button>
<!-- equivalent to
<button disabled={disabled}>...</button>
-->
```


## Component props

By convention, values passed to components are referred to as _properties_ or _props_ rather than _attributes_, which are a feature of the DOM.

As with elements, `name={name}` can be replaced with the `{name}` shorthand.

```svelte
<Widget foo={bar} answer={42} text="hello" />
```


## Spread attributes

_Spread attributes_ allow many attributes or properties to be passed to an element or component at once.

An element or component can have multiple spread attributes, interspersed with regular ones. Order matters — if `things.a` exists it will take precedence over `a="b"`, while `c="d"` would take precedence over `things.c`:

```svelte
<Widget a="b" {...things} c="d" />
```


## Events

Listening to DOM events is possible by adding attributes to the element that start with `on`. For example, to listen to the `click` event, add the `onclick` attribute to a button:

```svelte
<button onclick={() => console.log('clicked')}>click me</button>
```

Event attributes are case sensitive. `onclick` listens to the `click` event, `onClick` listens to the `Click` event, which is different. This ensures you can listen to custom events that have uppercase characters in them.

Because events are just attributes, the same rules as for attributes apply:

- you can use the shorthand form: `<button {onclick}>click me</button>`
- you can spread them: `<button {...thisSpreadContainsEventAttributes}>click me</button>`

Timing-wise, event attributes always fire after events from bindings (e.g. `oninput` always fires after an update to `bind:value`). Under the hood, some event handlers are attached directly with `addEventListener`, while others are _delegated_.

When using `ontouchstart` and `ontouchmove` event attributes, the handlers are [passive](https://developer.mozilla.org/en-US/docs/Web/API/EventTarget/addEventListener#using_passive_listeners) for better performance. This greatly improves responsiveness by allowing the browser to scroll the document immediately, rather than waiting to see if the event handler calls `event.preventDefault()`.

In the very rare cases that you need to prevent these event defaults, you should use [`on`](svelte-events#on) instead (for example inside an action).


## Event delegation

To reduce memory footprint and increase performance, Svelte uses a technique called event delegation. This means that for certain events — see the list below — a single event listener at the application root takes responsibility for running any handlers on the event's path.

There are a few gotchas to be aware of:

- when you manually dispatch an event with a delegated listener, make sure to set the `{ bubbles: true }` option or it won't reach the application root
- when using `addEventListener` directly, avoid calling `stopPropagation` or the event won't reach the application root and handlers won't be invoked. Similarly, handlers added manually inside the application root will run _before_ handlers added declaratively deeper in the DOM (with e.g. `onclick={...}`), in both capturing and bubbling phases. For these reasons it's better to use the `on` function imported from `svelte/events` rather than `addEventListener`, as it will ensure that order is preserved and `stopPropagation` is handled correctly.

The following event handlers are delegated:

- `beforeinput`
- `click`
- `change`
- `dblclick`
- `contextmenu`
- `focusin`
- `focusout`
- `input`
- `keydown`
- `keyup`
- `mousedown`
- `mousemove`
- `mouseout`
- `mouseover`
- `mouseup`
- `pointerdown`
- `pointermove`
- `pointerout`
- `pointerover`
- `pointerup`
- `touchend`
- `touchmove`
- `touchstart`


## Text expressions

A JavaScript expression can be included as text by surrounding it with curly braces.

```svelte
{expression}
```

Expressions that are `null` or `undefined` will be omitted; all others are [coerced to strings](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/String#string_coercion).

Curly braces can be included in a Svelte template by using their [HTML entity](https://developer.mozilla.org/docs/Glossary/Entity) strings: `&lbrace;`, `&lcub;`, or `&#123;` for `{` and `&rbrace;`, `&rcub;`, or `&#125;` for `}`.

If you're using a regular expression (`RegExp`) [literal notation](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/RegExp#literal_notation_and_constructor), you'll need to wrap it in parentheses.

<!-- prettier-ignore -->
```svelte
<h1>Hello {name}!</h1>
<p>{a} + {b} = {a + b}.</p>

<div>{(/^[A-Za-z ]+$/).test(value) ? x : y}</div>
```

The expression will be stringified and escaped to prevent code injections. If you want to render HTML, use the `{@html}` tag instead.

```svelte
{@html potentiallyUnsafeHtmlString}
```

> [!NOTE] Make sure that you either escape the passed string or only populate it with values that are under your control in order to prevent [XSS attacks](https://owasp.org/www-community/attacks/xss/)


## Comments

You can use HTML comments inside components.

```svelte
<!-- this is a comment! --><h1>Hello world</h1>
```

Comments beginning with `svelte-ignore` disable warnings for the next block of markup. Usually, these are accessibility warnings; make sure that you're disabling them for a good reason.

```svelte
<!-- svelte-ignore a11y_autofocus -->
<input bind:value={name} autofocus />
```

You can add a special comment starting with `@component` that will show up when hovering over the component name in other files.

````svelte
<!--
@component
- You can use markdown here.
- You can also use code blocks here.
- Usage:
  ```html
  <Main name="Arethra">
  ```
-->
<script>
	let { name } = $props();
</script>

<main>
	<h1>
		Hello, {name}
	</h1>
</main>
````


## {#if ...}

```svelte
<!--- copy: false  --->
{#if expression}...{/if}
```

```svelte
<!--- copy: false  --->
{#if expression}...{:else if expression}...{/if}
```

```svelte
<!--- copy: false  --->
{#if expression}...{:else}...{/if}
```

Content that is conditionally rendered can be wrapped in an if block.

```svelte
{#if answer === 42}
	<p>what was the question?</p>
{/if}
```

Additional conditions can be added with `{:else if expression}`, optionally ending in an `{:else}` clause.

```svelte
{#if porridge.temperature > 100}
	<p>too hot!</p>
{:else if 80 > porridge.temperature}
	<p>too cold!</p>
{:else}
	<p>just right!</p>
{/if}
```

(Blocks don't have to wrap elements, they can also wrap text within elements.)


## {#each ...}

```svelte
<!--- copy: false  --->
{#each expression as name}...{/each}
```

```svelte
<!--- copy: false  --->
{#each expression as name, index}...{/each}
```

Iterating over values can be done with an each block. The values in question can be arrays, array-like objects (i.e. anything with a `length` property), or iterables like `Map` and `Set`. (Internally, they are converted to arrays with [`Array.from`](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Array/from).)

If the value is `null` or `undefined`, it is treated the same as an empty array (which will cause [else blocks](#Else-blocks) to be rendered, where applicable).

```svelte
<h1>Shopping list</h1>
<ul>
	{#each items as item}
		<li>{item.name} x {item.qty}</li>
	{/each}
</ul>
```

An each block can also specify an _index_, equivalent to the second argument in an `array.map(...)` callback:

```svelte
{#each items as item, i}
	<li>{i + 1}: {item.name} x {item.qty}</li>
{/each}
```


## Keyed each blocks

```svelte
<!--- copy: false  --->
{#each expression as name (key)}...{/each}
```

```svelte
<!--- copy: false  --->
{#each expression as name, index (key)}...{/each}
```

If a _key_ expression is provided — which must uniquely identify each list item — Svelte will use it to intelligently update the list when data changes by inserting, moving and deleting items, rather than adding or removing items at the end and updating the state in the middle.

The key can be any object, but strings and numbers are recommended since they allow identity to persist when the objects themselves change.

```svelte
{#each items as item (item.id)}
	<li>{item.name} x {item.qty}</li>
{/each}

<!-- or with additional index value -->
{#each items as item, i (item.id)}
	<li>{i + 1}: {item.name} x {item.qty}</li>
{/each}
```

You can freely use destructuring and rest patterns in each blocks.

```svelte
{#each items as { id, name, qty }, i (id)}
	<li>{i + 1}: {name} x {qty}</li>
{/each}

{#each objects as { id, ...rest }}
	<li><span>{id}</span><MyComponent {...rest} /></li>
{/each}

{#each items as [id, ...rest]}
	<li><span>{id}</span><MyComponent values={rest} /></li>
{/each}
```


## Each blocks without an item

```svelte
<!--- copy: false  --->
{#each expression}...{/each}
```

```svelte
<!--- copy: false  --->
{#each expression, index}...{/each}
```

In case you just want to render something `n` times, you can omit the `as` part:

<!-- codeblock:start {"title":"Chess board"} -->
```svelte
<!--- file: App.svelte --->
<div class="chess-board">
	{#each { length: 8 }, rank}
		{#each { length: 8 }, file}
			<div class:black={(rank + file) % 2 === 1}></div>
		{/each}
	{/each}
</div>

<style>
	.chess-board {
		display: grid;
		grid-template-columns: repeat(8, 1fr);
		grid-template-rows: repeat(8, 1fr);
		border: 1px solid black;
		aspect-ratio: 1;

		.black {
			background: black;
		}
	}
</style>
```
<!-- codeblock:end -->


## Else blocks

```svelte
<!--- copy: false  --->
{#each expression as name}...{:else}...{/each}
```

An each block can also have an `{:else}` clause, which is rendered if the list is empty.

```svelte
{#each todos as todo}
	<p>{todo.text}</p>
{:else}
	<p>No tasks today!</p>
{/each}
```


## {#key ...}

```svelte
<!--- copy: false  --->
{#key expression}...{/key}
```

Key blocks destroy and recreate their contents when the value of an expression changes. When used around components, this will cause them to be reinstantiated and reinitialised:

```svelte
{#key value}
	<Component />
{/key}
```

It's also useful if you want a transition to play whenever a value changes:

```svelte
{#key value}
	<div transition:fade>{value}</div>
{/key}
```


## {#await ...}

```svelte
<!--- copy: false  --->
{#await expression}...{:then name}...{:catch name}...{/await}
```

```svelte
<!--- copy: false  --->
{#await expression}...{:then name}...{/await}
```

```svelte
<!--- copy: false  --->
{#await expression then name}...{/await}
```

```svelte
<!--- copy: false  --->
{#await expression catch name}...{/await}
```

Await blocks allow you to branch on the three possible states of a [`Promise`](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Promise) — pending, fulfilled or rejected.

```svelte
{#await promise}
	<!-- promise is pending -->
	<p>waiting for the promise to resolve...</p>
{:then value}
	<!-- promise was fulfilled or not a Promise -->
	<p>The value is {value}</p>
{:catch error}
	<!-- promise was rejected -->
	<p>Something went wrong: {error.message}</p>
{/await}
```

> [!NOTE] During server-side rendering, only the pending branch will be rendered.
>
> If the provided expression is not a `Promise`, only the `:then` branch will be rendered, including during server-side rendering.

The `catch` block can be omitted if you don't need to render anything when the promise rejects (or no error is possible).

```svelte
{#await promise}
	<!-- promise is pending -->
	<p>waiting for the promise to resolve...</p>
{:then value}
	<!-- promise was fulfilled -->
	<p>The value is {value}</p>
{/await}
```

If you don't care about the pending state, you can also omit the initial block.

```svelte
{#await promise then value}
	<p>The value is {value}</p>
{/await}
```

Similarly, if you only want to show the error state, you can omit the `then` block.

```svelte
{#await promise catch error}
	<p>The error is {error}</p>
{/await}
```

> [!NOTE] You can use `#await` with [`import(...)`](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/import) to render components lazily:
>
> ```svelte
> {#await import('./Component.svelte') then { default: Component }}
> 	<Component />
> {/await}
> ```


## {#snippet ...}

```svelte
<!--- copy: false  --->
{#snippet name()}...{/snippet}
```

```svelte
<!--- copy: false  --->
{#snippet name(param1, param2, paramN)}...{/snippet}
```

Snippets, and [render tags](@render), are a way to create reusable chunks of markup inside your components. Instead of writing duplicative code like this...

```svelte
{#each images as image}
	{#if image.href}
		<a href={image.href}>
			<figure>
				<img src={image.src} alt={image.caption} width={image.width} height={image.height} />
				<figcaption>{image.caption}</figcaption>
			</figure>
		</a>
	{:else}
		<figure>
			<img src={image.src} alt={image.caption} width={image.width} height={image.height} />
			<figcaption>{image.caption}</figcaption>
		</figure>
	{/if}
{/each}
```

...you can write this:

```svelte
{#snippet figure(image)}
	<figure>
		<img src={image.src} alt={image.caption} width={image.width} height={image.height} />
		<figcaption>{image.caption}</figcaption>
	</figure>
{/snippet}

{#each images as image}
	{#if image.href}
		<a href={image.href}>
			{@render figure(image)}
		</a>
	{:else}
		{@render figure(image)}
	{/if}
{/each}
```

Like function declarations, snippets can have an arbitrary number of parameters, which can have default values, and you can destructure each parameter. You cannot use rest parameters, however.


## Snippet scope

Snippets can be declared anywhere inside your component. They can reference values declared outside themselves, for example in the `<script>` tag or in `{#each ...}` blocks...

<!-- codeblock:start {"title":"Snippets"} -->
```svelte
<!--- file: App.svelte --->
<script>
	let { message = `it's great to see you!` } = $props();
</script>

{#snippet hello(name)}
	<p>hello {name}! {message}!</p>
{/snippet}

{@render hello('alice')}
{@render hello('bob')}
```
<!-- codeblock:end -->

...and they are 'visible' to everything in the same lexical scope (i.e. siblings, and children of those siblings):

```svelte
<div>
	{#snippet x()}
		{#snippet y()}...{/snippet}

		<!-- this is fine -->
		{@render y()}
	{/snippet}

	<!-- this will error, as `y` is not in scope -->
	{@render y()}
</div>

<!-- this will also error, as `x` is not in scope -->
{@render x()}
```

Snippets can reference themselves and each other:

<!-- codeblock:start {"title":"Self-referencing snippets"} -->
```svelte
<!--- file: App.svelte --->
{#snippet blastoff()}
	<span>🚀</span>
{/snippet}

{#snippet countdown(n)}
	{#if n > 0}
		<span>{n}...</span>
		{@render countdown(n - 1)}
	{:else}
		{@render blastoff()}
	{/if}
{/snippet}

{@render countdown(10)}
```
<!-- codeblock:end -->


## Passing snippets to components



## Explicit props

Within the template, snippets are values just like any other. As such, they can be passed to components as props:

<!-- codeblock:start {"title":"Explicit snippet props"} -->
```svelte
<!--- file: App.svelte --->
<script>
	import Table from './Table.svelte';

	const fruits = [
		{ name: 'apples', qty: 5, price: 2 },
		{ name: 'bananas', qty: 10, price: 1 },
		{ name: 'cherries', qty: 20, price: 0.5 }
	];
</script>

{#snippet header()}
	<th>fruit</th>
	<th>qty</th>
	<th>price</th>
	<th>total</th>
{/snippet}

{#snippet row(d)}
	<td>{d.name}</td>
	<td>{d.qty}</td>
	<td>{d.price}</td>
	<td>{d.qty * d.price}</td>
{/snippet}

<Table data={fruits} +++{header} {row}+++ />
```

```svelte
<!--- file: Table.svelte --->
<script>
	let { data, header, row } = $props();
</script>

<table>
	{#if header}
		<thead>
			<tr>{@render header()}</tr>
		</thead>
	{/if}

	<tbody>
		{#each data as d}
			<tr>{@render row(d)}</tr>
		{/each}
	</tbody>
</table>

<style>
	table {
		text-align: left;
		border-spacing: 0;
	}

	tbody tr:nth-child(2n+1) {
		background: ButtonFace;
	}

	table :global(th), table :global(td) {
		padding: 0.5em;
	}
</style>
```
<!-- codeblock:end -->

Think about it like passing content instead of data to a component. The concept is similar to slots in web components.


## Implicit props

As an authoring convenience, snippets declared directly _inside_ a component implicitly become props _on_ the component:

<!-- codeblock:start {"title":"Implicit snippet props"} -->
```svelte
<!--- file: App.svelte --->
<script>
	import Table from './Table.svelte';

	const fruits = [
		{ name: 'apples', qty: 5, price: 2 },
		{ name: 'bananas', qty: 10, price: 1 },
		{ name: 'cherries', qty: 20, price: 0.5 }
	];
</script>

<Table data={fruits}>
	{#snippet header()}
		<th>fruit</th>
		<th>qty</th>
		<th>price</th>
		<th>total</th>
	{/snippet}

	{#snippet row(d)}
		<td>{d.name}</td>
		<td>{d.qty}</td>
		<td>{d.price}</td>
		<td>{d.qty * d.price}</td>
	{/snippet}
</Table>
```

```svelte
<!--- file: Table.svelte --->
<script>
	let { data, header, row } = $props();
</script>

<table>
	{#if header}
		<thead>
			<tr>{@render header()}</tr>
		</thead>
	{/if}

	<tbody>
		{#each data as d}
			<tr>{@render row(d)}</tr>
		{/each}
	</tbody>
</table>

<style>
	table {
		text-align: left;
		border-spacing: 0;
	}

	tbody tr:nth-child(2n+1) {
		background: ButtonFace;
	}

	table :global(th), table :global(td) {
		padding: 0.5em;
	}
</style>
```
<!-- codeblock:end -->


## Implicit `children` snippet

Any content inside the component tags that is _not_ a snippet declaration implicitly becomes part of the `children` snippet:

<!-- codeblock:start {"title":"Implicit children snippet","selected":"Button.svelte"} -->
```svelte
<!--- file: App.svelte --->
<script>
	import Button from './Button.svelte';
</script>

<Button>click me</Button>
```

```svelte
<!--- file: Button.svelte --->
<script>
	let { children } = $props();
</script>

<!-- result will be <button>click me</button> -->
<button>{@render children()}</button>
```
<!-- codeblock:end -->

> [!NOTE] Note that you cannot have a prop called `children` if you also have content inside the component — for this reason, you should avoid having props with that name


## Optional snippet props

You can declare snippet props as being optional. You can either use optional chaining to not render anything if the snippet isn't set...

```svelte
<script>
    let { children } = $props();
</script>

{@render children?.()}
```

...or use an `#if` block to render fallback content:

```svelte
<script>
    let { children } = $props();
</script>

{#if children}
    {@render children()}
{:else}
    fallback content
{/if}
```


## Typing snippets

Snippets implement the `Snippet` interface imported from `'svelte'`:

```svelte
<script lang="ts">
	import type { Snippet } from 'svelte';

	interface Props {
		data: any[];
		children: Snippet;
		row: Snippet<[any]>;
	}

	let { data, children, row }: Props = $props();
</script>
```

With this change, red squigglies will appear if you try and use the component without providing a `data` prop and a `row` snippet. Notice that the type argument provided to `Snippet` is a tuple, since snippets can have multiple parameters.

We can tighten things up further by declaring a generic, so that `data` and `row` refer to the same type:

```svelte
<script lang="ts" generics="T">
	import type { Snippet } from 'svelte';

	let {
		data,
		children,
		row
	}: {
		data: T[];
		children: Snippet;
		row: Snippet<[T]>;
	} = $props();
</script>
```


## Exporting snippets

Snippets declared at the top level of a `.svelte` file can be exported from a `<script module>` for use in other components, provided they don't reference any declarations in a non-module `<script>` (whether directly or indirectly, via other snippets):

<!-- codeblock:start {"title":"Exported snippets","selected":"snippets.svelte"} -->
```svelte
<!--- file: App.svelte --->
<script>
	import { add } from './snippets.svelte';
</script>

{@render add(1, 2)}

```

```svelte
<!--- file: snippets.svelte --->
<script module>
	export { add };
</script>

{#snippet add(a, b)}
	{a} + {b} = {a + b}
{/snippet}
```
<!-- codeblock:end -->

> [!NOTE]
> This requires Svelte 5.5.0 or newer


## Programmatic snippets

Snippets can be created programmatically with the [`createRawSnippet`](svelte#createRawSnippet) API. This is intended for advanced use cases.


## Snippets and slots

In Svelte 4, content can be passed to components using [slots](legacy-slots). Snippets are more powerful and flexible, and so slots have been deprecated in Svelte 5.


## {@render ...}

To render a [snippet](snippet), use a `{@render ...}` tag.

```svelte
{#snippet sum(a, b)}
	<p>{a} + {b} = {a + b}</p>
{/snippet}

{@render sum(1, 2)}
{@render sum(3, 4)}
{@render sum(5, 6)}
```

The expression can be an identifier like `sum`, or an arbitrary JavaScript expression:

```svelte
{@render (cool ? coolSnippet : lameSnippet)()}
```


## Optional snippets

If the snippet is potentially undefined — for example, because it's an incoming prop — then you can use optional chaining to only render it when it _is_ defined:

```svelte
{@render children?.()}
```

Alternatively, use an [`{#if ...}`](if) block with an `:else` clause to render fallback content:

```svelte
{#if children}
	{@render children()}
{:else}
	<p>fallback content</p>
{/if}
```


## {@html ...}

To inject raw HTML into your component, use the `{@html ...}` tag:

```svelte
<article>
	{@html content}
</article>
```

> [!NOTE] Make sure that you either escape the passed string or only populate it with values that are under your control in order to prevent [XSS attacks](https://owasp.org/www-community/attacks/xss/). Never render unsanitized content.

The expression should be valid standalone HTML — this will not work, because `</div>` is not valid HTML:

```svelte
{@html '<div>'}content{@html '</div>'}
```

It also will not compile Svelte code.


## Styling

Content rendered this way is 'invisible' to Svelte and as such will not receive [scoped styles](scoped-styles). In other words, this will not work, and the `a` and `img` styles will be regarded as unused:

<!-- prettier-ignore -->
```svelte
<article>
	{@html content}
</article>

<style>
	article {
		a { color: hotpink }
		img { width: 100% }
	}
</style>
```

Instead, use the `:global` modifier to target everything inside the `<article>`:

<!-- prettier-ignore -->
```svelte
<style>
	article +++:global+++ {
		a { color: hotpink }
		img { width: 100% }
	}
</style>
```


## {@attach ...}

Attachments are functions that run in an [effect]($effect) when an element is mounted to the DOM or when [state]($state) read inside the function updates.

Optionally, they can return a function that is called before the attachment re-runs, or after the element is later removed from the DOM.

> [!NOTE]
> Attachments are available in Svelte 5.29 and newer.

```svelte
<!--- file: App.svelte --->
<script>
	/** @type {import('svelte/attachments').Attachment} */
	function myAttachment(element) {
		console.log(element.nodeName); // 'DIV'

		return () => {
			console.log('cleaning up');
		};
	}
</script>

<div {@attach myAttachment}>...</div>
```

An element can have any number of attachments.


## Attachment factories

A useful pattern is for a function, such as `tooltip` in this example, to _return_ an attachment (demo:

```svelte
<!--- file: App.svelte --->
<script>
	import tippy from 'tippy.js';

	let content = $state('Hello!');

	/**
	 * @param {string} content
	 * @returns {import('svelte/attachments').Attachment}
	 */
	function tooltip(content) {
		return (element) => {
			const tooltip = tippy(element, { content });
			return tooltip.destroy;
		};
	}
</script>

<input bind:value={content} />

<button {@attach tooltip(content)}>
	Hover me
</button>
```

Since the `tooltip(content)` expression runs inside an [effect]($effect), the attachment will be destroyed and recreated whenever `content` changes. The same thing would happen for any state read _inside_ the attachment function when it first runs. (If this isn't what you want, see [Controlling when attachments re-run](#Controlling-when-attachments-re-run).)


## Inline attachments

Attachments can also be created inline (demo:

```svelte
<!--- file: App.svelte --->
<canvas
	width={32}
	height={32}
	{@attach (canvas) => {
		const context = canvas.getContext('2d');

		$effect(() => {
			context.fillStyle = color;
			context.fillRect(0, 0, canvas.width, canvas.height);
		});
	}}
></canvas>
```

> [!NOTE]
> The nested effect runs whenever `color` changes, while the outer effect (where `canvas.getContext(...)` is called) only runs once, since it doesn't read any reactive state.


## Conditional attachments

Falsy values like `false` or `undefined` are treated as no attachment, enabling conditional usage:

```svelte
<div {@attach enabled && myAttachment}>...</div>
```


## Passing attachments to components

When used on a component, `{@attach ...}` will create a prop whose key is a [`Symbol`](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Symbol). If the component then [spreads](/tutorial/svelte/spread-props) props onto an element, the element will receive those attachments.

This allows you to create _wrapper components_ that augment elements (demo:

```svelte
<!--- file: Button.svelte --->
<script>
	/** @type {import('svelte/elements').HTMLButtonAttributes} */
	let { children, ...props } = $props();
</script>

<!-- `props` includes attachments -->
<button {...props}>
	{@render children?.()}
</button>
```

```svelte
<!--- file: App.svelte --->
<script>
	import tippy from 'tippy.js';
	import Button from './Button.svelte';

	let content = $state('Hello!');

	/**
	 * @param {string} content
	 * @returns {import('svelte/attachments').Attachment}
	 */
	function tooltip(content) {
		return (element) => {
			const tooltip = tippy(element, { content });
			return tooltip.destroy;
		};
	}
</script>

<input bind:value={content} />

<Button {@attach tooltip(content)}>
	Hover me
</Button>
```


## Controlling when attachments re-run

Attachments, unlike [actions](use), are fully reactive: `{@attach foo(bar)}` will re-run on changes to `foo` _or_ `bar` (or any state read inside `foo`):

```js
// @errors: 7006 2304 2552
function foo(bar) {
	return (node) => {
		veryExpensiveSetupWork(node);
		update(node, bar);
	};
}
```

In the rare case that this is a problem (for example, if `foo` does expensive and unavoidable setup work) consider passing the data inside a function and reading it in a child effect:

```js
// @errors: 7006 2304 2552
function foo(+++getBar+++) {
	return (node) => {
		veryExpensiveSetupWork(node);

+++		$effect(() => {
			update(node, getBar());
		});+++
	}
}
```


## Creating attachments programmatically

To add attachments to an object that will be spread onto a component or element, use [`createAttachmentKey`](svelte-attachments#createAttachmentKey).


## Converting actions to attachments

If you're using a library that only provides actions, you can convert them to attachments with [`fromAction`](svelte-attachments#fromAction), allowing you to (for example) use them with components.


## {@const ...}

> [!NOTE] `{@const x = y}` is legacy syntax — use [`{const x = $derived(y)}`](declaration-tags) instead

The `{@const ...}` tag defines a local constant.

```svelte
{#each boxes as box}
	{@const area = box.width * box.height}
	{box.width} * {box.height} = {area}
{/each}
```

`{@const}` is only allowed as an immediate child of a block — `{#if ...}`, `{#each ...}`, `{#snippet ...}` and so on — a `<Component />` or a `<svelte:boundary>`.


## {@debug ...}

The `{@debug ...}` tag offers an alternative to `console.log(...)`. It logs the values of specific variables whenever they change, and pauses code execution if you have devtools open.

```svelte
<script>
	let user = {
		firstname: 'Ada',
		lastname: 'Lovelace'
	};
</script>

{@debug user}

<h1>Hello {user.firstname}!</h1>
```

`{@debug ...}` accepts a comma-separated list of variable names (not arbitrary expressions).

```svelte
<!-- Compiles -->
{@debug user}
{@debug user1, user2, user3}

<!-- WON'T compile -->
{@debug user.firstname}
{@debug myArray[0]}
{@debug !isReady}
{@debug typeof user === 'object'}
```

The `{@debug}` tag without any arguments will insert a `debugger` statement that gets triggered when _any_ state changes, as opposed to the specified variables.


## {let/const ...}

Declaration tags define local variables inside markup with `const` or `let`:

<!-- codeblock:start {"title":"Declaration tags"} -->
```svelte
<!--- file: App.svelte --->
<script>
	let boxes = [{ width: 10, height: 10 }, { width: 15, height: 15 }];
</script>

{#each boxes as box}
	{const area = box.width * box.height}
	{const label = `${box.width} ⨉ ${box.height} = ${area}`}

	<p>{label}</p>
{/each}
```
<!-- codeblock:end -->

> [!NOTE] Declaration tags are available since Svelte 5.56.

> [!NOTE] The [`{@const ...}`](@const) syntax is considered legacy — use declaration tags instead.

When values should be reactive, you can use `$state` and `$derived`:

<!-- codeblock:start {"title":"Reactive declaration tags"} -->
```svelte
<!--- file: App.svelte --->
<script>
	let user = $state({ name: 'Svelte' });
	let editing = $state(false);
</script>

<p>Hello {user.name}</p>
<button onclick={() => editing = true}>edit name</button>

{#if editing}
	{let name = $state(user.name)}
	{const greeting = $derived(`Hello ${name}`)}

	<hr>
	<input bind:value={name} />
	<p>{greeting}</p>

	<button onclick={() => {
		user.name = name;
		editing = false;
	}}>save</button>
{/if}
```
<!-- codeblock:end -->

Declaration tags can be used anywhere inside the component. They can reference values declared outside themselves (for example in the `<script>` tag or in `{#each ...}` blocks) and are 'visible' to everything in the same lexical scope (i.e. siblings, and children of those siblings):

<!-- codeblock:start {"title":"Declaration tag scope"} -->
```svelte
<!--- file: App.svelte --->
{const hello = 'hello'}
{hello} <!-- 'hello' -->
<div>
	{const hello = 'hi'}
	{hello} <!-- 'hi' -->
	<div>
		{hello} <!-- 'hi' -->
	</div>
</div>
{hello} <!-- 'hello' -->
```
<!-- codeblock:end -->


## bind:

Data ordinarily flows down, from parent to child. The `bind:` directive allows data to flow the other way, from child to parent.

The general syntax is `bind:property={expression}`, where `expression` is an [_lvalue_](https://press.rebus.community/programmingfundamentals/chapter/lvalue-and-rvalue/) (i.e. a variable or an object property). When the expression is an identifier with the same name as the property, we can omit the expression — in other words these are equivalent:

<!-- prettier-ignore -->
```svelte
<input bind:value={value} />
<input bind:value />
```


Svelte creates an event listener that updates the bound value. If an element already has a listener for the same event, that listener will be fired before the bound value is updated.

Most bindings are _two-way_, meaning that changes to the value will affect the element and vice versa. A few bindings are _readonly_, meaning that changing their value will have no effect on the element.


## Function bindings

You can also use `bind:property={get, set}`, where `get` and `set` are functions, allowing you to perform validation and transformation:

```svelte
<input bind:value={
	() => value,
	(v) => value = v.toLowerCase()}
/>
```

In the case of readonly bindings like [dimension bindings](#Dimensions), the `get` value should be `null`:

```svelte
<div
	bind:clientWidth={null, redraw}
	bind:clientHeight={null, redraw}
>...</div>
```

> [!NOTE]
> Function bindings are available in Svelte 5.9.0 and newer.


## `<input bind:value>`

A `bind:value` directive on an `<input>` element binds the input's `value` property:

<!-- prettier-ignore -->
```svelte
<script>
	let message = $state('hello');
</script>

<input bind:value={message} />
<p>{message}</p>
```

In the case of a numeric input (`type="number"` or `type="range"`), the value will be coerced to a number:

<!-- codeblock:start {"title":"Numeric bindings"} -->
```svelte
<!--- file: App.svelte --->
<script>
	let a = $state(1);
	let b = $state(2);
</script>

<label>
	<input type="number" bind:value={a} min="0" max="10" />
	<input type="range" bind:value={a} min="0" max="10" />
</label>

<label>
	<input type="number" bind:value={b} min="0" max="10" />
	<input type="range" bind:value={b} min="0" max="10" />
</label>

<p>{a} + {b} = {a + b}</p>
```
<!-- codeblock:end -->

If the input is empty or invalid (in the case of `type="number"`), the value is `undefined`.

Since 5.6.0, if an `<input>` has a `defaultValue` and is part of a form, it will revert to that value instead of the empty string when the form is reset. Note that for the initial render the value of the binding takes precedence unless it is `null` or `undefined`.

```svelte
<script>
	let value = $state('');
</script>

<form>
	<input bind:value defaultValue="not the empty string">
	<input type="reset" value="Reset">
</form>
```

> [!NOTE]
> Use reset buttons sparingly, and ensure that users won't accidentally click them while trying to submit the form.


## `<input bind:checked>`

Checkbox inputs can be bound with `bind:checked`:

```svelte
<label>
	<input type="checkbox" bind:checked={accepted} />
	Accept terms and conditions
</label>
```

Since 5.6.0, if an `<input>` has a `defaultChecked` attribute and is part of a form, it will revert to that value instead of `false` when the form is reset. Note that for the initial render the value of the binding takes precedence unless it is `null` or `undefined`.

```svelte
<script>
	let checked = $state(true);
</script>

<form>
	<input type="checkbox" bind:checked defaultChecked={true}>
	<input type="reset" value="Reset">
</form>
```

> [!NOTE] Use `bind:group` for radio inputs instead of `bind:checked`.


## `<input bind:indeterminate>`

Checkboxes can be in an [indeterminate](https://developer.mozilla.org/en-US/docs/Web/API/HTMLInputElement/indeterminate) state, independently of whether they are checked or unchecked:

```svelte
<script>
	let checked = $state(false);
	let indeterminate = $state(true);
</script>

<form>
	<input type="checkbox" bind:checked bind:indeterminate>

	{#if indeterminate}
		waiting...
	{:else if checked}
		checked
	{:else}
		unchecked
	{/if}
</form>
```


## `<input bind:group>`

Inputs that work together can use `bind:group`:

<!-- codeblock:start {"title":"bind:group"} -->
```svelte
<!--- file: App.svelte --->
<script>
	let tortilla = $state('Plain');

	/** @type {string[]} */
	let fillings = $state([]);
</script>

<h1>Customize your burrito</h1>

<!-- grouped radio inputs are mutually exclusive -->
<label><input type="radio" bind:group={tortilla} value="Plain" /> Plain</label>
<label><input type="radio" bind:group={tortilla} value="Whole wheat" /> Whole wheat</label>
<label><input type="radio" bind:group={tortilla} value="Spinach" /> Spinach</label>

<!-- grouped checkbox inputs populate an array -->
<label><input type="checkbox" bind:group={fillings} value="Rice" /> Rice</label>
<label><input type="checkbox" bind:group={fillings} value="Beans" /> Beans</label>
<label><input type="checkbox" bind:group={fillings} value="Cheese" /> Cheese</label>
<label><input type="checkbox" bind:group={fillings} value="Guac (extra)" /> Guac (extra)</label>

<p>Tortilla: {tortilla}</p>
<p>Fillings: {fillings.join(', ') || 'None'}</p>

<style>
	label {
		display: block;
	}
</style>
```
<!-- codeblock:end -->

> [!NOTE] `bind:group` only works if the inputs are in the same Svelte component.


## `<input bind:files>`

On `<input>` elements with `type="file"`, you can use `bind:files` to get the [`FileList` of selected files](https://developer.mozilla.org/en-US/docs/Web/API/FileList). When you want to update the files programmatically, you always need to use a `FileList` object. Currently `FileList` objects cannot be constructed directly, so you need to create a new [`DataTransfer`](https://developer.mozilla.org/en-US/docs/Web/API/DataTransfer) object and get `files` from there.

```svelte
<script>
	let files = $state();

	function clear() {
		files = new DataTransfer().files; // null or undefined does not work
	}
</script>

<label for="avatar">Upload a picture:</label>
<input accept="image/png, image/jpeg" bind:files id="avatar" name="avatar" type="file" />
<button onclick={clear}>clear</button>
```

`FileList` objects also cannot be modified, so if you want to e.g. delete a single file from the list, you need to create a new `DataTransfer` object and add the files you want to keep.

> [!NOTE] `DataTransfer` may not be available in server-side JS runtimes. Leaving the state that is bound to `files` uninitialized prevents potential errors if components are server-side rendered.


## `<select bind:value>`

A `<select>` value binding corresponds to the `value` property on the selected `<option>`, which can be any value (not just strings, as is normally the case in the DOM).

```svelte
<select bind:value={selected}>
	<option value={a}>a</option>
	<option value={b}>b</option>
	<option value={c}>c</option>
</select>
```

A `<select multiple>` element behaves similarly to a checkbox group. The bound variable is an array with an entry corresponding to the `value` property of each selected `<option>`.

```svelte
<select multiple bind:value={fillings}>
	<option value="Rice">Rice</option>
	<option value="Beans">Beans</option>
	<option value="Cheese">Cheese</option>
	<option value="Guac (extra)">Guac (extra)</option>
</select>
```

When the value of an `<option>` matches its text content, the attribute can be omitted.

```svelte
<select multiple bind:value={fillings}>
	<option>Rice</option>
	<option>Beans</option>
	<option>Cheese</option>
	<option>Guac (extra)</option>
</select>
```

You can give the `<select>` a default value by adding a `selected` attribute to the `<option>` (or options, in the case of `<select multiple>`) that should be initially selected. If the `<select>` is part of a form, it will revert to that selection when the form is reset. Note that for the initial render the value of the binding takes precedence if it's not `undefined`.

```svelte
<select bind:value={selected}>
	<option value={a}>a</option>
	<option value={b} selected>b</option>
	<option value={c}>c</option>
</select>
```


## `<audio>`

`<audio>` elements have their own set of bindings — five two-way ones...

- [`currentTime`](https://developer.mozilla.org/en-US/docs/Web/API/HTMLMediaElement/currentTime)
- [`playbackRate`](https://developer.mozilla.org/en-US/docs/Web/API/HTMLMediaElement/playbackRate)
- [`paused`](https://developer.mozilla.org/en-US/docs/Web/API/HTMLMediaElement/paused)
- [`volume`](https://developer.mozilla.org/en-US/docs/Web/API/HTMLMediaElement/volume)
- [`muted`](https://developer.mozilla.org/en-US/docs/Web/API/HTMLMediaElement/muted)

...and six readonly ones:

- [`duration`](https://developer.mozilla.org/en-US/docs/Web/API/HTMLMediaElement/duration)
- [`buffered`](https://developer.mozilla.org/en-US/docs/Web/API/HTMLMediaElement/buffered)
- [`seekable`](https://developer.mozilla.org/en-US/docs/Web/API/HTMLMediaElement/seekable)
- [`seeking`](https://developer.mozilla.org/en-US/docs/Web/API/HTMLMediaElement/seeking_event)
- [`ended`](https://developer.mozilla.org/en-US/docs/Web/API/HTMLMediaElement/ended)
- [`readyState`](https://developer.mozilla.org/en-US/docs/Web/API/HTMLMediaElement/readyState)
- [`played`](https://developer.mozilla.org/en-US/docs/Web/API/HTMLMediaElement/played)

```svelte
<audio src={clip} bind:duration bind:currentTime bind:paused></audio>
```


## `<video>`

`<video>` elements have all the same bindings as [`<audio>`](#audio) elements, plus readonly [`videoWidth`](https://developer.mozilla.org/en-US/docs/Web/API/HTMLVideoElement/videoWidth) and [`videoHeight`](https://developer.mozilla.org/en-US/docs/Web/API/HTMLVideoElement/videoHeight) bindings.


## `<img>`

`<img>` elements have two readonly bindings:

- [`naturalWidth`](https://developer.mozilla.org/en-US/docs/Web/API/HTMLImageElement/naturalWidth)
- [`naturalHeight`](https://developer.mozilla.org/en-US/docs/Web/API/HTMLImageElement/naturalHeight)


## `<details bind:open>`

`<details>` elements support binding to the `open` property.

```svelte
<details bind:open={isOpen}>
	<summary>How do you comfort a JavaScript bug?</summary>
	<p>You console it.</p>
</details>
```


## `window` and `document`

To bind to properties of `window` and `document`, see [`<svelte:window>`](svelte-window) and [`<svelte:document>`](svelte-document).


## Contenteditable bindings

Elements with the `contenteditable` attribute support the following bindings:

- [`innerHTML`](https://developer.mozilla.org/en-US/docs/Web/API/Element/innerHTML)
- [`innerText`](https://developer.mozilla.org/en-US/docs/Web/API/HTMLElement/innerText)
- [`textContent`](https://developer.mozilla.org/en-US/docs/Web/API/Node/textContent)

> [!NOTE] There are [subtle differences between `innerText` and `textContent`](https://developer.mozilla.org/en-US/docs/Web/API/Node/textContent#differences_from_innertext).

<!-- for some reason puts the comment and html on same line -->
<!-- prettier-ignore -->
```svelte
<div contenteditable="true" bind:innerHTML={html}></div>
```


## Dimensions

All visible elements have the following readonly bindings, measured with a `ResizeObserver`:

- [`clientWidth`](https://developer.mozilla.org/en-US/docs/Web/API/Element/clientWidth)
- [`clientHeight`](https://developer.mozilla.org/en-US/docs/Web/API/Element/clientHeight)
- [`offsetWidth`](https://developer.mozilla.org/en-US/docs/Web/API/HTMLElement/offsetWidth)
- [`offsetHeight`](https://developer.mozilla.org/en-US/docs/Web/API/HTMLElement/offsetHeight)
- [`contentRect`](https://developer.mozilla.org/en-US/docs/Web/API/ResizeObserverEntry/contentRect)
- [`contentBoxSize`](https://developer.mozilla.org/en-US/docs/Web/API/ResizeObserverEntry/contentBoxSize)
- [`borderBoxSize`](https://developer.mozilla.org/en-US/docs/Web/API/ResizeObserverEntry/borderBoxSize)
- [`devicePixelContentBoxSize`](https://developer.mozilla.org/en-US/docs/Web/API/ResizeObserverEntry/devicePixelContentBoxSize)

```svelte
<div bind:offsetWidth={width} bind:offsetHeight={height}>
	<Chart {width} {height} />
</div>
```

> [!NOTE] `display: inline` elements do not have a width or height (except for elements with 'intrinsic' dimensions, like `<img>` and `<canvas>`), and cannot be observed with a `ResizeObserver`. You will need to change the `display` style of these elements to something else, such as `inline-block`. Note that CSS transformations do not trigger `ResizeObserver` callbacks.


## bind:this

```svelte
<!--- copy: false --->
bind:this={dom_node}
```

To get a reference to a DOM node, use `bind:this`. The value will be `undefined` until the component is mounted — in other words, you should read it inside an effect or an event handler, but not during component initialisation:

```svelte
<script>
	/** @type {HTMLCanvasElement} */
	let canvas;

	$effect(() => {
		const ctx = canvas.getContext('2d');
		drawStuff(ctx);
	});
</script>

<canvas bind:this={canvas}></canvas>
```

Components also support `bind:this`, allowing you to interact with component instances programmatically.

```svelte
<!--- file: App.svelte --->
<ShoppingCart bind:this={cart} />

<button onclick={() => cart.empty()}> Empty shopping cart </button>
```

```svelte
<!--- file: ShoppingCart.svelte --->
<script>
	// All instance exports are available on the instance object
	export function empty() {
		// ...
	}
</script>
```

> [!NOTE] In case of using [the function bindings](#Function-bindings), the getter is required to ensure that the correct value is nullified on component or element destruction.


## bind:_property_ for components

```svelte
bind:property={variable}
```

You can bind to component props using the same syntax as for elements.

```svelte
<Keypad bind:value={pin} />
```

While Svelte props are reactive without binding, that reactivity only flows downward into the component by default. Using `bind:property` allows changes to the property from within the component to flow back up out of the component.

To mark a property as bindable, use the [`$bindable`]($bindable) rune:

```svelte
<script>
	let { readonlyProperty, bindableProperty = $bindable() } = $props();
</script>
```

Declaring a property as bindable means it _can_ be used using `bind:`, not that it _must_ be used using `bind:`.

Bindable properties can have a fallback value:

```svelte
<script>
	let { bindableProperty = $bindable('fallback value') } = $props();
</script>
```

This fallback value _only_ applies when the property is _not_ bound. When the property is bound and a fallback value is present, the parent is expected to provide a value other than `undefined`, else a runtime error is thrown. This prevents hard-to-reason-about situations where it's unclear which value should apply.


## use:

> [!NOTE]
> In Svelte 5.29 and newer, consider using [attachments](@attach) instead, as they are more flexible and composable.

Actions are functions that are called when an element is mounted. They are added with the `use:` directive, and will typically use an `$effect` so that they can reset any state when the element is unmounted:

```svelte
<!--- file: App.svelte --->
<script>
	/** @type {import('svelte/action').Action} */
	function myaction(node) {
		// the node has been mounted in the DOM

		$effect(() => {
			// setup goes here

			return () => {
				// teardown goes here
			};
		});
	}
</script>

<div use:myaction>...</div>
```

An action can be called with an argument:

```svelte
<!--- file: App.svelte --->
<script>
	/** @type {import('svelte/action').Action} */
	function myaction(node, +++data+++) {
		// ...
	}
</script>

<div use:myaction={+++data+++}>...</div>
```

The action is only called once (but not during server-side rendering) — it will _not_ run again if the argument changes.

> [!LEGACY]
> Prior to the `$effect` rune, actions could return an object with `update` and `destroy` methods, where `update` would be called with the latest value of the argument if it changed. Using effects is preferred.


## Typing

The `Action` interface receives three optional type arguments — a node type (which can be `Element`, if the action applies to everything), a parameter, and any custom event handlers created by the action:

```svelte
<!--- file: App.svelte --->
<script>
	/**
	 * @type {import('svelte/action').Action<
	 * 	HTMLDivElement,
	 * 	undefined,
	 * 	{
	 * 		onswiperight: (e: CustomEvent) => void;
	 * 		onswipeleft: (e: CustomEvent) => void;
	 * 		// ...
	 * 	}
	 * >}
	 */
	function gestures(node) {
		$effect(() => {
			// ...
			node.dispatchEvent(new CustomEvent('swipeleft'));

			// ...
			node.dispatchEvent(new CustomEvent('swiperight'));
		});
	}
</script>

<div
	use:gestures
	onswipeleft={next}
	onswiperight={prev}
>...</div>
```


## transition:

A _transition_ is triggered by an element entering or leaving the DOM as a result of a state change.

When a block (such as an `{#if ...}` block) is transitioning out, all elements inside it, including those that do not have their own transitions, are kept in the DOM until every transition in the block has been completed.

The `transition:` directive indicates a _bidirectional_ transition, which means it can be smoothly reversed while the transition is in progress.

```svelte
<script>
	+++import { fade } from 'svelte/transition';+++

	let visible = $state(false);
</script>

<button onclick={() => visible = !visible}>toggle</button>

{#if visible}
	<div +++transition:fade+++>fades in and out</div>
{/if}
```


## Local vs global

Transitions are local by default. Local transitions only play when the block they belong to is created or destroyed, _not_ when parent blocks are created or destroyed.

```svelte
{#if x}
	{#if y}
		<p transition:fade>fades in and out only when y changes</p>

		<p transition:fade|global>fades in and out when x or y change</p>
	{/if}
{/if}
```


## Built-in transitions

A selection of built-in transitions can be imported from the [`svelte/transition`](svelte-transition) module.


## Transition parameters

Transitions can have parameters.

(The double `{{curlies}}` aren't a special syntax; this is an object literal inside an expression tag.)

```svelte
{#if visible}
	<div transition:fade={{ duration: 2000 }}>fades in and out over two seconds</div>
{/if}
```


## Custom transition functions

```js
/// copy: false
// @noErrors
transition = (node: HTMLElement, params: any, options: { direction: 'in' | 'out' | 'both' }) => {
	delay?: number,
	duration?: number,
	easing?: (t: number) => number,
	css?: (t: number, u: number) => string,
	tick?: (t: number, u: number) => void
}
```

Transitions can use custom functions. If the returned object has a `css` function, Svelte will generate keyframes for a [web animation](https://developer.mozilla.org/en-US/docs/Web/API/Web_Animations_API).

The `t` argument passed to `css` is a value between `0` and `1` after the `easing` function has been applied. _In_ transitions run from `0` to `1`, _out_ transitions run from `1` to `0` — in other words, `1` is the element's natural state, as though no transition had been applied. The `u` argument is equal to `1 - t`.

The function is called repeatedly _before_ the transition begins, with different `t` and `u` arguments.

```svelte
<!--- file: App.svelte --->
<script>
	import { elasticOut } from 'svelte/easing';

	/** @type {boolean} */
	export let visible;

	/**
	 * @param {HTMLElement} node
	 * @param {{ delay?: number, duration?: number, easing?: (t: number) => number }} params
	 */
	function whoosh(node, params) {
		const existingTransform = getComputedStyle(node).transform.replace('none', '');

		return {
			delay: params.delay || 0,
			duration: params.duration || 400,
			easing: params.easing || elasticOut,
			css: (t, u) => `transform: ${existingTransform} scale(${t})`
		};
	}
</script>

{#if visible}
	<div in:whoosh>whooshes in</div>
{/if}
```

A custom transition function can also return a `tick` function, which is called _during_ the transition with the same `t` and `u` arguments.

> [!NOTE] If it's possible to use `css` instead of `tick`, do so — web animations can run off the main thread, preventing jank on slower devices.

```svelte
<!--- file: App.svelte --->
<script>
	export let visible = false;

	/**
	 * @param {HTMLElement} node
	 * @param {{ speed?: number }} params
	 */
	function typewriter(node, { speed = 1 }) {
		const valid = node.childNodes.length === 1 && node.childNodes[0].nodeType === Node.TEXT_NODE;

		if (!valid) {
			throw new Error(`This transition only works on elements with a single text node child`);
		}

		const text = node.textContent;
		const duration = text.length / (speed * 0.01);

		return {
			duration,
			tick: (t) => {
				const i = ~~(text.length * t);
				node.textContent = text.slice(0, i);
			}
		};
	}
</script>

{#if visible}
	<p in:typewriter={{ speed: 1 }}>The quick brown fox jumps over the lazy dog</p>
{/if}
```

If a transition returns a function instead of a transition object, the function will be called in the next microtask. This allows multiple transitions to coordinate, making [crossfade effects](/tutorial/deferred-transitions) possible.

Transition functions also receive a third argument, `options`, which contains information about the transition.

Available values in the `options` object are:

- `direction` - one of `in`, `out`, or `both` depending on the type of transition


## Transition events

An element with transitions will dispatch the following events in addition to any standard DOM events:

- `introstart`
- `introend`
- `outrostart`
- `outroend`

```svelte
{#if visible}
	<p
		transition:fly={{ y: 200, duration: 2000 }}
		onintrostart={() => (status = 'intro started')}
		onoutrostart={() => (status = 'outro started')}
		onintroend={() => (status = 'intro ended')}
		onoutroend={() => (status = 'outro ended')}
	>
		Flies in and out
	</p>
{/if}
```


## in: and out:

The `in:` and `out:` directives are identical to [`transition:`](transition), except that the resulting transitions are not bidirectional — an `in` transition will continue to 'play' alongside the `out` transition, rather than reversing, if the block is outroed while the transition is in progress. If an out transition is aborted, transitions will restart from scratch.

```svelte
<script>
  import { fade, fly } from 'svelte/transition';

  let visible = $state(false);
</script>

<label>
  <input type="checkbox" bind:checked={visible}>
  visible
</label>

{#if visible}
	<div in:fly={{ y: 200 }} out:fade>flies in, fades out</div>
{/if}
```


## animate:

An animation is triggered when the contents of a [keyed each block](each#Keyed-each-blocks) are re-ordered. Animations do not run when an element is added or removed, only when the index of an existing data item within the each block changes. Animate directives must be on an element that is an _immediate_ child of a keyed each block.

Animations can be used with Svelte's [built-in animation functions](svelte-animate) or [custom animation functions](#Custom-animation-functions).

```svelte
<!-- When `list` is reordered the animation will run -->
{#each list as item, index (item)}
	<li animate:flip>{item}</li>
{/each}
```


## Animation Parameters

As with actions and transitions, animations can have parameters.

(The double `{{curlies}}` aren't a special syntax; this is an object literal inside an expression tag.)

```svelte
{#each list as item, index (item)}
	<li animate:flip={{ delay: 500 }}>{item}</li>
{/each}
```


## Custom animation functions

```js
/// copy: false
// @noErrors
animation = (node: HTMLElement, { from: DOMRect, to: DOMRect } , params: any) => {
	delay?: number,
	duration?: number,
	easing?: (t: number) => number,
	css?: (t: number, u: number) => string,
	tick?: (t: number, u: number) => void
}
```

Animations can use custom functions that provide the `node`, an `animation` object and any `parameters` as arguments. The `animation` parameter is an object containing `from` and `to` properties each containing a [DOMRect](https://developer.mozilla.org/en-US/docs/Web/API/DOMRect#Properties) describing the geometry of the element in its `start` and `end` positions. The `from` property is the DOMRect of the element in its starting position, and the `to` property is the DOMRect of the element in its final position after the list has been reordered and the DOM updated.

If the returned object has a `css` method, Svelte will create a [web animation](https://developer.mozilla.org/en-US/docs/Web/API/Web_Animations_API) that plays on the element.

The `t` argument passed to `css` is a value that goes from `0` and `1` after the `easing` function has been applied. The `u` argument is equal to `1 - t`.

The function is called repeatedly _before_ the animation begins, with different `t` and `u` arguments.

<!-- TODO: Types -->

```svelte
<!--- file: App.svelte --->
<script>
	import { cubicOut } from 'svelte/easing';

	/**
	 * @param {HTMLElement} node
	 * @param {{ from: DOMRect; to: DOMRect }} states
	 * @param {any} params
	 */
	function whizz(node, { from, to }, params) {
		const dx = from.left - to.left;
		const dy = from.top - to.top;

		const d = Math.sqrt(dx * dx + dy * dy);

		return {
			delay: 0,
			duration: Math.sqrt(d) * 120,
			easing: cubicOut,
			css: (t, u) => `transform: translate(${u * dx}px, ${u * dy}px) rotate(${t * 360}deg);`
		};
	}
</script>

{#each list as item, index (item)}
	<div animate:whizz>{item}</div>
{/each}
```

A custom animation function can also return a `tick` function, which is called _during_ the animation with the same `t` and `u` arguments.

> [!NOTE] If it's possible to use `css` instead of `tick`, do so — web animations can run off the main thread, preventing jank on slower devices.

```svelte
<!--- file: App.svelte --->
<script>
	import { cubicOut } from 'svelte/easing';

	/**
	 * @param {HTMLElement} node
	 * @param {{ from: DOMRect; to: DOMRect }} states
	 * @param {any} params
	 */
	function whizz(node, { from, to }, params) {
		const dx = from.left - to.left;
		const dy = from.top - to.top;

		const d = Math.sqrt(dx * dx + dy * dy);

		return {
			delay: 0,
			duration: Math.sqrt(d) * 120,
			easing: cubicOut,
			tick: (t, u) => Object.assign(node.style, { color: t > 0.5 ? 'Pink' : 'Blue' })
		};
	}
</script>

{#each list as item, index (item)}
	<div animate:whizz>{item}</div>
{/each}
```


## style:

The `style:` directive provides a shorthand for setting multiple styles on an element.

```svelte
<!-- These are equivalent -->
<div style:color="red">...</div>
<div style="color: red;">...</div>
```

The value can contain arbitrary expressions:

```svelte
<div style:color={myColor}>...</div>
```

The shorthand form is allowed:

```svelte
<div style:color>...</div>
```

Multiple styles can be set on a single element:

```svelte
<div style:color style:width="12rem" style:background-color={darkMode ? 'black' : 'white'}>...</div>
```

To mark a style as important, use the `|important` modifier:

```svelte
<div style:color|important="red">...</div>
```

When `style:` directives are combined with `style` attributes, the directives will take precedence,
even over `!important` properties:

```svelte
<div style:color="red" style="color: blue">This will be red</div>
<div style:color="red" style="color: blue !important">This will still be red</div>
```

You can set CSS custom properties:

```svelte
<div style:--columns={columns}>...</div>
```


## class

There are two ways to set classes on elements: the `class` attribute, and the `class:` directive.


## Attributes

Primitive values are treated like any other attribute:

```svelte
<div class={large ? 'large' : 'small'}>...</div>
```

> [!NOTE]
> For historical reasons, falsy values (like `false` and `NaN`) are stringified (`class="false"`), though `class={undefined}` (or `null`) cause the attribute to be omitted altogether. In a future version of Svelte, all falsy values will cause `class` to be omitted.


## Objects and arrays

Since Svelte 5.16, `class` can be an object or array, and is converted to a string using [clsx](https://github.com/lukeed/clsx).

If the value is an object, the truthy keys are added:

```svelte
<script>
	let { cool } = $props();
</script>

<!-- results in `class="cool"` if `cool` is truthy,
     `class="lame"` otherwise -->
<div class={{ cool, lame: !cool }}>...</div>
```

If the value is an array, the truthy values are combined:

```svelte
<!-- if `faded` and `large` are both truthy, results in
     `class="saturate-0 opacity-50 scale-200"` -->
<div class={[faded && 'saturate-0 opacity-50', large && 'scale-200']}>...</div>
```

Note that whether we're using the array or object form, we can set multiple classes simultaneously with a single condition, which is particularly useful if you're using things like Tailwind.

Arrays can contain arrays and objects, and clsx will flatten them. This is useful for combining local classes with props, for example:

```svelte
<!--- file: Button.svelte --->
<script>
	let props = $props();
</script>

<button {...props} class={['cool-button', props.class]}>
	{@render props.children?.()}
</button>
```

The user of this component has the same flexibility to use a mixture of objects, arrays and strings:

```svelte
<!--- file: App.svelte --->
<script>
	import Button from './Button.svelte';
	let useTailwind = $state(false);
</script>

<Button
	onclick={() => useTailwind = true}
	class={{ 'bg-blue-700 sm:w-1/2': useTailwind }}
>
	Accept the inevitability of Tailwind
</Button>
```

Since Svelte 5.19, Svelte also exposes the `ClassValue` type, which is the type of value that the `class` attribute on elements accept. This is useful if you want to use a type-safe class name in component props:

```svelte
<script lang="ts">
	import type { ClassValue } from 'svelte/elements';

	const props: { class: ClassValue } = $props();
</script>

<div class={['original', props.class]}>...</div>
```


## The `class:` directive

Prior to Svelte 5.16, the `class:` directive was the most convenient way to set classes on elements conditionally.

```svelte
<!-- These are equivalent -->
<div class={{ cool, lame: !cool }}>...</div>
<div class:cool={cool} class:lame={!cool}>...</div>
```

As with other directives, we can use a shorthand when the name of the class coincides with the value:

```svelte
<div class:cool class:lame={!cool}>...</div>
```

> [!NOTE] Unless you're using an older version of Svelte, consider avoiding `class:`, since the attribute is more powerful and composable.


## await

As of Svelte 5.36, you can use the `await` keyword inside your components in three places where it was previously unavailable:

- at the top level of your component's `<script>`
- inside `$derived(...)` declarations
- inside your markup

This feature is currently experimental, and you must opt in by adding the `experimental.async` option wherever you [configure](/docs/kit/configuration) Svelte, usually `svelte.config.js`:

```js
/// file: svelte.config.js
export default {
	compilerOptions: {
		experimental: {
			async: true
		}
	}
};
```

The experimental flag will be removed in Svelte 6.


## Synchronized updates

When an `await` expression depends on a particular piece of state, changes to that state will not be reflected in the UI until the asynchronous work has completed, so that the UI is not left in an inconsistent state. In other words, in an example like this...

<!-- codeblock:start {"title":"Synchronized updates"} -->
```svelte
<!--- file: App.svelte --->
<script>
	let a = $state(1);
	let b = $state(2);

	async function add(a, b) {
		await new Promise((f) => setTimeout(f, 500)); // artificial delay
		return a + b;
	}
</script>

<input type="number" bind:value={a}>
<input type="number" bind:value={b}>

<p>{a} + {b} = {await add(a, b)}</p>
```
<!-- codeblock:end -->

...if you increment `a`, the contents of the `<p>` will _not_ immediately update to read this —

```html
<p>2 + 2 = 3</p>
```

— instead, the text will update to `2 + 2 = 4` when `add(a, b)` resolves.

Updates can overlap — a fast update will be reflected in the UI while an earlier slow update is still ongoing.


## Concurrency

Svelte will do as much asynchronous work as it can in parallel. For example if you have two `await` expressions in your markup...

```svelte
<p>{await one(x)}</p>
<p>{await two(y)}</p>
```

...both functions will run at the same time, as they are independent expressions, even though they are _visually_ sequential.

This does not apply to sequential `await` expressions inside your `<script>` or inside async functions — these run like any other asynchronous JavaScript. An exception is that independent `$derived` expressions will update independently, even though they will run sequentially when they are first created:

```js
/** @param {number} x */
async function one(x) { return x; }
/** @param {number} y */
async function two(y) { return y; }
let x = $state(1);
let y = $state(2);
// ---cut---
// `b` will not be created until `a` has resolved,
// but once created they will update independently
// even if `x` and `y` update simultaneously
let a = $derived(await one(x));
let b = $derived(await two(y));
```

> [!NOTE] If you write code like this, expect Svelte to give you an [`await_waterfall`](runtime-warnings#Client-warnings-await_waterfall) warning


## Indicating loading states

To render placeholder UI, you can wrap content in a `<svelte:boundary>` with a [`pending`](svelte-boundary#Properties-pending) snippet. This will be shown when the boundary is first created, but not for subsequent updates, which are globally coordinated.

After the contents of a boundary have resolved for the first time and have replaced the `pending` snippet, you can detect subsequent async work with [`$effect.pending()`]($effect#$effect.pending). This is what you would use to display a "we're asynchronously validating your input" spinner next to a form field, for example.

You can also use [`settled()`](svelte#settled) to get a promise that resolves when the current update is complete:

```js
let color = 'red';
let answer = -1;
let updating = false;
// ---cut---
import { tick, settled } from 'svelte';

async function onclick() {
	updating = true;

	// without this, the change to `updating` will be
	// grouped with the other changes, meaning it
	// won't be reflected in the UI
	await tick();

	color = 'octarine';
	answer = 42;

	await settled();

	// any updates affected by `color` or `answer`
	// have now been applied
	updating = false;
}
```


## Error handling

Errors in `await` expressions will bubble to the nearest [error boundary](svelte-boundary).


## Server-side rendering

Svelte supports asynchronous server-side rendering (SSR) with the `render(...)` API. To use it, simply await the return value:

```js
/// file: server.js
import { render } from 'svelte/server';
import App from './App.svelte';

const { head, body } = +++await+++ render(App);
```

> [!NOTE] If you're using a framework like SvelteKit, this is done on your behalf.

If a `<svelte:boundary>` with a `pending` snippet is encountered during SSR, that snippet will be rendered while the rest of the content is ignored. All `await` expressions encountered outside boundaries with `pending` snippets will resolve and render their contents prior to `await render(...)` returning.

> [!NOTE] In the future, we plan to add a streaming implementation that renders the content in the background.


## Forking

The [`fork(...)`](svelte#fork) API, added in 5.42, makes it possible to run `await` expressions that you _expect_ to happen in the near future. This is mainly intended for frameworks like SvelteKit to implement preloading when (for example) users signal an intent to navigate.

```svelte
<script>
	import { fork } from 'svelte';
	import Menu from './Menu.svelte';

	let open = $state(false);

	/** @type {import('svelte').Fork | null} */
	let pending = null;

	function preload() {
		pending ??= fork(() => {
			open = true;
		});
	}

	function discard() {
		pending?.discard();
		pending = null;
	}
</script>

<button
	onfocusin={preload}
	onfocusout={discard}
	onpointerenter={preload}
	onpointerleave={discard}
	onclick={() => {
		pending?.commit();
		pending = null;

		// in case `pending` didn't exist
		// (if it did, this is a no-op)
		open = true;
	}}
>open menu</button>

{#if open}
	<!-- any async work inside this component will start
	     as soon as the fork is created -->
	<Menu onclose={() => open = false} />
{/if}
```


## Caveats

As an experimental feature, the details of how `await` is handled (and related APIs like `$effect.pending()`) are subject to breaking changes outside of a semver major release, though we intend to keep such changes to a bare minimum.


## Breaking changes

Effects run in a slightly different order when the `experimental.async` option is `true`. Specifically, _block_ effects like `{#if ...}` and `{#each ...}` now run before an `$effect.pre` or `beforeUpdate` in the same component, which means that in very rare situations.


## Scoped styles

Svelte components can include a `<style>` element containing CSS that belongs to the component. This CSS is _scoped_ by default, meaning that styles will not apply to any elements on the page outside the component in question.

This works by adding a class to affected elements, which is based on a hash of the component styles (e.g. `svelte-123xyz`).

```svelte
<style>
	p {
		/* this will only affect <p> elements in this component */
		color: burlywood;
	}
</style>
```


## Specificity

Each scoped selector receives a [specificity](https://developer.mozilla.org/en-US/docs/Web/CSS/Specificity) increase of 0-1-0, as a result of the scoping class (e.g. `.svelte-123xyz`) being added to the selector. This means that (for example) a `p` selector defined in a component will take precedence over a `p` selector defined in a global stylesheet, even if the global stylesheet is loaded later.

In some cases, the scoping class must be added to a selector multiple times, but after the first occurrence it is added with `:where(.svelte-xyz123)` in order to not increase specificity further.


## Scoped keyframes

If a component defines `@keyframes`, the name is scoped to the component using the same hashing approach. Any `animation` rules in the component will be similarly adjusted:

```svelte
<style>
	.bouncy {
		animation: bounce 10s;
	}

	/* these keyframes are only accessible inside this component */
	@keyframes bounce {
		/* ... */
	}
</style>
```


## Global styles



## :global(...)

To apply styles to a single selector globally, use the `:global(...)` modifier:

```svelte
<style>
	:global(body) {
		/* applies to <body> */
		margin: 0;
	}

	div :global(strong) {
		/* applies to all <strong> elements, in any component,
		   that are inside <div> elements belonging
		   to this component */
		color: goldenrod;
	}

	p:global(.big.red) {
		/* applies to all <p> elements belonging to this component
		   with `class="big red"`, even if it is applied
		   programmatically (for example by a library) */
	}
</style>
```

If you want to make @keyframes that are accessible globally, you need to prepend your keyframe names with `-global-`.

The `-global-` part will be removed when compiled, and the keyframe will then be referenced using just `my-animation-name` elsewhere in your code.

```svelte
<style>
	@keyframes -global-my-animation-name {
		/* code goes here */
	}
</style>
```


## :global

To apply styles to a group of selectors globally, create a `:global {...}` block:

```svelte
<style>
	:global {
		/* applies to every <div> in your application */
		div { ... }

		/* applies to every <p> in your application */
		p { ... }
	}

	.a :global {
		/* applies to every `.b .c .d` element, in any component,
		   that is inside an `.a` element in this component */
		.b .c .d {...}
	}
</style>
```

> [!NOTE] The second example above could also be written as an equivalent `.a :global .b .c .d` selector, where everything after the `:global` is unscoped, though the nested form is preferred.


## Custom properties

You can pass CSS custom properties — both static and dynamic — to components:

```svelte
<Slider
	bind:value
	min={0}
	max={100}
	--track-color="black"
	--thumb-color="rgb({r} {g} {b})"
/>
```

The above code essentially desugars to this:

```svelte
<svelte-css-wrapper style="display: contents; --track-color: black; --thumb-color: rgb({r} {g} {b})">
	<Slider
		bind:value
		min={0}
		max={100}
	/>
</svelte-css-wrapper>
```

For an SVG element, it would use `<g>` instead:

```svelte
<g style="--track-color: black; --thumb-color: rgb({r} {g} {b})">
	<Slider
		bind:value
		min={0}
		max={100}
	/>
</g>
```

Inside the component, we can read these custom properties (and provide fallback values) using [`var(...)`](https://developer.mozilla.org/en-US/docs/Web/CSS/Using_CSS_custom_properties):

```svelte
<style>
	.track {
		background: var(--track-color, #aaa);
	}

	.thumb {
		background: var(--thumb-color, blue);
	}
</style>
```

You don't _have_ to specify the values directly on the component; as long as the custom properties are defined on a parent element, the component can use them. It's common to define custom properties on the `:root` element in a global stylesheet so that they apply to your entire application.

> [!NOTE] While the extra element will not affect layout, it _will_ affect any CSS selectors that (for example) use the `>` combinator to target an element directly inside the component's container.


## Nested `<style>` elements

There can only be one top-level `<style>` tag per component.

However, it is possible to have a `<style>` tag nested inside other elements or logic blocks.

In that case, the `<style>` tag will be inserted as-is into the DOM; no scoping or processing will be done on the `<style>` tag.

```svelte
<div>
	<style>
		/* this style tag will be inserted as-is */
		div {
			/* this will apply to all `<div>` elements in the DOM */
			color: red;
		}
	</style>
</div>
```


## `<svelte:boundary>`

```svelte
<svelte:boundary onerror={handler}>...</svelte:boundary>
```

> [!NOTE]
> This feature was added in 5.3.0

Boundaries allow you to 'wall off' parts of your app, so that you can:

- provide UI that should be shown when [`await`](await-expressions) expressions are first resolving
- handle errors that occur during rendering or while running effects, and provide UI that should be rendered when an error happens

If a boundary handles an error (with a `failed` snippet or `onerror` handler, or both) its existing content will be removed.

> [!NOTE] Errors occurring outside the rendering process (for example, in event handlers or after a `setTimeout` or async work) are _not_ caught by error boundaries.


## Properties

For the boundary to do anything, one or more of the following must be provided.


## `pending`

This snippet will be shown when the boundary is first created, and will remain visible until all the [`await`](await-expressions) expressions inside the boundary have resolved (demo:

```svelte
<svelte:boundary>
	<p>{await delayed('hello!')}</p>

	{#snippet pending()}
		<p>loading...</p>
	{/snippet}
</svelte:boundary>
```

The `pending` snippet will _not_ be shown for subsequent async updates — for these, you can use [`$effect.pending()`]($effect#$effect.pending).

> [!NOTE] In the [playground](/playground), your app is rendered inside a boundary with an empty pending snippet, so that you can use `await` without having to create one.



## `failed`

If a `failed` snippet is provided, it will be rendered when an error is thrown inside the boundary, with the `error` and a `reset` function that recreates the contents (demo:

```svelte
<svelte:boundary>
	<FlakyComponent />

	{#snippet failed(error, reset)}
		<button onclick={reset}>oops! try again</button>
	{/snippet}
</svelte:boundary>
```

> [!NOTE]
> As with [snippets passed to components](snippet#Passing-snippets-to-components), the `failed` snippet can be passed explicitly as a property...
>
> ```svelte
> <svelte:boundary {failed}>...</svelte:boundary>
> ```
>
> ...or implicitly by declaring it directly inside the boundary, as in the example above.


## `onerror`

If an `onerror` function is provided, it will be called with the same two `error` and `reset` arguments. This is useful for tracking the error with an error reporting service...

```svelte
<svelte:boundary onerror={(e) => report(e)}>
	...
</svelte:boundary>
```

...or using `error` and `reset` outside the boundary itself:

```svelte
<script>
	let error = $state(null);
	let reset = $state(() => {});

	function onerror(e, r) {
		error = e;
		reset = r;
	}
</script>

<svelte:boundary {onerror}>
	<FlakyComponent />
</svelte:boundary>

{#if error}
	<button onclick={() => {
		error = null;
		reset();
	}}>
		oops! try again
	</button>
{/if}
```

If an error occurs inside the `onerror` function (or if you rethrow the error), it will be handled by a parent boundary if such exists.


## Using `transformError`

By default, error boundaries have no effect on the server — if an error occurs during rendering, the render as a whole will fail.

Since 5.51 you can control this behaviour for boundaries with a `failed` snippet, by calling [`render(...)`](imperative-component-api#render) with a `transformError` function.

> [!NOTE] If you're using Svelte via a framework such as SvelteKit, you most likely don't have direct access to the `render(...)` call — the framework must configure `transformError` on your behalf. SvelteKit will add support for this in the near future, via the [`handleError`](../kit/hooks#Shared-hooks-handleError) hook.

The `transformError` function must return a JSON-stringifiable object which will be used to render the `failed` snippet. This object will be serialized and used to hydrate the snippet in the browser:

```js
// @errors: 1005
import { render } from 'svelte/server';
import App from './App.svelte';

const { head, body } = await render(App, {
	transformError: (error) => {
		// log the original error, with the stack trace...
		console.error(error);

		// ...and return a sanitized user-friendly error
		// to display in the `failed` snippet
		return {
			message: 'An error occurred!'
		};
	};
});
```

If `transformError` throws (or rethrows) an error, `render(...)` as a whole will fail with that error.

> [!NOTE] Errors that occur during server-side rendering can contain sensitive information in the `message` and `stack`. It's recommended to redact these rather than sending them unaltered to the browser.

If the boundary has an `onerror` handler, it will be called upon hydration with the deserialized error object.

The [`mount`](imperative-component-api#mount) and [`hydrate`](imperative-component-api#hydrate) functions also accept a `transformError` option, which defaults to the identity function. As with `render`, this function transforms a render-time error before it is passed to a `failed` snippet or `onerror` handler.


## `<svelte:window>`

```svelte
<svelte:window onevent={handler} />
```

```svelte
<svelte:window bind:prop={value} />
```

The `<svelte:window>` element allows you to add event listeners to the `window` object without worrying about removing them when the component is destroyed, or checking for the existence of `window` when server-side rendering.

This element may only appear at the top level of your component — it cannot be inside a block or element.

```svelte
<script>
	function handleKeydown(event) {
		alert(`pressed the ${event.key} key`);
	}
</script>

<svelte:window onkeydown={handleKeydown} />
```

You can also bind to the following properties:

- `innerWidth`
- `innerHeight`
- `outerWidth`
- `outerHeight`
- `scrollX`
- `scrollY`
- `online` — an alias for `window.navigator.onLine`
- `devicePixelRatio`

All except `scrollX` and `scrollY` are readonly.

```svelte
<svelte:window bind:scrollY={y} />
```

> [!NOTE] Note that the page will not be scrolled to the initial value to avoid accessibility issues. Only subsequent changes to the bound variable of `scrollX` and `scrollY` will cause scrolling. If you have a legitimate reason to scroll when the component is rendered, call `scrollTo()` in an `$effect`.


## `<svelte:document>`

```svelte
<svelte:document onevent={handler} />
```

```svelte
<svelte:document bind:prop={value} />
```

Similarly to `<svelte:window>`, this element allows you to add listeners to events on `document`, such as `visibilitychange`, which don't fire on `window`. It also lets you use [attachments](@attach) on `document`.

As with `<svelte:window>`, this element may only appear the top level of your component and must never be inside a block or element.

```svelte
<svelte:document onvisibilitychange={handleVisibilityChange} {@attach someAttachment} />
```

You can also bind to the following properties:

- `activeElement`
- `fullscreenElement`
- `pointerLockElement`
- `visibilityState`

All are readonly.


## `<svelte:body>`

```svelte
<svelte:body onevent={handler} />
```

Similarly to `<svelte:window>`, this element allows you to add listeners to events on `document.body`, such as `mouseenter` and `mouseleave`, which don't fire on `window`. It also lets you use [actions](use) on the `<body>` element.

As with `<svelte:window>` and `<svelte:document>`, this element may only appear at the top level of your component and must never be inside a block or element.

```svelte
<svelte:body onmouseenter={handleMouseenter} onmouseleave={handleMouseleave} use:someAction />
```


## `<svelte:head>`

```svelte
<svelte:head>...</svelte:head>
```

This element makes it possible to insert elements into `document.head`. During server-side rendering, `head` content is exposed separately to the main `body` content.

As with `<svelte:window>`, `<svelte:document>` and `<svelte:body>`, this element may only appear at the top level of your component and must never be inside a block or element.

```svelte
<svelte:head>
	<title>Hello world!</title>
	<meta name="description" content="This is where the description goes for SEO" />
</svelte:head>
```


## `<svelte:element>`

```svelte
<svelte:element this={expression} />
```

The `<svelte:element>` element lets you render an element that is unknown at author time, for example because it comes from a CMS. Any properties and event listeners present will be applied to the element.

The only supported binding is `bind:this`, since Svelte's built-in bindings do not work with generic elements.

If `this` has a nullish value, the element and its children will not be rendered.

If `this` is the name of a [void element](https://developer.mozilla.org/en-US/docs/Glossary/Void_element) (e.g., `br`) and `<svelte:element>` has child elements, a runtime error will be thrown in development mode:

```svelte
<script>
	let tag = $state('hr');
</script>

<svelte:element this={tag}>
	This text cannot appear inside an hr element
</svelte:element>
```

Svelte tries its best to infer the correct namespace from the element's surroundings, but it's not always possible. You can make it explicit with an `xmlns` attribute:

```svelte
<svelte:element this={tag} xmlns="http://www.w3.org/2000/svg" />
```

`this` needs to be a valid DOM element tag, things like `#text` or `svelte:head` will not work.


## `<svelte:options>`

```svelte
<svelte:options option={value} />
```

The `<svelte:options>` element provides a place to specify per-component compiler options, which are detailed in the [compiler section](svelte-compiler#compile). The possible options are:

- `runes={true}` — forces a component into _runes mode_ (see the [Legacy APIs](legacy-overview) section)
- `runes={false}` — forces a component into _legacy mode_
- `namespace="..."` — the namespace where this component will be used, can be "html" (the default), "svg" or "mathml"
- `customElement={...}` — the [options](custom-elements#Component-options) to use when compiling this component as a custom element. If a string is passed, it is used as the `tag` option
- `css="injected"` — the component will inject its styles inline: During server-side rendering, it's injected as a `<style>` tag in the `head`, during client side rendering, it's loaded via JavaScript

> [!LEGACY] Deprecated options
> Svelte 4 also included the following options. They are deprecated in Svelte 5 and non-functional in runes mode.
>
> - `immutable={true}` — you never use mutable data, so the compiler can do simple referential equality checks to determine if values have changed
> - `immutable={false}` — the default. Svelte will be more conservative about whether or not mutable objects have changed
> - `accessors={true}` — adds getters and setters for the component's props
> - `accessors={false}` — the default

```svelte
<svelte:options customElement="my-custom-element" />
```


## Stores

<!-- - how to use
- how to write
- TODO should the details for the store methods belong to the reference section? -->

A _store_ is an object that allows reactive access to a value via a simple _store contract_. The [`svelte/store` module](../svelte-store) contains minimal store implementations which fulfil this contract.

Any time you have a reference to a store, you can access its value inside a component by prefixing it with the `$` character. This causes Svelte to declare the prefixed variable, subscribe to the store at component initialisation and unsubscribe when appropriate.

Assignments to `$`-prefixed variables require that the variable be a writable store, and will result in a call to the store's `.set` method.

Note that the store must be declared at the top level of the component — not inside an `if` block or a function, for example.

Local variables (that do not represent store values) must _not_ have a `$` prefix.

```svelte
<script>
	import { writable } from 'svelte/store';

	const count = writable(0);
	console.log($count); // logs 0

	count.set(1);
	console.log($count); // logs 1

	$count = 2;
	console.log($count); // logs 2
</script>
```


## When to use stores

Prior to Svelte 5, stores were the go-to solution for creating cross-component reactive states or extracting logic. With runes, these use cases have greatly diminished.

- when extracting logic, it's better to take advantage of runes' universal reactivity: You can use runes outside the top level of components and even place them into JavaScript or TypeScript files (using a `.svelte.js` or `.svelte.ts` file ending)
- when creating shared state, you can create a `$state` object containing the values you need and then manipulate said state

```ts
/// file: state.svelte.js
export const userState = $state({
	name: 'name',
	/* ... */
});
```

```svelte
<!--- file: App.svelte --->
<script>
	import { userState } from './state.svelte.js';
</script>

<p>User name: {userState.name}</p>
<button onclick={() => {
	userState.name = 'new name';
}}>
	change name
</button>
```

Stores are still a good solution when you have complex asynchronous data streams or it's important to have more manual control over updating values or listening to changes. If you're familiar with RxJs and want to reuse that knowledge, the `$` also comes in handy for you.


## svelte/store

The `svelte/store` module contains a minimal store implementation which fulfil the store contract. It provides methods for creating stores that you can update from the outside, stores you can only update from the inside, and for combining and deriving stores.


## `writable`

Function that creates a store which has values that can be set from 'outside' components. It gets created as an object with additional `set` and `update` methods.

`set` is a method that takes one argument which is the value to be set. The store value gets set to the value of the argument if the store value is not already equal to it.

`update` is a method that takes one argument which is a callback. The callback takes the existing store value as its argument and returns the new value to be set to the store.

```js
/// file: store.js
import { writable } from 'svelte/store';

const count = writable(0);

count.subscribe((value) => {
	console.log(value);
}); // logs '0'

count.set(1); // logs '1'

count.update((n) => n + 1); // logs '2'
```

If a function is passed as the second argument, it will be called when the number of subscribers goes from zero to one (but not from one to two, etc). That function will be passed a `set` function which changes the value of the store, and an `update` function which works like the `update` method on the store, taking a callback to calculate the store's new value from its old value. It must return a `stop` function that is called when the subscriber count goes from one to zero.

```js
/// file: store.js
import { writable } from 'svelte/store';

const count = writable(0, () => {
	console.log('got a subscriber');
	return () => console.log('no more subscribers');
});

count.set(1); // does nothing

const unsubscribe = count.subscribe((value) => {
	console.log(value);
}); // logs 'got a subscriber', then '1'

unsubscribe(); // logs 'no more subscribers'
```

Note that the value of a `writable` is lost when it is destroyed, for example when the page is refreshed. However, you can write your own logic to sync the value to for example the `localStorage`.


## `readable`

Creates a store whose value cannot be set from 'outside', the first argument is the store's initial value, and the second argument to `readable` is the same as the second argument to `writable`.

```ts
import { readable } from 'svelte/store';

const time = readable(new Date(), (set) => {
	set(new Date());

	const interval = setInterval(() => {
		set(new Date());
	}, 1000);

	return () => clearInterval(interval);
});

const ticktock = readable('tick', (set, update) => {
	const interval = setInterval(() => {
		update((sound) => (sound === 'tick' ? 'tock' : 'tick'));
	}, 1000);

	return () => clearInterval(interval);
});
```


## `derived`

Derives a store from one or more other stores. The callback runs initially when the first subscriber subscribes and then whenever the store dependencies change.

In the simplest version, `derived` takes a single store, and the callback returns a derived value.

```ts
// @filename: ambient.d.ts
import { type Writable } from 'svelte/store';

declare global {
	const a: Writable<number>;
}

export {};

// @filename: index.ts
// ---cut---
import { derived } from 'svelte/store';

const doubled = derived(a, ($a) => $a * 2);
```

The callback can set a value asynchronously by accepting a second argument, `set`, and an optional third argument, `update`, calling either or both of them when appropriate.

In this case, you can also pass a third argument to `derived` — the initial value of the derived store before `set` or `update` is first called. If no initial value is specified, the store's initial value will be `undefined`.

```ts
// @filename: ambient.d.ts
import { type Writable } from 'svelte/store';

declare global {
	const a: Writable<number>;
}

export {};

// @filename: index.ts
// @errors: 18046 2769 7006
// ---cut---
import { derived } from 'svelte/store';

const delayed = derived(
	a,
	($a, set) => {
		setTimeout(() => set($a), 1000);
	},
	2000
);

const delayedIncrement = derived(a, ($a, set, update) => {
	set($a);
	setTimeout(() => update((x) => x + 1), 1000);
	// every time $a produces a value, this produces two
	// values, $a immediately and then $a + 1 a second later
});
```

If you return a function from the callback, it will be called when a) the callback runs again, or b) the last subscriber unsubscribes.

```ts
// @filename: ambient.d.ts
import { type Writable } from 'svelte/store';

declare global {
	const frequency: Writable<number>;
}

export {};

// @filename: index.ts
// ---cut---
import { derived } from 'svelte/store';

const tick = derived(
	frequency,
	($frequency, set) => {
		const interval = setInterval(() => {
			set(Date.now());
		}, 1000 / $frequency);

		return () => {
			clearInterval(interval);
		};
	},
	2000
);
```

In both cases, an array of arguments can be passed as the first argument instead of a single store.

```ts
// @filename: ambient.d.ts
import { type Writable } from 'svelte/store';

declare global {
	const a: Writable<number>;
	const b: Writable<number>;
}

export {};

// @filename: index.ts

// ---cut---
import { derived } from 'svelte/store';

const summed = derived([a, b], ([$a, $b]) => $a + $b);

const delayed = derived([a, b], ([$a, $b], set) => {
	setTimeout(() => set($a + $b), 1000);
});
```


## `readonly`

This simple helper function makes a store readonly. You can still subscribe to the changes from the original one using this new readable store.

```js
import { readonly, writable } from 'svelte/store';

const writableStore = writable(1);
const readableStore = readonly(writableStore);

readableStore.subscribe(console.log);

writableStore.set(2); // console: 2
// @errors: 2339
readableStore.set(2); // ERROR
```


## `get`

Generally, you should read the value of a store by subscribing to it and using the value as it changes over time. Occasionally, you may need to retrieve the value of a store to which you're not subscribed. `get` allows you to do so.

> [!NOTE] This works by creating a subscription, reading the value, then unsubscribing. It's therefore not recommended in hot code paths.

```ts
// @filename: ambient.d.ts
import { type Writable } from 'svelte/store';

declare global {
	const store: Writable<string>;
}

export {};

// @filename: index.ts
// ---cut---
import { get } from 'svelte/store';

const value = get(store);
```


## Store contract

```ts
// @noErrors
store = { subscribe: (subscription: (value: any) => void) => (() => void), set?: (value: any) => void }
```

You can create your own stores without relying on [`svelte/store`](../svelte-store), by implementing the _store contract_:

1. A store must contain a `.subscribe` method, which must accept as its argument a subscription function. This subscription function must be immediately and synchronously called with the store's current value upon calling `.subscribe`. All of a store's active subscription functions must later be synchronously called whenever the store's value changes.
2. The `.subscribe` method must return an unsubscribe function. Calling an unsubscribe function must stop its subscription, and its corresponding subscription function must not be called again by the store.
3. A store may _optionally_ contain a `.set` method, which must accept as its argument a new value for the store, and which synchronously calls all of the store's active subscription functions. Such a store is called a _writable store_.

For interoperability with RxJS Observables, the `.subscribe` method is also allowed to return an object with an `.unsubscribe` method, rather than return the unsubscription function directly. Note however that unless `.subscribe` synchronously calls the subscription (which is not required by the Observable spec), Svelte will see the value of the store as `undefined` until it does.


## Context

Context allows components to access values owned by parent components without passing them down as props (potentially through many layers of intermediate components, known as 'prop-drilling').

By creating a `[get, set]` pair of functions with `createContext`, you can set the context in a parent component and get it in a child component:

<!-- codeblock:start {"title":"Context","selected":"context.ts"} -->
```svelte
<!--- file: App.svelte --->
<script>
	import Parent from './Parent.svelte';
	import Child from './Child.svelte';
</script>

<Parent>
	<Child />
</Parent>
```

```svelte
<!--- file: Parent.svelte --->
<script>
	import { setUserContext } from './context';

	let { children } = $props();

	setUserContext({ name: 'world' });
</script>

{@render children()}
```

```svelte
<!--- file: Child.svelte --->
<script>
	import { getUserContext } from './context';

	const user = getUserContext();
</script>

<h1>hello {user.name}, inside Child.svelte</h1>
```

```ts
/// file: context.ts
import { createContext } from 'svelte';

interface User {
	name: string;
}

export const [getUserContext, setUserContext] = createContext<User>();
```
<!-- codeblock:end -->

> [!NOTE] `createContext` was added in version 5.40. If you are using an earlier version of Svelte, you must use `setContext` and `getContext` instead.

This is particularly useful when `Parent.svelte` is not directly aware of `Child.svelte`, but instead renders it as part of a `children` [snippet](snippet) as shown above.


## `setContext` and `getContext`

As an alternative to `createContext`, you can use `setContext` and `getContext` directly. The parent component sets context with `setContext(key, value)`...

```svelte
<!--- file: Parent.svelte --->
<script>
	import { setContext } from 'svelte';

	setContext('my-context', 'hello from Parent.svelte');
</script>
```

...and the child retrieves it with `getContext`:

```svelte
<!--- file: Child.svelte --->
<script>
	import { getContext } from 'svelte';

	const message = getContext('my-context');
</script>

<h1>{message}, inside Child.svelte</h1>
```

The key (`'my-context'`, in the example above) and the context itself can be any JavaScript value.

> [!NOTE] `createContext` is preferred since it provides better type safety and makes it unnecessary to use keys.

In addition to [`setContext`](svelte#setContext) and [`getContext`](svelte#getContext), Svelte exposes [`hasContext`](svelte#hasContext) and [`getAllContexts`](svelte#getAllContexts) functions.


## Using context with state

You can store reactive state in context...

<!-- codeblock:start {"title":"Context with state"} -->
```svelte
<!--- file: App.svelte --->
<script>
	import { setCounter } from './context.ts';
	import Child from './Child.svelte';

	let counter = $state({
		count: 0
	});

	setCounter(counter);
</script>

<button onclick={() => counter.count += 1}>
	increment
</button>

<Child />
<Child />
<Child />

<button onclick={() => counter.count = 0}>
	reset
</button>
```

```svelte
<!--- file: Child.svelte --->
<script>
	import { getCounter } from './context.ts';

	const counter = getCounter();
</script>

<p>{counter.count}</p>
```

```ts
/// file: context.ts
import { createContext } from 'svelte';

interface Counter {
	count: number;
}

export const [getCounter, setCounter] = createContext<Counter>();
```
<!-- codeblock:end -->

...though note that if you _reassign_ `counter` instead of updating it, you will 'break the link' — in other words instead of this...

```svelte
<button onclick={() => counter = { count: 0 } }>
	reset
</button>
```

...you must do this:

```svelte
<button onclick={() => +++counter.count = 0+++}>
	reset
</button>
```

Svelte will warn you if you get it wrong.

Similarly, to pass primitive values through context, use functions as described in [Passing state into functions]($state#Passing-state-into-functions).


## Component testing

When writing [component tests](testing#Unit-and-component-tests-with-Vitest-Component-testing), it can be useful to create a wrapper component that sets the context in order to check the behaviour of a component that uses it. As of version 5.49, you can do this sort of thing:

```js
import { mount, unmount } from 'svelte';
import { expect, test } from 'vitest';
import { setUserContext } from './context';
import MyComponent from './MyComponent.svelte';

test('MyComponent', () => {
	function Wrapper(...args) {
		setUserContext({ name: 'Bob' });
		return MyComponent(...args);
	}

	const component = mount(Wrapper, {
		target: document.body
	});

	expect(document.body.innerHTML).toBe('<h1>Hello Bob!</h1>');

	unmount(component);
});
```

This approach also works with [`hydrate`](imperative-component-api#hydrate) and [`render`](imperative-component-api#render).


## Replacing global state

When you have state shared by many different components, you might be tempted to put it in its own module and just import it wherever it's needed:

```js
/// file: state.svelte.js
export const myGlobalState = $state({
	user: {
		// ...
	}
	// ...
});
```

In many cases this is perfectly fine, but there is a risk: if you mutate the state during server-side rendering (which is discouraged, but entirely possible!)...

```svelte
<!--- file: App.svelte --->
<script>
	import { myGlobalState } from './state.svelte.js';

	let { data } = $props();

	if (data.user) {
		myGlobalState.user = data.user;
	}
</script>
```

...then the data may be accessible by the _next_ user. Context solves this problem because it is not shared between requests.


## Lifecycle hooks

<!-- - onMount/onDestroy
- mention that `$effect` might be better for your use case
- beforeUpdate/afterUpdate with deprecation notice?
- or skip this entirely and only have it in the reference docs? -->

In Svelte 5, the component lifecycle consists of only two parts: Its creation and its destruction. Everything in-between — when certain state is updated — is not related to the component as a whole; only the parts that need to react to the state change are notified. This is because under the hood the smallest unit of change is actually not a component, it's the (render) effects that the component sets up upon component initialization. Consequently, there's no such thing as a "before update"/"after update" hook.


## `onMount`

The `onMount` function schedules a callback to run as soon as the component has been mounted to the DOM. It must be called during the component's initialisation (but doesn't need to live _inside_ the component; it can be called from an external module).

`onMount` does not run inside a component that is rendered on the server.

```svelte
<script>
	import { onMount } from 'svelte';

	onMount(() => {
		console.log('the component has mounted');
	});
</script>
```

If a function is returned from `onMount`, it will be called when the component is unmounted.

```svelte
<script>
	import { onMount } from 'svelte';

	onMount(() => {
		const interval = setInterval(() => {
			console.log('beep');
		}, 1000);

		return () => clearInterval(interval);
	});
</script>
```

> [!NOTE] This behaviour will only work when the function passed to `onMount` is _synchronous_. `async` functions always return a `Promise`.


## `onDestroy`

Schedules a callback to run immediately before the component is unmounted.

Out of `onMount`, `beforeUpdate`, `afterUpdate` and `onDestroy`, this is the only one that runs inside a server-side component.

```svelte
<script>
	import { onDestroy } from 'svelte';

	onDestroy(() => {
		console.log('the component is being destroyed');
	});
</script>
```


## `tick`

While there's no "after update" hook, you can use `tick` to ensure that the UI is updated before continuing. `tick` returns a promise that resolves once any pending state changes have been applied, or in the next microtask if there are none.

```svelte
<script>
	import { tick } from 'svelte';

	$effect.pre(() => {
		console.log('the component is about to update');
		tick().then(() => {
				console.log('the component just updated');
		});
	});
</script>
```


## Deprecated: `beforeUpdate` / `afterUpdate`

Svelte 4 contained hooks that ran before and after the component as a whole was updated. For backwards compatibility, these hooks were shimmed in Svelte 5 but not available inside components that use runes.

```svelte
<script>
	import { beforeUpdate, afterUpdate } from 'svelte';

	beforeUpdate(() => {
		console.log('the component is about to update');
	});

	afterUpdate(() => {
		console.log('the component just updated');
	});
</script>
```

Instead of `beforeUpdate` use `$effect.pre` and instead of `afterUpdate` use `$effect` instead — these runes offer more granular control and only react to the changes you're actually interested in.


## Chat window example

To implement a chat window that autoscrolls to the bottom when new messages appear (but only if you were _already_ scrolled to the bottom), we need to measure the DOM before we update it.

In Svelte 4, we do this with `beforeUpdate`, but this is a flawed approach — it fires before _every_ update, whether it's relevant or not. In the example below, we need to introduce checks like `updatingMessages` to make sure we don't mess with the scroll position when someone toggles dark mode.

With runes, we can use `$effect.pre`, which behaves the same as `$effect` but runs before the DOM is updated. As long as we explicitly reference `messages` inside the effect body, it will run whenever `messages` changes, but _not_ when `theme` changes.

`beforeUpdate`, and its equally troublesome counterpart `afterUpdate`, are therefore deprecated in Svelte 5.

- Before
- After

<!-- prettier-ignore -->
```svelte
<script>
	import { ---beforeUpdate, afterUpdate,--- tick } from 'svelte';

	---let updatingMessages = false;---
	let theme = +++$state('dark')+++;
	let messages = +++$state([])+++;

	let viewport;

	---beforeUpdate(() => {---
	+++$effect.pre(() => {+++
		---if (!updatingMessages) return;---
		+++messages;+++
		const autoscroll = viewport && viewport.offsetHeight + viewport.scrollTop > viewport.scrollHeight - 50;

		if (autoscroll) {
			tick().then(() => {
				viewport.scrollTo(0, viewport.scrollHeight);
			});
		}

		---updatingMessages = false;---
	});

	function handleKeydown(event) {
		if (event.key === 'Enter') {
			const text = event.target.value;
			if (!text) return;

			---updatingMessages = true;---
			messages = [...messages, text];
			event.target.value = '';
		}
	}

	function toggle() {
		theme = theme === 'dark' ? 'light' : 'dark';
	}
</script>

<div class:dark={theme === 'dark'}>
	<div bind:this={viewport}>
		{#each messages as message}
			<p>{message}</p>
		{/each}
	</div>

	<input +++onkeydown+++={handleKeydown} />

	<button +++onclick+++={toggle}> Toggle dark mode </button>
</div>
```


## Imperative component API

<!-- better title needed?

- mount
- unmount
- render
- hydrate
- how they interact with each other -->

Every Svelte application starts by imperatively creating a root component. On the client this component is mounted to a specific element. On the server, you want to get back a string of HTML instead which you can render. The following functions help you achieve those tasks.


## `mount`

Instantiates a component and mounts it to the given target:

```js
// @errors: 2322
import { mount } from 'svelte';
import App from './App.svelte';

const app = mount(App, {
	target: document.querySelector('#app'),
	props: { some: 'property' }
});
```

You can mount multiple components per page, and you can also mount from within your application, for example when creating a tooltip component and attaching it to the hovered element.

Note that unlike calling `new App(...)` in Svelte 4, things like effects (including `onMount` callbacks, and action functions) will not run during `mount`. If you need to force pending effects to run (in the context of a test, for example) you can do so with `flushSync()`.


## `unmount`

Unmounts a component that was previously created with [`mount`](#mount) or [`hydrate`](#hydrate).

If `options.outro` is `true`, [transitions](transition) will play before the component is removed from the DOM:

```js
import { mount, unmount } from 'svelte';
import App from './App.svelte';

const app = mount(App, { target: document.body });

// later
unmount(app, { outro: true });
```

Returns a `Promise` that resolves after transitions have completed if `options.outro` is true, or immediately otherwise.


## `render`

Only available on the server and when compiling with the `server` option. Takes a component and returns an object with `body` and `head` properties on it, which you can use to populate the HTML when server-rendering your app:

```js
// @errors: 2724 2305 2307
import { render } from 'svelte/server';
import App from './App.svelte';

const result = render(App, {
	props: { some: 'property' }
});
result.body; // HTML for somewhere in this <body> tag
result.head; // HTML for somewhere in this <head> tag
```


## `hydrate`

Like `mount`, but will reuse up any HTML rendered by Svelte's SSR output (from the [`render`](#render) function) inside the target and make it interactive:

```js
// @errors: 2322
import { hydrate } from 'svelte';
import App from './App.svelte';

const app = hydrate(App, {
	target: document.querySelector('#app'),
	props: { some: 'property' }
});
```

As with `mount`, effects will not run during `hydrate` — use `flushSync()` immediately afterwards if you need them to.


## Hydratable data

In Svelte, when you want to render asynchronous content data on the server, you can simply `await` it. This is great! However, it comes with a pitfall: when hydrating that content on the client, Svelte has to redo the asynchronous work, which blocks hydration for however long it takes:

```svelte
<script>
  import { getUser } from 'my-database-library';

  // This will get the user on the server, render the user's name into the h1,
  // and then, during hydration on the client, it will get the user _again_,
  // blocking hydration until it's done.
  const user = await getUser();
</script>

<h1>{user.name}</h1>
```

That's silly, though. If we've already done the hard work of getting the data on the server, we don't want to get it again during hydration on the client. `hydratable` is a low-level API built to solve this problem. You probably won't need this very often — it will be used behind the scenes by whatever datafetching library you use. For example, it powers [remote functions in SvelteKit](/docs/kit/remote-functions).

To fix the example above:

```svelte
<script>
  import { hydratable } from 'svelte';
  import { getUser } from 'my-database-library';

  // During server rendering, this will serialize and stash the result of `getUser`, associating
  // it with the provided key and baking it into the `head` content. During hydration, it will
  // look for the serialized version, returning it instead of running `getUser`. After hydration
  // is done, if it's called again, it'll simply invoke `getUser`.
  const user = await hydratable('user', () => getUser());
</script>

<h1>{user.name}</h1>
```

This API can also be used to provide access to random or time-based values that are stable between server rendering and hydration. For example, to get a random number that doesn't update on hydration:

```ts
import { hydratable } from 'svelte';
const rand = hydratable('random', () => Math.random());
```

If you're a library author, be sure to prefix the keys of your `hydratable` values with the name of your library so that your keys don't conflict with other libraries.


## Serialization

All data returned from a `hydratable` function must be serializable. But this doesn't mean you're limited to JSON — Svelte uses [`devalue`](https://npmjs.com/package/devalue), which can serialize all sorts of things including `Map`, `Set`, `URL`, and `BigInt`. Check the documentation page for a full list. In addition to these, thanks to some Svelte magic, you can also fearlessly use promises:

```svelte
<script>
  import { hydratable } from 'svelte';
  const promises = hydratable('random', () => {
    return {
      one: Promise.resolve(1),
      two: Promise.resolve(2)
    }
  });
</script>

{await promises.one}
{await promises.two}
```


## CSP

`hydratable` adds an inline `<script>` block to the `head` returned from `render`. If you're using [Content Security Policy](https://developer.mozilla.org/en-US/docs/Web/HTTP/Guides/CSP) (CSP), this script will likely fail to run. You can provide a `nonce` to `render`:

```js
/// file: server.js
import { render } from 'svelte/server';
import App from './App.svelte';
// ---cut---
const nonce = crypto.randomUUID();

const { head, body } = await render(App, {
	csp: { nonce }
});
```

This will add the `nonce` to the script block, on the assumption that you will later add the same nonce to the CSP header of the document that contains it:

```js
/// file: server.js
let response = new Response();
let nonce = 'xyz123';
// ---cut---
response.headers.set(
  'Content-Security-Policy',
  `script-src 'nonce-${nonce}'`
 );
```

It's essential that a `nonce` — which, British slang definition aside, means 'number used once' — is only used when dynamically server rendering an individual response.

If instead you are generating static HTML ahead of time, you must use hashes instead:

```js
/// file: server.js
import { render } from 'svelte/server';
import App from './App.svelte';
// ---cut---
const { head, body, hashes } = await render(App, {
	csp: { hash: true }
});
```

`hashes.script` will be an array of strings like `["sha256-abcd123"]`. As with `nonce`, the hashes should be used in your CSP header:

```js
/// file: server.js
let response = new Response();
let hashes = { script: ['sha256-xyz123'] };
// ---cut---
response.headers.set(
  'Content-Security-Policy',
  `script-src ${hashes.script.map((hash) => `'${hash}'`).join(' ')}`
 );
```

We recommend using `nonce` over hash if you can, as `hash` will interfere with streaming SSR in the future.


## Best practices



## `$state`

Only use the `$state` rune for variables that should be _reactive_ — in other words, variables that cause an `$effect`, `$derived` or template expression to update. Everything else can be a normal variable.

Objects and arrays (`$state({...})` or `$state([...])`) are made deeply reactive, meaning mutation will trigger updates. This has a trade-off: in exchange for fine-grained reactivity, the objects must be proxied, which has performance overhead. In cases where you're dealing with large objects that are only ever reassigned (rather than mutated), use `$state.raw` instead. This is often the case with API responses, for example.


## `$derived`

To compute something from state, use `$derived` rather than `$effect`:

```js
// @errors: 2451
let num = 0;
// ---cut---
// do this
let square = $derived(num * num);

// don't do this
let square;

$effect(() => {
	square = num * num;
});
```

> [!NOTE] `$derived` is given an expression, _not_ a function. If you need to use a function (because the expression is complex, for example) use `$derived.by`.

Deriveds are writable — you can assign to them, just like `$state`, except that they will re-evaluate when their expression changes.

If the derived expression is an object or array, it will be returned as-is — it is _not_ made deeply reactive. You can, however, use `$state` inside `$derived.by` in the rare cases that you need this.


## `$effect`

Effects are an escape hatch and should mostly be avoided. In particular, avoid updating state inside effects.

- If you need to sync state to an external library such as D3, it is often neater to use [`{@attach ...}`](@attach)
- If you need to run some code in response to user interaction, put the code directly in an event handler or use a [function binding](bind#Function-bindings) as appropriate
- If you need to log values for debugging purposes, use [`$inspect`]($inspect)
- If you need to observe something external to Svelte, use [`createSubscriber`](svelte-reactivity#createSubscriber)

Never wrap the contents of an effect in `if (browser) {...}` or similar — effects do not run on the server.


## `$props`

Treat props as though they will change. For example, values that depend on props should usually use `$derived`:

```js
// @errors: 2451
let { type } = $props();

// do this
let color = $derived(type === 'danger' ? 'red' : 'green');

// don't do this — `color` will not update if `type` changes
let color = type === 'danger' ? 'red' : 'green';
```


## `$inspect.trace`

`$inspect.trace` is a debugging tool for reactivity. If something is not updating properly or running more than it should you can add `$inspect.trace(label)` as the first line of an `$effect` or `$derived.by` (or any function they call) to trace their dependencies and discover which one triggered an update.


## Events

Any element attribute starting with `on` is treated as an event listener:

```svelte
<button onclick={() => {...}}>click me</button>

<!-- attribute shorthand also works -->
<button {onclick}>...</button>

<!-- so do spread attributes -->
<button {...props}>...</button>
```

If you need to attach listeners to `window` or `document` you can use `<svelte:window>` and `<svelte:document>`:

```svelte
<svelte:window onkeydown={...} />
<svelte:document onvisibilitychange={...} />
```

Avoid using `onMount` or `$effect` for this.


## Snippets

[Snippets](snippet) are a way to define reusable chunks of markup that can be instantiated with the [`{@render ...}`](@render) tag, or passed to components as props. They must be declared within the template.

```svelte
{#snippet greeting(name)}
  <p>hello {name}!</p>
{/snippet}

{@render greeting('world')}
```

> [!NOTE] Snippets declared at the top level of a component (i.e. not inside elements or blocks) can be referenced inside `<script>`. A snippet that doesn't reference component state is also available in a `<script module>`, in which case it can be exported for use by other components.


## Each blocks

Prefer to use [keyed each blocks](each#Keyed-each-blocks) — this improves performance by allowing Svelte to surgically insert or remove items rather than updating the DOM belonging to existing items.

> [!NOTE] The key _must_ uniquely identify the object. Do not use the index as a key.

Avoid destructuring if you need to mutate the item (with something like `bind:value={item.count}`, for example).


## Using JavaScript variables in CSS

If you have a JS variable that you want to use inside CSS you can set a CSS custom property with the `style:` directive.

```svelte
<div style:--columns={columns}>...</div>
```

You can then reference `var(--columns)` inside the component's `<style>`.


## Styling child components

The CSS in a component's `<style>` is scoped to that component. If a parent component needs to control the child's styles, the preferred way is to use CSS custom properties:

```svelte
<!-- Parent.svelte -->
<Child --color="red" />

<!-- Child.svelte -->
<h1>Hello</h1>

<style>
	h1 {
		color: var(--color);
	}
</style>
```

If this is impossible (for example, the child component comes from a library) you can use `:global` to override styles:

```svelte
<div>
	<Child />
</div>

<style>
	div :global {
		h1 {
			color: red;
		}
	}
</style>
```


## Context

Consider using context instead of declaring state in a shared module. This will scope the state to the part of the app that needs it, and eliminate the possibility of it leaking between users when server-side rendering.

Use `createContext` rather than `setContext` and `getContext`, as it provides type safety.


## Async Svelte

If using version 5.36 or higher, you can use [await expressions](await-expressions) and [hydratable](hydratable) to use promises directly inside components. Note that these require the `experimental.async` option to be enabled in `svelte.config.js` as they are not yet considered fully stable.


## Avoid legacy features

Always use runes mode for new code, and avoid features that have more modern replacements:

- use `$state` instead of implicit reactivity (e.g. `let count = 0; count += 1`)
- use `$derived` and `$effect` instead of `$:` assignments and statements (but only use effects when there is no better solution)
- use `$props` instead of `export let`, `$$props` and `$$restProps`
- use `onclick={...}` instead of `on:click={...}`
- use `{#snippet ...}` and `{@render ...}` instead of `<slot>` and `$$slots` and `<svelte:fragment>`
- use `<DynamicComponent>` instead of `<svelte:component this={DynamicComponent}>`
- use `import Self from './ThisComponent.svelte'` and `<Self>` instead of `<svelte:self>`
- use classes with `$state` fields to share reactivity between components, instead of using stores
- use `{@attach ...}` instead of `use:action`
- use clsx-style arrays and objects in `class` attributes, instead of the `class:` directive


## Testing

Testing helps you write and maintain your code and guard against regressions. Testing frameworks help you with that, allowing you to describe assertions or expectations about how your code should behave. Svelte is unopinionated about which testing framework you use — you can write unit tests, integration tests, and end-to-end tests using solutions like [Vitest](https://vitest.dev/), [Jasmine](https://jasmine.github.io/), [Cypress](https://www.cypress.io/) and [Playwright](https://playwright.dev/).


## Unit and component tests with Vitest

Unit tests allow you to test small isolated parts of your code. Integration tests allow you to test parts of your application to see if they work together. If you're using Vite (including via SvelteKit), we recommend using [Vitest](https://vitest.dev/). You can use the Svelte CLI to [setup Vitest](/docs/cli/vitest) either during project creation or later on.

To setup Vitest manually, first install it:

```sh
npm install -D vitest
```

Then adjust your `vite.config.js`:

<!-- prettier-ignore -->
```js
/// file: vite.config.js
import { defineConfig } from +++'vitest/config'+++;

export default defineConfig({
	// ...
	// Tell Vitest to use the `browser` entry points in `package.json` files, even though it's running in Node
	resolve: process.env.VITEST
		? {
				conditions: ['browser']
			}
		: undefined
});
```

> [!NOTE] If loading the browser version of all your packages is undesirable, because (for example) you also test backend libraries, [you may need to resort to an alias configuration](https://github.com/testing-library/svelte-testing-library/issues/222#issuecomment-1909993331)

You can now write unit tests for code inside your `.js/.ts` files:

```js
/// file: multiplier.svelte.test.js
// @filename: multiplier.svelte.ts
export function multiplier(initial: number, k: number) {
	let count = $state(initial);

	return {
		get value() {
			return count * k;
		},
		set: (c: number) => {
			count = c;
		}
	};
}
// @filename: multiplier.svelte.test.js
// ---cut---
import { flushSync } from 'svelte';
import { expect, test } from 'vitest';
import { multiplier } from './multiplier.svelte.js';

test('Multiplier', () => {
	let double = multiplier(0, 2);

	expect(double.value).toEqual(0);

	double.set(5);

	expect(double.value).toEqual(10);
});
```

```js
/// file: multiplier.svelte.js
/**
 * @param {number} initial
 * @param {number} k
 */
export function multiplier(initial, k) {
	let count = $state(initial);

	return {
		get value() {
			return count * k;
		},
		/** @param {number} c */
		set: (c) => {
			count = c;
		}
	};
}
```


## Using runes inside your test files

Since Vitest processes your test files the same way as your source files, you can use runes inside your tests as long as the filename includes `.svelte`:

```js
/// file: multiplier.svelte.test.js
// @filename: multiplier.svelte.ts
export function multiplier(getCount: () => number, k: number) {
	return {
		get value() {
			return getCount() * k;
		}
	};
}
// @filename: multiplier.svelte.test.js
// ---cut---
import { flushSync } from 'svelte';
import { expect, test } from 'vitest';
import { multiplier } from './multiplier.svelte.js';

test('Multiplier', () => {
	let count = $state(0);
	let double = multiplier(() => count, 2);

	expect(double.value).toEqual(0);

	count = 5;

	expect(double.value).toEqual(10);
});
```

```js
/// file: multiplier.svelte.js
/**
 * @param {() => number} getCount
 * @param {number} k
 */
export function multiplier(getCount, k) {
	return {
		get value() {
			return getCount() * k;
		}
	};
}
```

If the code being tested uses effects, you need to wrap the test inside `$effect.root`:

```js
/// file: logger.svelte.test.js
// @filename: logger.svelte.ts
export function logger(fn: () => void) {}
// @filename: logger.svelte.test.js
// ---cut---
import { flushSync } from 'svelte';
import { expect, test } from 'vitest';
import { logger } from './logger.svelte.js';

test('Effect', () => {
	const cleanup = $effect.root(() => {
		let count = $state(0);

		// logger uses an $effect to log updates of its input
		let log = logger(() => count);

		// effects normally run after a microtask,
		// use flushSync to execute all pending effects synchronously
		flushSync();
		expect(log).toEqual([0]);

		count = 1;
		flushSync();

		expect(log).toEqual([0, 1]);
	});

	cleanup();
});
```

```js
/// file: logger.svelte.js
/**
 * @param {() => any} getValue
 */
export function logger(getValue) {
	/** @type {any[]} */
	let log = [];

	$effect(() => {
		log.push(getValue());
	});

	return log;
}
```


## Component testing

It is possible to test your components in isolation, which allows you to render them in a browser (real or simulated), simulate behavior, and make assertions, without spinning up your whole app.

> [!NOTE] Before writing component tests, think about whether you actually need to test the component, or if it's more about the logic _inside_ the component. If so, consider extracting out that logic to test it in isolation, without the overhead of a component.

To get started, install jsdom (a library that shims DOM APIs):

```sh
npm install -D jsdom
```

Then adjust your `vite.config.js`:

```js
/// file: vite.config.js
import { defineConfig } from 'vitest/config';

export default defineConfig({
	plugins: [
		/* ... */
	],
	test: {
		// If you are testing components client-side, you need to set up a DOM environment.
		// If not all your files should have this environment, you can use a
		// `// @vitest-environment jsdom` comment at the top of the test files instead.
		environment: 'jsdom'
	},
	// Tell Vitest to use the `browser` entry points in `package.json` files, even though it's running in Node
	resolve: process.env.VITEST
		? {
				conditions: ['browser']
			}
		: undefined
});
```

After that, you can create a test file in which you import the component to test, interact with it programmatically and write expectations about the results:

```js
/// file: component.test.js
import { flushSync, mount, unmount } from 'svelte';
import { expect, test } from 'vitest';
import Component from './Component.svelte';

test('Component', () => {
	// Instantiate the component using Svelte's `mount` API
	const component = mount(Component, {
		target: document.body, // `document` exists because of jsdom
		props: { initial: 0 }
	});

	expect(document.body.innerHTML).toBe('<button>0</button>');

	// Click the button, then flush the changes so you can synchronously write expectations
	document.body.querySelector('button')?.click();
	flushSync();

	expect(document.body.innerHTML).toBe('<button>1</button>');

	// Remove the component from the DOM
	unmount(component);
});
```

While the process is very straightforward, it is also low level and somewhat brittle, as the precise structure of your component may change frequently. Tools like [@testing-library/svelte](https://testing-library.com/docs/svelte-testing-library/intro/) can help streamline your tests. The above test could be rewritten like this:

```js
// @errors: 2339
/// file: component.test.js
import { render, screen } from '@testing-library/svelte';
import userEvent from '@testing-library/user-event';
import { expect, test } from 'vitest';
import Component from './Component.svelte';

test('Component', async () => {
	const user = userEvent.setup();
	render(Component);

	const button = screen.getByRole('button');
	expect(button).toHaveTextContent(0);

	await user.click(button);
	expect(button).toHaveTextContent(1);
});
```

When writing component tests that involve two-way bindings, context or snippet props, it's best to create a wrapper component for your specific test and interact with that. `@testing-library/svelte` contains some [examples](https://testing-library.com/docs/svelte-testing-library/example).


## Component tests with Storybook

[Storybook](https://storybook.js.org) is a tool for developing and documenting UI components, and it can also be used to test your components. They're run with Vitest's browser mode, which renders your components in a real browser for the most realistic testing environment.

To get started, first install Storybook ([using Svelte's CLI](/docs/cli/storybook)) in your project via `npx sv add storybook` and choose the recommended configuration that includes testing features. If you're already using Storybook, and for more information on Storybook's testing capabilities, follow the [Storybook testing docs](https://storybook.js.org/docs/writing-tests?renderer=svelte) to get started.

You can create stories for component variations and test interactions with the [play function](https://storybook.js.org/docs/writing-tests/interaction-testing?renderer=svelte#writing-interaction-tests), which allows you to simulate behavior and make assertions using the Testing Library and Vitest APIs. Here's an example of two stories that can be tested, one that renders an empty LoginForm component and one that simulates a user filling out the form:

```svelte
/// file: LoginForm.stories.svelte
<script module>
	import { defineMeta } from '@storybook/addon-svelte-csf';
	import { expect, fn } from 'storybook/test';

	import LoginForm from './LoginForm.svelte';

	const { Story } = defineMeta({
		component: LoginForm,
		args: {
			// Pass a mock function to the `onSubmit` prop
			onSubmit: fn(),
		}
	});
</script>

<Story name="Empty Form" />

<Story
	name="Filled Form"
	play={async ({ args, canvas, userEvent }) => {
		// Simulate a user filling out the form
		await userEvent.type(canvas.getByTestId('email'), 'email@provider.com');
		await userEvent.type(canvas.getByTestId('password'), 'a-random-password');
		await userEvent.click(canvas.getByRole('button'));

		// Run assertions
		await expect(args.onSubmit).toHaveBeenCalledTimes(1);
		await expect(canvas.getByText('You’re in!')).toBeInTheDocument();
	}}
/>
```


## End-to-end tests with Playwright

E2E (short for 'end to end') tests allow you to test your full application through the eyes of the user. This section uses [Playwright](https://playwright.dev/) as an example, but you can also use other solutions like [Cypress](https://www.cypress.io/) or [NightwatchJS](https://nightwatchjs.org/).

You can use the Svelte CLI to [setup Playwright](/docs/cli/playwright) either during project creation or later on. You can also [set it up with `npm init playwright`](https://playwright.dev/docs/intro). Additionally, you may also want to install an IDE plugin such as [the VS Code extension](https://playwright.dev/docs/getting-started-vscode) to be able to execute tests from inside your IDE.

If you've run `npm init playwright` or are not using Vite, you may need to adjust the Playwright config to tell Playwright what to do before running the tests — mainly starting your application at a certain port. For example:

```js
/// file: playwright.config.js
const config = {
	webServer: {
		command: 'npm run build && npm run preview',
		port: 4173
	},
	testDir: 'tests',
	testMatch: /(.+\.)?(test|spec)\.[jt]s/
};

export default config;
```

You can now start writing tests. These are totally unaware of Svelte as a framework, so you mainly interact with the DOM and write assertions.

```js
// @errors: 2307 7031
/// file: tests/hello-world.spec.js
import { expect, test } from '@playwright/test';

test('home page has expected h1', async ({ page }) => {
	await page.goto('/');
	await expect(page.locator('h1')).toBeVisible();
});
```


## TypeScript

<!-- - [basically what we have today](https://svelte.dev/docs/typescript)
- built-in support, but only for type-only features
- generics
- using `Component` and the other helper types
- using `svelte-check` -->

You can use TypeScript within Svelte components. IDE extensions like the [Svelte VS Code extension](https://marketplace.visualstudio.com/items?itemName=svelte.svelte-vscode) will help you catch errors right in your editor, and [`svelte-check`](https://www.npmjs.com/package/svelte-check) does the same on the command line, which you can integrate into your CI.


## `<script lang="ts">`

To use TypeScript inside your Svelte components, add `lang="ts"` to your `script` tags:

```svelte
<script lang="ts">
	let name: string = 'world';

	function greet(name: string) {
		alert(`Hello, ${name}!`);
	}
</script>

<button onclick={(e: Event) => greet(e.target.innerText)}>
	{name as string}
</button>
```

Doing so allows you to use TypeScript's _type-only_ features. That is, all features that just disappear when transpiling to JavaScript, such as type annotations or interface declarations. Features that require the TypeScript compiler to output actual code are not supported. This includes:

- using enums
- using `private`, `protected` or `public` modifiers in constructor functions together with initializers
- using features that are not yet part of the ECMAScript standard (i.e. not level 4 in the TC39 process) and therefore not implemented yet within Acorn, the parser we use for parsing JavaScript

If you want to use one of these features, you need to setup up a `script` preprocessor.


## Preprocessor setup

To use non-type-only TypeScript features within Svelte components, you need to add a preprocessor that will turn TypeScript into JavaScript.


## Using Vite

If you're using SvelteKit, or Vite _without_ SvelteKit, you can use `vitePreprocess` from `@sveltejs/vite-plugin-svelte` in your config file:

```ts
/// file: svelte.config.js
// @noErrors
import { vitePreprocess } from '@sveltejs/vite-plugin-svelte';

const config = {
	// Note the additional `{ script: true }`
	preprocess: vitePreprocess({ script: true })
};

export default config;
```


## Using other build tools

If you're using tools like Rollup (via [rollup-plugin-svelte](https://github.com/sveltejs/rollup-plugin-svelte)) or Webpack (via [svelte-loader](https://github.com/sveltejs/svelte-loader)) instead, install `typescript` and `svelte-preprocess` and add the preprocessor to the plugin config. See the respective plugin READMEs for more info.

> [!NOTE] If you're starting a new project, we recommend using SvelteKit or Vite instead


## tsconfig.json settings

When using TypeScript, make sure your `tsconfig.json` is setup correctly.

- Use a [`target`](https://www.typescriptlang.org/tsconfig/#target) of at least `ES2015` so classes are not compiled to functions
- Set [`verbatimModuleSyntax`](https://www.typescriptlang.org/tsconfig/#verbatimModuleSyntax) to `true` so that imports are left as-is
- Set [`isolatedModules`](https://www.typescriptlang.org/tsconfig/#isolatedModules) to `true` so that each file is looked at in isolation. TypeScript has a few features which require cross-file analysis and compilation, which the Svelte compiler and tooling like Vite don't do.


## Typing `$props`

Type `$props` just like a regular object with certain properties.

```svelte
<script lang="ts">
	import type { Snippet } from 'svelte';

	interface Props {
		requiredProperty: number;
		optionalProperty?: boolean;
		snippetWithStringArgument: Snippet<[string]>;
		eventHandler: (arg: string) => void;
		[key: string]: unknown;
	}

	let {
		requiredProperty,
		optionalProperty,
		snippetWithStringArgument,
		eventHandler,
		...everythingElse
	}: Props = $props();
</script>

<button onclick={() => eventHandler('clicked button')}>
	{@render snippetWithStringArgument('hello')}
</button>
```


## Generic `$props`

Components can declare a generic relationship between their properties. One example is a generic list component that receives a list of items and a callback property that receives an item from the list. To declare that the `items` property and the `select` callback operate on the same types, add the `generics` attribute to the `script` tag:

```svelte
<script lang="ts" generics="Item